# 培黎智寻 环境变量模板
#
# 使用方法：复制本文件为 .env，再按需取消注释并填写取值。
# .env 已被 .gitignore 排除，不会进入版本控制。
# 请勿在本文件中填写真实密钥、密码、Token 或个人绝对路径。
# M0 阶段后端无任何必需的环境变量，以下条目均为占位说明。

# ---- 后端 ----
# 后端服务监听端口（默认 8000）
# BACKEND_PORT=8000

# ---- 前端 ----
# 前端开发服务器端口（默认 5173）
# VITE_PORT=5173
