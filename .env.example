# 环境变量样例（哑值，不含真实秘密——00_MASTER_PLAN §4.5 秘密纪律）。
# 复制为 .env 并按本机实际修改；.env 已被 .gitignore 排除，禁止入库。
# Django 不自动加载 .env——先导出到 shell 再执行 manage.py / pytest：
#   set -a; source .env; set +a
#
# A1.1（03 计划 S1）：本地与测试统一 PostgreSQL（SQLite 不再是基线），
# 连接与敏感配置全部环境变量外置。
#
# 开发库（migrate / runserver 目标）
DATABASE_URL=postgres://peiligo_restart:CHANGE_ME@localhost:5432/peiligo_restart_dev
# 独立测试库：pytest-django 沿用此库名创建/销毁测试库（不加 test_ 前缀）
TEST_DATABASE_URL=postgres://peiligo_restart:CHANGE_ME@localhost:5432/peiligo_restart_test
# 密钥（dev 与 production 均必填；生产须为足够长的随机值，勿用样例值）
SECRET_KEY=django-insecure-change-me-not-a-real-secret
# 允许主机（production 必填，逗号分隔；dev 固定 ALLOWED_HOSTS=["*"]，不读取此项）
ALLOWED_HOSTS=example.com
# 后台通知邮件等全 URL 基准地址（production 必填；不带 /admin 与结尾斜杠，终审 L-1）
WAGTAILADMIN_BASE_URL=https://example.com
# 媒体文件根（可选；未设置时默认仓库内 media/）
MEDIA_ROOT=/tmp/peiligo_restart_media
# 统一反馈邮箱（M2.2 页脚反馈入口；哑值——正式载体为站点级配置，M3+ 终判）
FEEDBACK_EMAIL=feedback@example.com
