# D0 · 基线确认记录与决策台账（G0 批准固化）

| 项目 | 内容 |
| --- | --- |
| 文件编号 | D0_BASELINE_CONFIRMATION（决策门 D0「审计与文档基线确认」的固化产物；00_MASTER_PLAN §2.2 落点=G0、M0.1） |
| 所属步骤 | M0.1 · 基线确认记录与决策台账（00_MASTER_PLAN v1.4 §7 M0.1；引源=01 §5 PG-S1，其产出改为此文件） |
| 编制日期 | 2026-08-18 |
| 编制方式 | 单步骤 Dispatch（Orca 派发，单对话单产物）；本步为 Git 化前唯一步骤：不建 worktree、未执行任何 git 写操作（git init 属 M0.2） |
| 文件地位 | 本仓库（`peiligo_restart`）第一份决策记录；G0 批准事实、PRD 最终确认、覆盖规则、已确认决策与决策门处置的仓库内固化锚点（见 §8） |
| 上游依据 | 00_MASTER_PLAN v1.4（2026-08-18 G0 批准后冻结为执行基线）；项目负责人 G0 结构化裁决（2026-08-18，总控 Coordinator 会话）；`docs/reviews/M00.review.md`（M00 独立审查报告） |

## 1. G0 批准记录

**G0 于 2026-08-18 通过。** 项目负责人（用户）在总控 Coordinator 会话以结构化裁决完成，四条件处置如下：

- **条件①（独立审查）：满足**——`docs/reviews/M00.review.md`，独立 GLM 新会话（Orca dispatch ctx_2ece3894b991，全信隔离）R3 双口径审查，结论=通过，0×P0、0×P1、5×P2（P2 不阻塞，登记后续吸收）。
- **条件②（R3 级 GPT 压缩 Risk/Architecture Review）：以一次性 R3_GPT_WAIVER 豁免**——四要素原文：
  - TASK=M00（00_MASTER_PLAN v1.4 G0 前 GPT 压缩 Risk/Architecture Review）
  - REASON=GPT-5.6 Sol 当前不可用，独立 GLM 双口径审查已通过且项目负责人裁决继续
  - HUMAN_APPROVAL=项目负责人（用户）2026-08-18 总控会话结构化裁决
  - DATE=2026-08-18

  该 waiver 仅对 M00 这一个 Task 有效，不可复用、不可泛化，不影响其他 R3/R4 任务。
- **条件③（书面批准）：满足**——项目负责人 2026-08-18 批准——批准 G0，00_MASTER_PLAN v1.4 冻结为执行基线。
- **条件④（PRD 最终确认）：满足**——项目负责人 2026-08-18 确认 PRODUCT_REQUIREMENTS.md 为最终确认基线；PRD 文档头"待用户最终确认"状态就此清零（确认引用详见本文件 §2）。

**批准的直接后果**：OR-3 ①–④ 解禁（项目初始化/依赖安装/Git 初始化/AI coding）；⑤ PVE/生产在 G4 前仍逐项授权。

**背景附注**：阶段一执行范围经项目负责人同日裁决为 M0–M7 至 G2 门（A 阶段，29 步），作为背景记录附注，不改变 00_MASTER_PLAN 全 55 步总表（§5.2）效力。

## 2. PRD 最终确认引用

| 项 | 内容 |
| --- | --- |
| 确认人 | 项目负责人（用户） |
| 确认日期 | 2026-08-18 |
| 确认对象 | `docs/PRODUCT_REQUIREMENTS.md`（仓库相对路径） |
| 确认结论 | PRODUCT_REQUIREMENTS.md 为**最终确认基线**；文档头"待用户最终确认"状态就此清零 |
| 冻结区范围 | §1–§17、§20–§23（与 PRD 文档头自述口径一致：冻结区未改动） |
| 确认场景 | 项目负责人 G0 结构化裁决条件④（2026-08-18，总控 Coordinator 会话） |

### 2.1 附带微修订记录（G0 裁决③④授权；本步对 PRD 的唯一触碰）

- **修订依据**：项目负责人 G0 裁决③（批准 G0，00_MASTER_PLAN v1.4 冻结为执行基线）与裁决④（PRD 最终确认）；按 00_MASTER_PLAN §3.0 裁决顺序第 1 级「项目负责人（Human）最新指令」优先，总控 Coordinator 于 M0.1 派发令中显式授权本附带微修订（超出 M0.1 原始文件范围）。
- **修订范围**：仅 `docs/PRODUCT_REQUIREMENTS.md` 文档头表格「文档状态」一行；PRD 其余一切内容（尤其冻结区 §1–§17、§20–§23）字节不动。
- **diff 记录**（修订前 → 修订后）：

```diff
- | 文档状态 | 产品需求基线草案（待用户最终确认） |
+ | 文档状态 | 产品需求基线（已最终确认：项目负责人 2026-08-18 批准，G0 通过） |
```

## 3. 覆盖规则全文 OR-1..OR-6

（以下自 00_MASTER_PLAN v1.4 §1 逐字复制；块内「§」编号均指 00_MASTER_PLAN 自身章节，非本文件章节）

- **OR-1 唯一项目位置**：本项目唯一位置为 `<LOCAL_PROJECT_PATH>_restart`。后续一切 Git 仓库、worktree、分支、计划文档、开发、PoC、构建产物均且仅位于该目录内。
- **OR-2 旧仓永久只读**：旧目录 `<LOCAL_PROJECT_PATH>` 及其全部 worktree **永久只读**，仅可用于审计与选择性复用（复用范围=综合审计 §3 清单）。任何步骤不得安排：M4 处置、建立 tag/branch/commit、文档迁入旧仓、在旧仓开发、针对旧仓的对话或任何写操作。旧归档状态（含 `feat/m4-admin-auth` 工作区不干净、无只读 tag）**不阻塞本项目任何步骤**。
- **OR-3 基线确认前禁止清单**：在 G0（本计划批准）通过前，持续禁止：① 项目初始化（创建骨架/正式项目结构）；② 依赖安装（含任何 PoC venv、pip/uv 安装）；③ Git 初始化；④ AI coding（业务/骨架代码）；⑤ PVE/生产操作。**G0 通过后解禁 ①–④；⑤ 在 G4 前仍逐项授权**（§4.9）。01 §7 的临时秘密禁区规则全程生效（§4.5）。
- **OR-4 已确认决策即基线**：§2.1 所列已确认技术决策直接作为基线，任何步骤、审查、代理不得重开。
- **OR-5 不得扩大 V1**：PRD §20 "V1 明确不做"清单全项有效；任何步骤不得以任何理由引入扩围能力；试运行期新想法一律进入需求候选池（M0.6 产物）。
- **OR-6 冲突处理**：本计划 §3 修订清单已按 OR-1..OR-5 修正来源计划；执行中发现新的来源冲突时，停止并按 §3.0 裁决顺序上报，不得自行取舍，不得修改 01/02/03。

## 4. 已确认决策表 C1–C5

（以下自 00_MASTER_PLAN v1.4 §2.1 逐字复制，表格原样；块内「§」编号均指 00_MASTER_PLAN 自身章节）

### 已确认决策（C1–C5，冻结，不得重开）

| # | 决策 | 落点 |
| --- | --- | --- |
| C1 | 版本组合：Wagtail 7.4.x LTS + Django 5.2 LTS + Python 3.13。首次锁依赖时（M1.2 / MB1）复核最新安全补丁后冻结具体补丁号；约束 `wagtail>=7.4.2,<7.5`、`django>=5.2,<5.3`、`modelsearch>=1.3.2,<1.4`；不采用 8.0 线（含 RC）与 `main` | M1.2 / ADR-0001 |
| C2 | 前端架构：Wagtail 服务端模板（Django 模板，无 SPA） | M1.3 / ADR-0002 |
| C3 | 权限结构：部门权限容器树——各部门容器节点承载权限；**无前台部门主页**：容器节点前台返回 404（或等效不可达），不得进入导航、站点地图、默认列表与默认搜索结果 | M1.5 / ADR-0004、M2.1、M4.2/M4.3 |
| C4 | 内容载体：所有面向学生且需独立 URL/预览/SEO/发布/搜索的通知、文章、活动、学习资料、软件工具、校园指南均为 **Page**；部门/受控类别/标签/站点配置用 **Snippet/设置**。活动类通过 Notice/Article Page 上的 EventFieldsMixin 结构化字段承载（M3.1 终判），载体仍为 Page，不违反本条 | M1.6 / ADR-0005、M3.1 |
| C5 | 项目骨架：`peiligo_restart` 全新仓库干净 Wagtail 骨架（由 OR-1 派发指示确认；不继承旧仓任何 Git 历史/分支/工作区） | M1.4 / ADR-0003（G1 正式批准） |

另：**中文搜索保留真实中文数据 PoC 决策门（D7）**——达标前不引入独立搜索服务（M6.1–M6.3）；V1 范围以 PRD 为准不得扩大（OR-5）。

## 5. 决策门处置表 D0–D9

（以下自 00_MASTER_PLAN v1.4 §2.2 逐字复制，表格原样；块内「§」编号均指 00_MASTER_PLAN 自身章节）

### 决策门处置表（综合审计 §8 D0–D9 的最终处置）

| 决策门 | 处置 | 落点 |
| --- | --- | --- |
| D0 审计与文档基线确认 | 事实确认已由审计轮完成；**G0 = 本计划独立审查通过 + 项目负责人批准**，M0.1 固化记录 | G0、M0.1 |
| D1 旧 M4 工作区处置 | **撤销**（OR-2：旧仓永久只读，不处置、不建归档 tag/分支；接受现状即闭环） | §3 修订 1、13 |
| D2 项目骨架 | **已确认 = C5**（ADR-0003 按修订 8 语义，G1 批准转 Accepted） | M1.4、G1 |
| D3 版本组合 | **已确认 = C1** | M1.2、G1 |
| D4 前端架构 | **已确认 = C2** | M1.3、G1 |
| D5 权限树结构 | **已确认 = C3**；M4.2/M4.3 仅为实现验证，若发现不可修复缺陷升级为决策门重议 | M1.5、M4.x |
| D6 Page/Snippet 分配 | **已确认 = C4** | M1.6、M3.1 |
| D7 中文搜索 | **保留**：真实中文数据 PoC 决策门（E1 数据库 icontains / E2 PG `simple` / E3 zhparser；三方案均不达标才评估独立搜索服务，且须项目负责人批准） | M6.1–M6.3、ADR-0006、G2 条件 3 |
| D8 AI 治理入库范围 | **保留**：M0.3 步骤内恰好一次 ask 裁决 | M0.3 |
| D9 外部上线门（生产服务器、仓库远端与可见性、统一邮箱、学校 VI、软件资源合规） | **保留**：登记为 §20 外部输入表；生产条件在 MC7（生产部署）前逐项落实（G4 批准后、部署前） | §20、G3、MC7（v1.1） |

## 6. 执行仓库声明

本项目唯一执行仓库为 `<LOCAL_PROJECT_PATH>_restart`（OR-1，全文见本文件 §3）。后续一切 Git 仓库、worktree、分支、计划文档、开发、PoC、构建产物均且仅位于该目录内；本文件即位于该仓库内。

本文件编制时点（M0.1，2026-08-18）该目录尚未 Git 化——属计划内正常状态：Git 初始化与文档基线入库由下一步骤 M0.2 执行；本步未建 worktree、未执行任何 git 写操作。

## 7. 旧仓只读声明

旧目录 `<LOCAL_PROJECT_PATH>` 及其全部 worktree 为**永久只读禁区**（OR-2，全文见本文件 §3）：仅可用于审计与选择性复用（复用范围=综合审计 §3 清单）；任何步骤不得安排 M4 处置、建立 tag/branch/commit、文档迁入旧仓、在旧仓开发、针对旧仓的对话或任何写操作；旧归档状态不阻塞本项目任何步骤。本步（M0.1）执行全程未进入、未读取、未对该路径执行任何命令。

## 8. 引用锚点说明

1. 本文件是 `peiligo_restart` 仓库内**第一份决策记录**：G0 批准事实（§1）、PRD 最终确认（§2）、覆盖规则 OR-1..OR-6（§3）、已确认决策 C1–C5（§4）、决策门处置 D0–D9（§5）自此在仓库内有固化载体。后续全部步骤引用上述事实时，以本文件与 00_MASTER_PLAN 为锚点：00_MASTER_PLAN 为唯一执行计划（Control/Execution SSOT），本文件为其 G0 批准状态与决策台账的仓库内固化记录，§3/§4/§5 与 00_MASTER_PLAN §1/§2.1/§2.2 逐字一致。
2. **M0.2 入库预告**：M0.2（Git 初始化与文档基线入库）将把本文件与 `docs/reviews/M00.review.md`（M00 独立审查记录）一并**字节级保真**入库；条件②既以一次性 R3_GPT_WAIVER 豁免，不存在 `M00.gpt-review.md`，入库集合以 M0.2 派发契约为准。
3. 本文件落盘后的修订路径：Git 化前按 00_MASTER_PLAN §4.8 失败与回滚（单文件修正重写）处理；G0 后正式变更一律走 M0.6 变更管理流程。
