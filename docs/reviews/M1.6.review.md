# M1.6 独立审查报告（ADR-0005 公开内容 Page / 受控数据 Snippet·设置）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M1.6 步骤全部产物：`docs/adr/0005-page-vs-snippet-allocation.md`（新建，100 行）、`docs/adr/README.md`（仅 0005 状态行一处变更）；实现提交 `a6fd14a`（分支 `arch/m1.6-adr-0005`，基于 `rebuild/v1@fb01af5`，worktree `.worktrees/m1.6` 检出） |
| 所属步骤 | M1.6 · ADR-0005 公开内容 Page / 受控数据 Snippet·设置（00_MASTER_PLAN §8 M1.6；引源 02 §3 S1.6） |
| 风险级 | R3（ADR 冻结；按 00 §4.6/§8 缺省，GPT 压缩 Risk/Architecture Review 留 G1 门合并一次完成，本审查为独立 GLM 新会话审查） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M1.6 实现；未读取实现 transcript、不以实现者自述为证据（其自述的指针出入已由本审查对原始文档独立复核）；除本报告外一切文件与 git 状态只读（仅运行只读 git/rg/sed/grep/test 取证）；未读取 01/03/PRD 全文（对 PRD 仅做两处定向 grep 抽查条款号与关键短语） |
| 权威对照源 | 00 §8 M1.6 步骤块（验收命令原文）、§8 引言 SE-A 断言定义表 SE-A06 行（v1.2 唯一定义源）、§2.1 C4 行、§2.2 D6 行、§4.5（扫描正则 v1.2）；02 §3 S1.6；上游审计（`docs/audit-report-wagtail-upstream.md`）与综合审计（`docs/PEILIGO_REBUILD_AUDIT.md`）指定章节 |
| 审查方法 | 六项清单一次覆盖（验收复跑 / SE-A06 / M3.1 终判边界 / C4 一致性 / 指针偏差 / 范围与结构）；验收命令、§4.5 正则扫描、C4 逐字 diff、上游引用逐条原文比对全部由审查者亲自复跑并记录（§3） |

## 2. 结论

**通过**。无 P0 阻断项、无 P1 批准前修复项、无 P2 修改条件项；2 项 P3 观察 + 1 项 P4 观察记录于 §4（均不构成修改条件，建议随 G1 或后续文档修订批次处理）。M1.6 完成条件「验收绿 + 独立审查通过」满足；M1.2–M1.6 五份 ADR 至此均以 Proposed 落盘并通过独立审查，G1 架构冻结门的前置条件就绪（G1 本身由项目负责人按 00 §6 执行，不属本审查权限）。

## 3. 逐项核对证据（派发令清单 1–6）

### 3.1 清单 1 · 验收命令复跑 — PASS

审查者于 worktree 内亲自复跑 00 §8 M1.6 步骤块验收命令原文：

```
$ test -f docs/adr/0005-page-vs-snippet-allocation.md && grep -q 'Page' docs/adr/0005-page-vs-snippet-allocation.md \
    && grep -q 'Snippet' docs/adr/0005-page-vs-snippet-allocation.md && grep -q 'M3.1' docs/adr/0005-page-vs-snippet-allocation.md
ACCEPTANCE: PASS

$ ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
SECRETS-SCAN: ZERO HITS
```

§4.5 正则（v1.2 双分支，00 §4.5 原文）对 `docs/` 全目录零命中（grep 退出码 1 且无输出）。附加加强证据：复跑 02 §3 S1.6 的更严验收命令亦全过——7 个关键词（通知/文章/学习资料/软件与工具/校园指南/部门/推荐位）逐一命中无缺失；`grep -q 'Snippet'`、`grep -q 'settings'` 命中；`grep -c 'PRD §7'` = 13（期望 ≥6，逐类引用充分）。

### 3.2 清单 2 · SE-A06 断言：C4 载体分配固化 — PASS

SE-A06 唯一定义源（00 §8 引言断言定义表）：**「ADR-0005 固化 C4 载体分配（六类面向学生内容=Page；部门/受控类别/标签/站点配置=Snippet/设置），且活动类载体终判显式留待 M3.1」，覆盖步骤 M1.6**。

对照 ADR「载体分配原则表」（L28–L54）逐行核对：

| SE-A06 要件 | ADR 落点 | 实测 |
| --- | --- | --- |
| 六类面向学生内容=Page | 表第 1–6 行：通知/文章/活动/学习资料条目/软件与工具条目/校园指南条目，载体列均为 Page（第 3 行活动=「载体=Page（按 C4 表述）」），每行附四维判据（URL/SEO·预览/排程/权限）与 PRD §7.1–§7.6 条款 | ✅ |
| 部门=Snippet | 第 7 行 Department（部门）=Snippet，判据「内部治理对象…无公开独立 URL」 | ✅ |
| 受控类别=Snippet | 第 8–11 行：学科·专业方向/资料类型/指南类别/适用平台四词表=Snippet | ✅ |
| 标签=Snippet | 第 12 行受控标签=「受控词表（Snippet 管控标签集）」，标签经 taggit `ClusterTaggableManager` 挂 Page 侧字段（词表=Snippet、字段在 Page 侧，与 C4「标签…用 Snippet/设置」及上游审计 §3.1「受控标签」条目一致） | ✅ |
| 站点配置=设置 | 第 14 行紧急提示/统一反馈邮箱/跳转页文案=settings（BaseSetting） | ✅ |
| 活动类载体终判显式留待 M3.1 | 见 §3.3 | ✅ |

对照 02 §3 S1.6「输入→输出」要求的清单：Page 五项、Snippet 六项（Department+四词表+FeaturedItem）、settings 三项在 ADR 表中全部落位且每行附判据与 PRD 条款；ADR 额外补入 C4 明列的「标签」行（02 S1.6 清单未单列），属对 C4 的完整覆盖而非扩围。另核实「硬规则」节（L58）含 02 S1.6 测试要求指定的断言原文（「任何后续设计不得把上述 Snippet 项改为公开 URL 内容，反之亦然——变更须新 ADR」）。

### 3.3 清单 3 · 活动类 EventFieldsMixin 载体终判显式留 M3.1 — PASS

- 载体分配表第 3 行：「载体=Page（按 C4 表述）；**载体终判显式留待 M3.1**」；判据列明确「预期经 Notice/Article Page 上的 EventFieldsMixin 结构化字段承载、不建独立活动 Page 类型——**此为预期而非终判**；字段级/类型级挂载安排的终判在 M3.1，本 ADR 不提前终判」——恰好落在派发令要求的「不提前终判，可注预期=Page」区间内。
- 背景节（L20）：「本 ADR 边界：只定载体级分配总则；字段级设计留给 M3.1…**活动类的结构化字段挂载方式（EventFieldsMixin）终判显式留给 M3.1**」。
- 替代方案第 2 项：把「本 ADR 提前终判活动类字段级挂载」作为被否决选项，否决理由引用 00 §8 M1.6 阶段目标与 SE-A06 定义——即 ADR 不仅未越权，还显式排除了越权路径。
- 一句话可检验（L60）与复核时点（L78，M3.1 完成时核对零冲突）两处再次锚定。

预期=Page 的注记与 C4 原文「（M3.1 终判），载体仍为 Page，不违反本条」逐字对齐，未改写 C4 语义。

### 3.4 清单 4 · 与 C4 一致；不替 M3.1 提前做字段级分配 — PASS

- **C4 逐字引用**：ADR 决策节 L24–L26 以 blockquote 引用 00 §2.1 C4 行全文并声明「逐字引用固化…不得改写语义」。审查者做程序化字节级 diff（剥除 blockquote 前缀 `> ` 后整串比对）：**VERBATIM MATCH**，零差异（含标点、`**Page**`/`**Snippet/设置**` 强调标记）。
- **无第三种载体发明**：全文载体仅 Page / Snippet / settings 三类（02 S1.6 独立审查维度「无第三种载体发明」实测通过）；taggit、SnippetViewSet、EventFieldsMixin 均作为机制/扩展点引用而非新载体。
- **不替 M3.1 做字段级分配**：全表仅到「载体级」，判据列为维度核对而非字段定义；字段级设计显式让渡给 M3.1（背景 L20、替代方案 2）；OR-4 合规条自述「未提前终判」与文本实测一致。
- PRD §7.1–§7.6 条款映射定向抽查（PRD 仅 grep 标题行，未读全文）：`### 7.1 通知`/`7.2 文章`/`7.3 校园活动`/`7.4 学习资料`/`7.5 软件与工具`/`7.6 校园指南` 与 ADR 表六行条款号逐一对应，无错位。

### 3.5 清单 5 · 指针偏差核对 — PASS（附 1 项补充发现，见 P3-2）

实现者在 ADR 合规性 OR-6 条自述：00 §8 M1.6 输入指针「上游审计 §4.2/§4.3」在该报告不存在（§4 无编号子节），实按 §3.1/§4 引用并在 OR-6 留痕。审查者对原始文档独立复核（不依赖该自述）：

**(a) 指针出入属实**：`docs/audit-report-wagtail-upstream.md` 标题实测——`## 4. 官方推荐项目结构`（L126）与 `## 5. 需求满足方式汇总`（L142）之间无任何 `###` 子节，§4.2/§4.3 不存在；§3.1（L67 `### 3.1 内容模型（PRD §7）`）存在。00 §8 M1.6 块「输入」行原文确为「上游审计 §4.2/§4.3」。

**(b) ADR 改引内容逐条有实据**（原文比对）：

| ADR 引用 | 上游审计原文落点 | 比对结果 |
| --- | --- | --- |
| §3.1「两种官方一等载体 Page / Snippet+SnippetViewSet（含 DraftStateMixin/RevisionMixin/PreviewableMixin 混入获草稿预览排程）；『具体每类选 Page 还是 Snippet 属技术设计决策』」 | §3.1 L73–75 原文逐句在（「Snippet 可按需混入…发布计划与过期」「具体每类选 Page 还是 Snippet 属技术设计决策…两者都是官方一等公民」） | ✅ 一致 |
| §4「曾把 guides 列为 Snippet、resources 标注『Snippet 或 Page』」 | §4 L135 原文「`resources`（学习资料/软件工具，Snippet 或 Page）、`guides`（校园指南 Snippet）」 | ✅ 逐字一致 |
| §6 R3「Snippet 无 per-instance 权限…『部门账号不能修改其他部门内容』为必测项」 | §6 L163 R3 原文一致 | ✅ 一致 |
| §9「全部需求可经官方扩展点实现，无需改核心」 | §9 L189 原文一致 | ✅ 一致 |

**(c) 无实质冲突**：ADR 将上游 §4 的早期载体建议（guides=Snippet）按 C4 修正并记入替代方案第 1 项，处理方式为「引用+修正留痕」而非篡改来源；OR-6 声明「未修改来源计划、未对任何实质冲突自行取舍」，与 `git diff rebuild/v1...HEAD` 仅两文件（来源计划零改动）实测相符。

**(d) 补充发现（实现者自述之外）**：编号「§4.2/§4.3」实际存在于**综合审计**（`docs/PEILIGO_REBUILD_AUDIT.md` §4「Wagtail 上游审计」→ §4.2 官方扩展方式 / §4.3 需要谨慎的设计点）。其 §4.3「Page vs Snippet」判据（「有独立 URL、SEO、预览、排程、归档搜索的内容倾向 Page；纯结构化词表/配置倾向 Snippet」）与 ADR 四维判据及分配结论实质一致，§4.2 扩展方式表亦与 ADR 载体结论无冲突——即无论 00 指针本意指哪份文档，证据实质均已覆盖、无实质冲突。详见观察项 P3-2。

### 3.6 清单 6 · README 变更、diff 范围、结构与 §4.5 — PASS

- **diff 仅两文件**：`git diff --stat rebuild/v1...HEAD` = `docs/adr/0005-page-vs-snippet-allocation.md`（+100）+ `docs/adr/README.md`（+1/−1），共 2 文件，与 00 §8 M1.6 允许修改范围（该 ADR 文件 + README 状态行）精确一致，无越界文件。
- **README 仅 0005 行变更**：README diff 唯一改动为 0005 状态看板行「—（待落盘）→ 2026-08-18」，其余行（含 0001–0004/0006）零改动；变更幅度=状态行共享约定（v1.1）的最小执行。
- **五节结构 + Proposed**：`rg '^## '` 实测 ADR 节结构=状态字段头/背景/决策/后果/替代方案/合规性，与 `docs/adr/_template.md`（M1.1 产物）逐节同名同序；状态字段头「状态=Proposed（G1 架构冻结门由项目负责人正式设为 Accepted——FIX-04 状态模型…）」，符合 00 §8 M1.6 输出要求与 FIX-04 状态模型。
- **§4.5 零命中**：见 §3.1；全文仅含路径与条款引用，无秘密、无凭据（OR 清单各条与文本实测相符）。

## 4. 观察项（不构成修改条件）

| # | 级别 | 观察 | 建议 |
| --- | --- | --- | --- |
| P3-1 | P3 | ADR 背景节首段「V1 内容模型设计（M3.1，引源 **02 §4 S3.1**）」指针有误：S3.1 实位于 **02 §5**（02 §4 为 P2 信息架构 S2.x；00 §8 M3.1 块原文亦作「02 §5 S3.1」）。纯指针笔误，不影响决策内容。 | G1 门或后续文档修订批次顺带更正为「02 §5 S3.1」；不阻断。 |
| P3-2 | P3 | 00 指针「上游审计 §4.2/§4.3」的编号实存于综合审计（其 §4 章题即「Wagtail 上游审计」，两文档名易混）。ADR OR-6 按「上游审计 §4 无编号子节」表述属实但未指出此混源可能；本审查已核两种读法下证据实质均覆盖、无冲突。 | 建议总控在 G1 记录或 00 下一修订时澄清该输入指针指向（综合审计 §4.2/§4.3 或上游审计 §3.1/§4），供后续步骤引用口径统一；ADR 本身可不改。 |
| P4-3 | P4 | ADR 引号短语「不为减少模型数量强行混用」（标注 PRD §7.2）系沿引源 02 §3 S1.6 的既有转述；PRD §7.2 原文为「通知与文章是两种独立内容类型，不通过单一模型强行混用」（定向 grep 核实），语义一致、非实现者自创。 | 无需动作；若他日统一引文口径可一并注明转述来源。 |

## 5. 风险建议

1. **G1 门衔接**：本 ADR 状态 Proposed、审查通过，M1.2–M1.6 五 ADR 前置就绪。G1 门应按 00 §6 由项目负责人执行（0005 对应 C4 签字 + 状态置 Accepted）；R3 级 GPT 压缩 Risk/Architecture Review 按缺省在 G1 一次完成——本报告可为该审查的直接输入（结论=通过，3 观察项均轻量）。
2. **M3.1 执行提示**：载体终判零冲突核对已有清晰锚点（ADR 复核时点节自我要求「M3.1 完成时核对 EventFieldsMixin 终判与本 ADR 零冲突」）；M3.1 若否决「EventFieldsMixin 挂 Notice/Article」预期挂载方式，须按替代方案 2 的遗留风险条核对与 C4「载体仍为 Page」的相容性并在产物留痕。
3. **权限测试前置登记**：ADR 后果节已把「Snippet 无 per-instance 权限（上游审计 §6 R3）→ 部门隔离依赖自定义 permission_policy、『部门账号不能修改其他部门内容』必测」显式登记，S4.1/S4.3 步骤应承接该测试义务，避免载体分配固化为权限覆盖缺口。
4. **指针卫生**：P3-1/P3-2 表明跨文档编号引用（02 §x / 审计 §x）在本仓已两次出现混源漂移；建议 G1 记录时一次性澄清 M1.6 输入指针口径，后续步骤引源尽量带文件名限定。
