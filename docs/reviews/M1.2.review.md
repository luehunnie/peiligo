# M1.2 独立审查报告（ADR-0001 版本组合冻结）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M1.2 步骤全部产物：`docs/adr/0001-wagtail-django-python-versions.md`（新增 88 行）、`docs/adr/README.md` 0001 状态行（分支 `arch/m1.2-adr-0001` @ `65c7083`，worktree `.worktrees/m1.2` 检出） |
| 所属步骤 | M1.2 · ADR-0001 版本组合冻结（= C1）（00_MASTER_PLAN §8 M1.2；引源 02 §3 S1.2） |
| 风险级 | R3（ADR 冻结，§4.6 缺省清单；独立 GLM 新会话审查，GPT 压缩 Risk/Architecture Review 按计划留待 G1 门合并一次完成） |
| 审查日期 | 2026-08-18（与 ADR 复核日期同日） |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M1.2 实现及 C1 起草；除本报告外一切文件与 git 状态只读（仅运行只读 git/rg/sed/grep/test/curl 取证），未执行任何 git 写操作，未读取实现 transcript、01/03/PRD/audit 全文，仅按派发令读取允许段落（00 §8 M1.2 步骤块、§8 引言 SE-A 断言定义表、§2.1 C1 行、§4.5 扫描正则）与待审两文件全文 |
| 权威对照源 | 00_MASTER_PLAN §8 M1.2 步骤块（验收命令/测试要求/独立需求原文）、§8 引言 SE-A 断言定义表 SE-A02 行、§2.1 表 C1 行、§4.4（共享文件）、§4.5（秘密纪律） |
| 审查方法 | 亲自复跑验收命令与 §4.5 扫描；C1 逐字比对（程序化 diff）；复核输出真实性抽查（超出最低要求：实时复跑 ADR 内置三条 PyPI 复核命令 + 二次核验 django 5.2.x 线/wagtail 7.4.x 线/modelsearch 1.3 线枚举与依赖 + 抽验一个二级官方来源） |

## 2. 结论

**通过**。无 P0 阻断项、无 P1 批准前修复项、无 P2 观察项；2 项 P3 观察记录于 §4（均不构成修改条件）。M1.2 完成条件「验收绿 + 独立审查通过」满足（G1 签字按计划在 G1 门完成，FIX-04：Proposed 落盘 → G1 设 Accepted）。ADR-0001 可作为 M1.3–M1.6、M6.1/M6.2、B1 的唯一版本依据进入后续步骤。

## 3. 逐项核对证据（派发令清单 1–6）

### 3.1 清单 1 · 验收命令复跑 — PASS

审查者于 worktree 内亲自复跑步骤块验收命令原文：

```
$ test -f docs/adr/0001-wagtail-django-python-versions.md && grep -q '7.4' docs/adr/0001-wagtail-django-python-versions.md && grep -q '5.2' docs/adr/0001-wagtail-django-python-versions.md && grep -q '3.13' docs/adr/0001-wagtail-django-python-versions.md && grep -q '>=1.3.2,<1.4' docs/adr/0001-wagtail-django-python-versions.md && echo "ACCEPTANCE: PASS"
ACCEPTANCE: PASS (exit 0)
```

验收命令附带两项条件同样满足：

- **复核日期=执行当日**：ADR L8「日期 | 2026-08-18（首次落盘）」、L38「当日版本复核（复核日期：2026-08-18……）」，审查日 `date +%Y-%m-%d` 实测 = 2026-08-18，同日 ✓。
- **§4.5 零命中**：审查者以 §4.5 v1.2 原文正则对 `docs/` 全量扫描（命令与输出见 3.5）。

### 3.2 清单 2 · SE-A02 断言：四项约束 + 当日复核证据 — PASS

SE-A02 唯一定义源（00 §8 引言断言定义表）：**「ADR-0001 含四项版本约束（`wagtail>=7.4.2,<7.5`、`django>=5.2,<5.3`、Python 3.13、`modelsearch>=1.3.2,<1.4`），且当日复核证据（补丁版本号 + 官方 URL）在文内」**。

四项约束逐条 grep 实测（版本号 + 行号）：

| 约束 | 文内位置 |
| --- | --- |
| `wagtail>=7.4.2,<7.5` | L29（C1 引用）、L33（展开项 1） |
| `django>=5.2,<5.3` | L29、L34（展开项 2） |
| Python `3.13`（运行时 3.13.x） | L35（展开项 3；`grep -c` 实测 1 处展开定义） |
| `modelsearch>=1.3.2,<1.4` | L29、L36（展开项 4） |

当日复核证据（补丁版本号 + 官方 URL + 日期三要素齐备）：L38–48「当日版本复核」表，7 个复核项逐项含当日事实（版本号）与官方来源 URL（pypi.org ×3、docs.wagtail.org、github.com/wagtail ×2、docs.djangoproject.com、djangoproject.com、devguide.python.org），表头明示「复核日期：2026-08-18；下列 URL 当日全部访问核验」。

结构合规附带核对：ADR 节结构（`rg '^## '` 实测：状态字段头/背景/决策/后果/替代方案/合规性）与 `_template.md` 六节逐名逐序一致，符合 README §5「五节结构与状态字段头不得增删改名」。

### 3.3 清单 3 · 与 C1 逐字一致；拒绝 8.0 线与 main；复核逻辑 — PASS

**C1 逐字一致（程序化 diff）**：提取 00 §2.1 表 C1 行决策文本与 ADR L29 决策节 blockquote，剥除表格/引用定界符后 diff：

```
$ sed -n '38p' docs/plans/00_MASTER_PLAN.md | sed 's/^| C1 | //; s/ | M1.2 \/ ADR-0001 |$//' > /tmp/c1_plan.txt
$ sed -n '29p' docs/adr/0001-wagtail-django-python-versions.md | sed 's/^> //' > /tmp/c1_adr.txt
$ diff /tmp/c1_plan.txt /tmp/c1_adr.txt && echo "VERBATIM: IDENTICAL"
VERBATIM: IDENTICAL
```

零差异，逐字一致；OR-4（不重开选型）成立——「替代方案」节（L68–75）仅作取舍留档，不构成重开。

**拒绝 8.0 线（含 RC）理由**（L70 方案 B，三重论据）：① 复核日 8.0 仅 `8.0rc1` pre-release、正式发布计划 2026-08-25，RC/首发无补丁沉淀；② 支持窗口短（8.0 安全支持止 2027-02-02、活跃支持止 2026-11-03，2026-11 即需升 8.1）；③ 其收益（API v3/Django Ninja 等）服务于 V1 无诉求的场景（PRD §20 不扩围）。**拒绝 `main` 理由**（L74 方案 F）：移动目标、无发布承诺、无支持窗口、无补丁纪律，与「不修改 Wagtail 核心」（02 §0.2-2）同一精神。两项拒绝均与 C1 原文「不采用 8.0 线（含 RC）与 `main`」对应。

**复核逻辑满足「首次锁依赖复核最新安全补丁」**（L50 复核结论，自洽性核实）：

- wagtail：7.4.x 线无晚于 7.4.2 的补丁 → 实际锁定 `wagtail==7.4.2`（= 约束下界，区间 `>=7.4.2,<7.5` 满足）；
- django：5.2.x 线最新安全补丁 5.2.17（2026-08-04，4 项 CVE）→ B1 首次锁依赖应落在 `>=5.2.17` 的 5.2.x（策略区间仍 `>=5.2,<5.3`）；
- modelsearch：1.3 线最新 1.3.2 → 实际锁定 `modelsearch==1.3.2`（= 约束下界，区间满足）。

三支逻辑与各自约束区间自洽；「实际锁定」的不对称（wagtail/modelsearch 当日即可定死、django 留 B1 定点）有正当理由——前两者约束下界即当日最新补丁，锁定值今日已确定；django 下界 5.2 低于最新补丁 5.2.17，按 C1「首次锁依赖时复核最新安全补丁后冻结具体补丁号」的时点设计留在 B1 执行。ADR L57 明列复核时点（G1 一次 / B1 复核并冻结补丁号 / 季度评估 / 上线前），L66 明列「若 7.4.x 出现晚于 7.4.2 的补丁→锁最新并回写」与「7.4 线意外终止→停下上报不得代决」两条边界处置，闭环完整。

### 3.4 清单 4 · 复核输出真实性抽查 — PASS（超出最低要求）

最低要求（URL 格式 + 日期 2026-08-18）核对：L38–48 表内 9 个 URL 全部为格式合法的官方域名 https 链接（pypi.org / docs.wagtail.org / github.com/wagtail / docs.djangoproject.com / djangoproject.com / devguide.python.org），无转写、无追踪参数；日期 2026-08-18 与审查当日一致（3.1 已证）。

审查环境网络可用，故超出最低要求做了两级实测：

**一级：实时复跑 ADR L61–63 内置三条复核命令**（输出与 ADR 记载逐项比对）：

```
$ curl -s https://pypi.org/pypi/wagtail/json | python3 -c "...print(['info']['version'])"
wagtail latest: 7.4.2        ← ADR L42 记载 7.4.2 ✓
$ curl -s https://pypi.org/pypi/django/json | python3 -c "..."
django latest: 6.1           ← ADR L45 记载「最新稳定版 6.1（超出约束线，不取）」✓
$ curl -s https://pypi.org/pypi/modelsearch/json | python3 -c "..."
modelsearch latest: 1.3.2    ← ADR L47 记载 1.3.2 ✓
```

**二级：对复核结论的支撑事实做独立再核验**（PyPI releases 全列表）：

| ADR 主张 | 审查者实测 | 结果 |
| --- | --- | --- |
| 7.4.x 线 = 7.4、7.4.1、7.4.2，无晚于 7.4.2 的补丁（L42） | releases 过滤 7.4 前缀（排除 a/b/rc）= `['7.4', '7.4.1', '7.4.2']` | ✓ |
| 5.2.x 线最新 = 5.2.17（L45） | releases 过滤 5.2 前缀共 18 项，latest = `5.2.17` | ✓ |
| modelsearch 1.3 线 = 1.3、1.3.1、1.3.2（L47） | releases 过滤 = `['1.3', '1.3.1', '1.3.2']` | ✓ |
| modelsearch 运行依赖 `Django>=4.2`，与 Django 5.2 兼容（L47） | `requires_dist` 含 `Django>=4.2` | ✓ |
| Wagtail 7.4 LTS 属性（L19/L44 引 docs.wagtail.org 7.4 发布页） | 审查者抓取该页，原文含 **"Wagtail 7.4 is designated a Long Term Support (LTS) release."** | ✓（引文逐字吻合） |

**未独立复核项（如实记录，属检查许可范围内）**：发布日程 wiki 的具体日期（8.0 计划 2026-08-25、7.4 支持止 2027-11-02、8.1 计划 2026-11-02）、Django 5.2.17 的 4 项 CVE 编号清单、Django 5.2 扩展支持止 2028-04、Python 3.13 EOL 2029-10——这些为二级官方来源页内容，本次仅核对 URL 格式与日期主张，未逐一抓取。承重事实（三个包的锁定决策依据）已全部实测证实，上列未复核项不改变锁定结论。

### 3.5 清单 5 · 变更范围：README 仅 0001 行、§4.5 零命中、diff 仅两文件 — PASS

分支全量 delta（`git diff --stat rebuild/v1...HEAD`，基点=分支点 fb01af5，分支仅含实现 commit 65c7083）：

```
 docs/adr/0001-wagtail-django-python-versions.md | 88 +++++++++++++++++++++++++
 docs/adr/README.md                              |  2 +-
 2 files changed, 89 insertions(+), 1 deletion(-)
```

恰两文件，与步骤块「允许修改的文件范围：该 ADR 文件 + `docs/adr/README.md` 状态行」一致。README diff 逐行核对：仅 §2 索引表 0001 行变化——日期列 `—（待落盘）` → `2026-08-18`（落盘日期），文件名加 markdown 链接；状态列 Proposed 不变（正确：FIX-04 规定 G1 门才转 Accepted）；其余五行（0002–0006）、§1 收录范围、§3/§4/§5 治理内容零改动。两处一致性附带核对：README 0001 行状态/日期与 ADR 状态字段头（L8/L9）一致，满足 README §5「两处一致方为更新完成」。

§4.5 v1.2 原文正则扫描（对 `docs/` 全部入库文件）：

```
$ ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/ && echo "SEC4.5 SCAN: ZERO HITS"
SEC4.5 SCAN: ZERO HITS
```

零命中 ✓（ADR L87 自报「文内仅含公开 URL、版本号与公开 CVE 编号，无任何秘密」与实测相符）。

### 3.6 清单 6 · 实现者自报风险评估（README §3 看板括注过时）— 不阻塞（P3-1）

实现者自报：README §3 状态看板 Proposed 行括注「（均为预列占位）」对 0001 已过时——0001 已实际落盘，不再是占位；因硬边界未改而保留原文。审查者逐项核实：

| 核实点 | 结果 |
| --- | --- |
| 看板计数仍正确？ | ✓ Proposed=6（0001–0006 全部仍处 Proposed，G1 未开） |
| 看板状态仍正确？ | ✓ 0001 落盘后状态仍为 Proposed（FIX-04），看板无误报 |
| 权威状态源是否受损？ | ✓ 未受损——权威状态在 §2 索引 0001 行与 ADR 状态字段头，两处均已正确更新且一致 |
| 过时程度 | 仅括注语义过时（「预列占位」→ 0001 实为已落盘的 Proposed），且 §3 表头自带「统计时点：2026-08-18（M1.1 预列时点）」限定语，读者可辨 |
| 实现者不改的理由是否成立？ | ✓ 成立——README §5 允许各步骤改动的仅「自己那一行的状态 / 日期列及 §3 看板计数」，括注位于 §3 编号列文字内，不在明列授权范围内；代理不代决、留给项目负责人处置是保守正确选择 |

**评估：不阻塞**。无任何验收命令、SE 断言或后续步骤机械依赖该括注；属 P3 外观债。处置建议见 §4 P3-1。

## 4. 观察与建议（2×P3，均不构成修改条件）

- **P3-1（承接 3.6，实现者自报）**：README §3 看板 Proposed 行括注「（均为预列占位）」对 0001 过时。建议在 G1 门由项目负责人（或经其明确授权的维护动作）将括注刷新为反映实况的表述（如「0001 已落盘；0002–0006 预列占位」），或由 G1 批准时随看板计数一并处理——按 §5 流转权限，该文字变更不宜由步骤代理自行执行。随 M1.3–M1.6/M6.3 逐步落盘，此括注还会继续过时，G1 一次性清理成本最低。
- **P3-2（审查者发现）**：README 0001 行文件名列由代码span改为 markdown 链接，超出 README §5 字面授权清单（「状态 / 日期列及 §3 看板计数」），但落在 00 §8 M1.2 步骤块授权的「README 状态行」范围内且限于自己那一行，语义零变化、纯可用性改进——两份治理文本对「状态行内可改动哪些列」存在轻度口径张力。无需回改；建议未来治理触碰点（如 G1 或 M0.5 政策修订）统一 §5 与步骤块措辞（例如明确「自己那一行内不得改动标题/编号/对应决策列，其余列可为落盘所需的最小更新」）。

## 5. 审查范围与限制

- 本审查按派发令 DO_NOT 约束执行：未读实现 transcript、未信 worker 自述（所有结论以命令实测与文件原文为据）、未读 01/03/PRD/audit 全文（PRD §17/§20 仅经 ADR 引用回溯，未溯源核对原文——属引源抽查范围外）。
- 二级官方来源页的具体日期主张未逐一抓取（3.4 已列明细）；承重事实（三包锁定依据）已全部实时证实。
- 本报告为审查范围内唯一写入文件；除 `git add` 本文件与 `git commit -m "review(M1.2): 独立审查报告"` 外无任何 git 写操作。
