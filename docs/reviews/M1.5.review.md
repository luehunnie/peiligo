# M1.5 独立审查报告（ADR-0004 部门权限容器树）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M1.5 步骤全部产物：`docs/adr/0004-department-permission-container-tree.md`（新建，103 行）、`docs/adr/README.md`（0004 状态行，分支 `arch/m1.5-adr-0004` @ `97d8700`，worktree `.worktrees/m1.5` 检出） |
| 所属步骤 | M1.5 · ADR-0004 部门权限容器树（= C3）（00_MASTER_PLAN §8 M1.5；引源 02 §3 S1.5） |
| 风险级 | R3（ADR 冻结类缺省；GPT 审查按计划留 G1 门合并一次完成，本审查为独立 GLM 新会话） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M1.5 实现及任何基线文件起草；除本报告外一切文件与 git 状态只读（仅运行只读 git/rg/sed/grep/test 取证）；未读取实现 transcript、01/03 全文、PRD 全文（PRD 仅按派发令 rg 定位后读 §4.3/§6/§20 相关行区间） |
| 权威对照源 | 00 §8 M1.5 步骤块 + §8 引言 SE-A 断言定义表（SE-A05）+ §2.1 C3 行 + §2.2 D5 行 + §4.5；02 §3 S1.5 + §0.1/§0.2；`docs/audit-report-wagtail-upstream.md` §3.2；`docs/PEILIGO_REBUILD_AUDIT.md` §4.3；PRD §4.3/§6/§20 相关行 |
| 审查方法 | 派发令清单 1–6 逐项；验收命令、§4.5 正则扫描、02 补充 grep、越界 diff、C3 逐字比对、审计引用逐条溯源全部由审查者亲自复跑并记录（§3） |

## 2. 结论

**通过**。无 P0 阻断项、无 P1 批准前修复项、无 P2 观察项；1 项 P3 观察记录于 §5（不构成修改条件）。M1.5 完成条件「验收绿 + 独立审查通过」满足；ADR-0004 以 Proposed 状态就绪，待 G1 门由项目负责人设为 Accepted（FIX-04）。

## 3. 逐项核对证据（派发令清单 1–6）

### 3.1 清单 1 · 验收命令亲自复跑 — PASS

审查者于 worktree 内复跑 00 §8 M1.5 验收命令原文：

```
$ test -f docs/adr/0004-department-permission-container-tree.md \
  && grep -q '404' docs/adr/0004-department-permission-container-tree.md \
  && grep -q '部门主页' docs/adr/0004-department-permission-container-tree.md
ACCEPTANCE: PASS (exit 0)
```

§4.5 秘密扫描（正则原文复跑）：

```
$ grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
grep exit code: 1        # 零命中，PASS
```

02 §3 S1.5 补充验收 grep（容器 / 404|不可达 / 部门主页 / Publish）合并复跑：`02-ACCEPTANCE: PASS`。

### 3.2 清单 2 · SE-A05 断言：C3 全项固化 — PASS

SE-A05 唯一定义源（00 §8 断言定义表 L445）：「ADR-0004 固化 C3 全项：容器节点承载权限、前台 404/等效不可达、不入导航/站点地图/默认列表/默认搜索」。另 M1.5 阶段目标（L500）含第四项「规避扁平树 Publish 横向越权」。逐项对位：

| C3 分项 | ADR-0004 落点位 | 实测 |
| --- | --- | --- |
| 容器节点承载权限 | 决策 1（L28）：页面树权限 Add/Edit/Publish/Lock/Unlock 挂各部门容器节点并向子树传播；内容页以容器为唯一父级 | ✓ |
| 前台 404/等效不可达 | 决策 2 首条（L30）：「前台访问容器节点 URL 返回 404（或等效不可达）」 | ✓ |
| 不入导航/站点地图/默认列表/默认搜索 | 决策 2 次条（L31）：四项排除逐字列出 | ✓ |
| 规避扁平树 Publish 横向越权 | 后果（L42）+ 替代方案节（L62–66）：方案 b 否决理由含 Publish 与 Edit 独立、横向发布/下线面、残余风险定性 | ✓ |

### 3.3 清单 3 · 「验证证据」节占位（M4.3 回写，不预写结论）— PASS

ADR L94–103 存在 `## 验证证据（占位，M4.3 回写）` 节：显式声明「本步骤（M1.5）不编写任何结论」（L96）；列 4 项待回写项（404/不可达实测、四项排除实测、横向越权矩阵含 Publish 权限面专项反向验证、最终影响判定）与回写来源 `docs/POC_PERMISSION_REPORT.md`。无任何预写结论。与 00 §8 M1.5 v1.1 附加节要求及 02 S1.5 输出⑤（生效条件 S4.3 全绿）一致。

### 3.4 清单 4 · 与 C3 及 PRD §6/§20 口径一致 — PASS

**C3 逐字性**：以 shell 变量剥离表格语法后比对 00 §2.1 C3 行（L40）与 ADR L14 引文：`VERBATIM MATCH`（含加粗标记逐字一致）。ADR L16 对 02 §0.1 C3 行（L30）的转述亦与原文相符。

**PRD 定点核对**（仅 rg 定位后读行区间）：

| PRD 位置 | 原文（节选） | ADR 对应 | 实测 |
| --- | --- | --- | --- |
| §6（L103–105） | 「任何部门均不建立独立主页」「部门筛选结果不是部门主页」 | 决策 2 + L33「部门筛选结果页不是部门主页」 | ✓ |
| §20（L340） | V1 不做清单含「部门独立主页」 | 决策 2、合规性 OR-5 行、追溯表 §20 行 | ✓ |
| §4.3（L62 小节头，L67） | 「只能创建、编辑、发布、下线和归档本部门内容」 | 背景 L12 引述、追溯表 §4.3 行 | ✓ |

### 3.5 清单 5 · 指针偏差核对（「上游审计 §4.3」→ §3.2 替换）— 替换成立

**事实链（审查者亲自取证）**：

1. `docs/audit-report-wagtail-upstream.md` **无 §4.3 小节**：§4 为「官方推荐项目结构」（L126，`wagtail start` 生成物），与权限无关；全文节头枚举证实。
2. 00 §8 M1.5 输入行原文「02 §3 S1.5；上游审计 §4.3」；而 02 §3 S1.5 依赖行原文「上游审计 **§3.2**、**综合审计 §4.3**/§6 P1 风险」——`docs/PEILIGO_REBUILD_AUDIT.md` §4.3「需要谨慎的设计点」（L99）**确实存在**，且其首条即部门权限树（容器节点承载权限、避免扁平树 Publish 横向越权、不等于新增部门主页）。00 的「上游审计 §4.3」判定为两指针混写（疑将「综合审计 §4.3」误冠「上游审计」名），属主控计划文本缺陷，非实现缺陷。
3. **ADR 审计引用逐条溯源 §3.2（L78–91）全部有据**：`docs/topics/permissions.md:13-40`（Add/Edit/Publish/Lock/Unlock 树传播 + Add=ownership）、`docs/reference/hooks.md:1087`（`construct_explorer_page_queryset`）、Publish 横向越权原文（「Publish 权限允许部门发布/下线该节点下任何部门的页面（Publish 与 Edit 独立）」）、「(a) 更干净」、Snippet 自定义 `permission_policy` 扩展点、「树形传播自动隔离」——逐字存在于 §3.2。
4. 综合审计 §4.3 首条的实质要求（容器承载权限 / 规避 Publish 越权 / 不新增部门主页）被 ADR 决策 1、2 与替代方案节全覆盖。

**评估**：实现者以 02 S1.5 自身指定的输入源（上游审计 §3.2）落实，证据引用真实无造假；「替换」不损及 ADR 任何断言的实据支撑。成立。00 指针勘误建议见 §5 P3-1。

### 3.6 清单 6 · 机械核对（README/diff/§4.5/结构/状态）— PASS

- **diff 仅两文件**：`git diff --stat rebuild/v1...HEAD` = ADR（+103）+ README（+2/-1），无越界文件。
- **README 仅 0004 行变更**：diff 实测唯一改动 = 0004 行「—（待落盘）」→「2026-08-18」，其余行未动。
- **§4.5 零命中**：见 §3.1（exit 1）。
- **五节结构**：`docs/adr/_template.md` 定义 = 状态字段头 + 背景/决策/后果/替代方案/合规性；ADR-0004 具备全部五节 + 状态字段头 + M1.5 指定「验证证据」占位节。
- **状态 Proposed**：L7 状态行 = Proposed，并注明 FIX-04 状态模型（G1 门项目负责人设 Accepted，代理不得代决）；README 0004 行同为 Proposed。

## 4. 合规性与越权代决抽查

ADR 合规性表（L68–83）自报「不违反任何一条」：抽查 OR-1（worktree 分支 `arch/m1.5-adr-0004`，无位置外产物——diff 实证）、OR-2（无旧仓读写安排）、OR-4（C3 逐字固化未重开选型——§3.4 实证；替代方案节仅留存已完成比较，不构成重开）、02 §0.2-2（不改核心）/§0.2-3（无部门主页硬约束，与 ADR 决策 2 措辞同源）/§0.2-5（V1 不做清单含部门独立主页——均定向实证）。状态为 Proposed、验证证据留白、生效条件挂 M4.3：无代理代决迹象。

## 5. 观察项（P3，不构成修改条件）

- **P3-1（主控计划指针勘误建议）**：00 §8 M1.5 输入「上游审计 §4.3」在上游审计中不存在，实指应为「上游审计 §3.2 + 综合审计 §4.3」（02 S1.5 依赖行引用正确）。本次以 02 为准核实无碍；建议后续经治理渠道（如 00 勘误登记）修正，避免 M4.2/M4.3 步骤再次踩到同一指针。00 不在本步骤可改范围，本审查不代改。

## 6. 风险建议

1. **G1 门**：项目负责人核对本报告 §3.5 后可直接采信指针替换；将 ADR-0004 设为 Accepted 时同步确认「验证证据」节仍为占位（M4.3 前不得有结论）。
2. **M4.3 回写**：按占位节 4 项逐项回写，尤其「Publish 权限面专项：方案 b 否决理由的反向验证」——该项是方案 a 相对方案 b 的核心安全增益声明，须以实测闭环。
3. **下游传递**：M2.1/S2.1（URL 与 404 语义）、M2.2/S2.2（导航/搜索排除与 noindex 位点）是 C3 硬约束的落地点，建议其步骤契约直接引用 ADR-0004 决策 2 原文，防口径漂移。
