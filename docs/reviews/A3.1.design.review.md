# A3.1 独立设计审查报告（M3.1+M3.2 内容模型设计）

- **步骤**：A3.1 · M3.1 类型清单与载体分配 ＋ M3.2 字段级定义/StreamField 白名单/受控标签（设计者分支 `arch/a3.1-content-model-design` @ `c52bba0`，单提交，基于 `rebuild/v1` @ `a34d75a`）
- **审查对象**：`docs/CONTENT_MODEL.md` §1–§13（+411 行，纯设计文档——`git show --stat` 亲证仅此一文件，零代码/迁移/测试/视觉改动）
- **审查会话**：Orca `arch/a3.1-design-review`（全新会话，不继承设计者上下文；只审查文档，不改设计、不写代码、不调用 Codex/GPT）
- **审查日期**：2026-08-21
- **VERDICT**：**PASS**（0×P2 / 0×P3 / 3×P4）

## 0. 审查环境与边界

纯文档审查：不运行代码、不建 venv、不触数据库（对照事实仅只读 `requirements.txt`、`src/peiligo/settings/base.py`、`home/models.py`、`departments/models.py` 及根级 app 清单）。worktree 守卫：`git rev-parse --git-common-dir` → `/Users/chenjunxian/vscode_projects/peiligo_restart/.git` 归属主仓 ✓；分支首步已规范化 `git branch -m A3.1-design-review arch/a3.1-design-review` ✓；基线归属 `git merge-base --is-ancestor a34d75a c52bba0` ✓。引源纪律：PRD/00/02/audit 仅 rg 定位＋只读所指章节（§7/§10/§11、§10 M3.1/M3.2 L556-615、L74-75+§0.1 C4+S3.1/S3.2 规格节、§3.1 机制行），未全文通读；ADR-0005/0004 与 IA 所指章节全读。上游 clone（`vscode_projects/Wagtail`）本机不存在，Wagtail 机制以审计报告 §3.1 引源＋官方文档化行为表述。未进旧仓 `vscode_projects/peiligo`。

## 1. 九项必查逐项

### 1.1 PRD §7 零漏项——PASS

PRD §7.1–§7.7（PRD L109-190）逐条对表 `CONTENT_MODEL.md` §5–§11：

| PRD 条款 | 要求 | 本文落位 | 判定 |
| --- | --- | --- | --- |
| §7.1 通知必含 7 项 | 标题/摘要/正文/所属板块/发布部门/发布时间/有效期 | §5.1 逐行：`title`(内建)/`summary`/`body` StreamField/板块推导 property/`department` FK/`first_published_at`(内建只读)/`expire_at` 必填（CM-01） | ✓ |
| §7.1 可含 4 项 | 图片/附件/外部链接/受控标签 | §5.1 `image` FK SET_NULL/`attachments` ParentalM2M/`external_url` URLField/`tags` CTM | ✓ |
| §7.1 行为三句 | 倒序展示/到期退出保留归档搜索/预约发布 | 展示与行为均 DEFERRED_TO_M3_3（§5.4），字段位已冻结 | ✓ |
| §7.2 文章必含 6 项＋可含 4 项 | 同通知减有效期 | §6 与 §5.1 完全同构、唯一差异 `expire_at` 可选（"不默认设置下线时间"＝编辑表单默认空） | ✓ |
| §7.2 末句独立类型 | 两种独立内容类型不混用 | §6 末行显式重申（见必查 5） | ✓ |
| §7.3 活动结构化字段 | 起止/地点＋线上标记/报名链接可选/三态状态/报名外跳 V1 无站内 | §7.1 五存储字段＋`event_status` property 三态闭区间（CM-03）；报名行注明"V1 无站内报名" | ✓ |
| §7.4 受控三维 | 学科或专业方向/资料类型/关键词标签 | §8.1 `discipline`/`material_type` 受控 FK（CM-04）＋`tags`；不做课程库＝无课程/教师/学期字段（显式声明） | ✓ |
| §7.5 至少包含 6 项 | 名称/用途/适用平台/来源链接/授权费用/发布或更新时间 | §9.1 六项逐一（`title` 标签定制/`body`/`platforms` M2M≥1/`source_url` 必填经跳转页 CM-05/`license_note`/`first_published_at`＋`last_published_at` 内建双位） | ✓ |
| §7.5 原则三句 | 不托管安装包/跳转页四要素无需勾选/不巡检 | §9.1 无附件字段＋白名单减附件块；§11.4 四要素；§9 头部声明 | ✓ |
| §7.6 指南九字段 | 一字不差一个不删（含看似重复） | §10.1 九行逐项：#1 服务名称映射内建 `title`（标签定制、不双轨）…#8 维护方式两选项逐源 PRD 两句、#9 `last_confirmed_on` DateField 显式业务字段；必填集＝除 #6 外全部（CM-06）；治理三句为运营流程不建模型 | ✓ |
| §7.7 受控组件 | 标题/正文/图片/附件/表格等组合＋禁任意 HTML/CSS＋StreamField 优先 | §11.1 六块白名单（外链块由"等"+§11+02 §11 规格支持）；无 HTML/CSS 直写入口＝结构性满足（CM-07） | ✓ |

### 1.2 ADR-0005 零冲突——PASS

载体分配原则表 14 行逐项对表：#1–#6 六类 Page（§1.1 六行，含 #3"活动＝内容类别、载体仍 Page、无独立活动 Page"与 ADR L38 逐字相容）；#7–#14 Snippet/设置（§1.3 八行：Department/Discipline/MaterialType/GuideCategory/Platform/Tag 词表/FeaturedItem/SiteSettings）。§2 对 C4 的引用与 ADR-0005 决策节 L26 逐字一致。ADR 显式留给 M3.1 的两点（EventFieldsMixin 挂载方式 L20/L38/L83；#13/#14 载体确认）均在本批终判并留痕（§2/§3）。硬规则（载体变更须新 ADR）§1.1 末行承接。零冲突。

### 1.3 IA §5 可挂载——PASS

`CONTENT_MODEL.md` §1.4 类型级挂载表与 IA §5 表（IA L160-176）逐行一致：HomePage 父＝站点根/子＝SectionPage 仅；SectionPage 父＝HomePage/子＝DepartmentContainerPage 仅；容器子＝五类内容页白名单（IA L164 同款）且不含容器/板块类型（IA L175）；五内容页父＝`departments.DepartmentContainerPage` 唯一、叶子；板块限制 Notice/Article∈{chronicle,events}、Material∈{materials}、SoftwareTool∈{software}、Guide∈{guide}，与 IA §2/§4.3 冻结五 slug 一致。树深四层/叶子/不嵌套/不跳层（IA L172-176）。代码现状亲证吻合：`home/models.py:18-19,44-45` 挂载白名单即上表前三行；`departments/models.py:21-25` `subpage_types=[]` 占位且注释预留五类扩展——§1.4"现状衔接"表述属实。板块级归属限制以 clean() 页面级校验落实＝IA §5 表头注（L158）的原样执行。

### 1.4 EventFieldsMixin 未生成 EventPage——PASS

`rg 'EventPage'` 仅三处（L59 批令排除行、L110 终判句、L124 否决记录），全部为排除性表述，零 EventPage 模型定义。§2 终判：纯抽象 Mixin（`Meta.abstract=True`、非 Page 子类、不注册 admin）；仅 Notice/Article 以 `class NoticePage(EventFieldsMixin, Page)` 继承，其余类型禁止继承；与 02 §5 S3.1 L398"默认候选采纳/否决须给结论"的要求对应——结论、理由、三个否决候选（独立 EventPage/拆 Snippet/双页复制）齐备。

### 1.5 Notice/Article 两种独立 Page 模型——PASS

§1.1 行 1/2 为两个独立正式 Model 名；§6 末行"通知与文章保持两种独立 Page 类型，不混用（PRD §7.2 末句；ADR-0005 #2）"；§2 否决记录含"活动字段拆 Snippet"防第三套答案。全文无单模型＋content_type 区分的设计痕迹。

### 1.6 无 RawHTMLBlock——PASS

`rg -c 'RawHTMLBlock' docs/CONTENT_MODEL.md` ＝ **0**（满足 00 M3.2 验收 `! grep -q 'RawHTMLBlock'`）；"RawHTML"字面仅两处（L308 显式禁项、L408 CM-07），均为禁止性声明——同时满足 02 §5 S3.2 验收"`grep -q 'RawHTML'` 必须显式禁止"（措辞选"RawHTML 全族"避开类名字面，两代验收命令同时通过，属有意合规）。禁令覆盖"任何内容类型、任何字段"，并按 PRD §7.7 以"全链路无 HTML/CSS 直写入口"结构性落实。

### 1.7 无未经 PRD/计划支持的字段——PASS

逐字段溯源（点名核对）：NoticePage 12 行全溯 PRD §7.1/§11/IA §4.3；EventFieldsMixin 5 存储字段＋1 推导全溯 §7.3；MaterialPage 12 行溯 §7.4/§7.7/§10/§11＋02 §5 S3.2 §8（`summary` 见裁决③）；SoftwareToolPage 9 行全溯 §7.5/§4.3；GuidePage 9＋3 行全溯 §7.6/§4.3/IA §4.3；Department `name/slug` 溯 PRD §4.3/§6＋IA §4.1/§4.3，`sort_order/is_active` 溯 02 §13（L433"名称/代号/排序/停用"）；四词表 `name/sort_order` 溯 02 §13＋PRD §7.4/§7.5/§7.6；FeaturedItem `content/start_at/end_at` 溯 PRD §9/§8，`enabled` 溯 02 §13（"启用"）；SiteSettings 五字段溯 PRD §9/§8/§7.6/§7.5＋02 §13。**无无源字段**。反向"无字段声明"齐备（通知无编号/置顶、资料无图片、软件无附件/摘要、指南无 StreamField/图片/附件/外链/标签），PRD 未列即不设的纪律成立。

### 1.8 Django/Wagtail 可实现性——PASS

版本现状亲证：`requirements.txt` `wagtail==7.4.2`/`Django==5.2.17`/`django-taggit==6.1.0`（L35/L8/L13，合 02 §0.2-1 区间）；`taggit` 已在 INSTALLED_APPS（base.py:58）；`wagtail.contrib.table_block` 确未启用——§11.1 落位提示的"现状实证"**属实**，B 阶段加入 INSTALLED_APPS 即可（table_block 随 Wagtail 分发，无新依赖）。逐类型核：

- StreamField 块全部官方内建：CharBlock＋choices（h2/h3）/RichTextBlock(features=…)/ImageChooserBlock/DocumentChooserBlock/TableBlock/StructBlock(url=URLBlock＋link_text=CharBlock)；`min_num=1` 为 StreamField 官方参数 ✓。
- RichText features 白名单 `bold, italic, link, ol, ul` 全为官方 feature 名；排除项命名准确（"文档链接 feature"即 document-link）；双层收窄（工具条渲染＋入库转换过滤）与审计 §3.1（L70，`field_block.py:747-762`）一致 ✓。
- 字段类型全部 Django/Wagtail 原生：DateTimeField/DateField/EmailField/URLField/SlugField/TextField/CharField(choices)/BooleanField(default)/IntegerField(default)；FK `on_delete=PROTECT/SET_NULL/CASCADE`；`ParentalManyToManyField`（wagtail.models）；`ClusterTaggableManager(through=…)`＋自定义 Tag＋register_snippet 词表＝审计 §3.1（L71）认定的官方路径；`BaseSiteSetting` 为 Wagtail 7 正确类名（ADR-0005 行文旧称 BaseSetting，实质一致）。
- `expire_at`/`go_live_at`/`first_published_at`/`last_published_at` 均为 Page 内建列；通知经 clean 强制 `expire_at` 非空＝字段位冻结＋必填性设计，Django 校验标准做法；PublishingPanel 含两位点属实。
- EventFieldsMixin＝Django 抽象基类 Mixin＋`class NoticePage(EventFieldsMixin, Page)` MRO 官方惯用法，迁移无障碍。
- 板块级 clean() 可行性：创建/编辑路径可行——Wagtail 后台 create 流程在表单校验前绑定所选父级（官方机制，slug 同父唯一校验即依赖之），编辑路径树位置已存在可 `get_parent()`；**移动不触发 clean()**，§1.4.2 如实声明并改为信号/hook 兜底＋"机制细节 B 阶段实测留痕、约束在此冻结"——设计文档对确定性与不确定性的边界划得诚实。继承树推导板块的 property 方案零存储字段，可行。

机制疑问定向查审计 §3.1（L70-71、L208-211）：StreamField＋内建 Block 全家桶、RichText features 按字段收窄、table_block、taggit＋ClusterTaggableManager＋受控词表，全部背书为官方机制，与本文 §11/§12 设计吻合。

### 1.9 不提前设计 M3.3/M3.4——PASS

`rg '^#{2,3} 1[4-9]|^#{2,3} 2[0-9]'` 零命中（无 §14–§24 章节）；全文 DEFERRED 标记 13 处。状态机/DraftStateMixin/go_live_at/expire_at 行为/search_fields/删除政策仅出现于：标记定义（L16-17）、字段位＋标记（L158 expire_at"到期行为 DEFERRED_TO_M3_3"、L175 PublishingPanel"行为语义 DEFERRED_TO_M3_3"）、范围声明（L411"PS-xx 与搜索映射断言不在本文"）。均为允许形态（仅 DEFERRED 标记＋字段位冻结），无行为语义、无索引映射、无归档/删除策略设计。

## 2. 三项设计者自裁留痕裁决

### 裁决① Page 终名 MaterialPage/GuidePage（偏离 02 字面候选 StudyMaterialPage/GuideEntryPage）——**合理自裁**

实证：02 §5 S3.1 L397 规格行确用 `StudyMaterialPage`/`GuideEntryPage`；IA §5 L168/L170 工作名确为 `MaterialPage`/`GuidePage`，且 IA §5 表头注（L158）明文"正式类型清单与命名由 M3.1 终判"——IA 主动把命名权交给本步骤；`departments/models.py:22-24` 注释预留的正是终判五名。00 M3.1 验收命令不 grep 02 候选名（02 S3.1 的旧验收 grep 因 §1.2 取舍记录保留候选名字面亦通过）。§1.2 理由成立：`XxxPage` 均一模式、"Study"前缀与板块/app 语境重复、"Entry"后缀无信息量且仅指南单用破坏均一性；A2.x 审查哨兵名的映射处置（L58）防后续 grep 断链。不属四种升级情形（无 ADR/PRD 实质冲突、不改载体、不改 URL/页面树）。
附 P4-1（证据失实，见 §3）：终判理由中"home/models.py 代码注释均按此预留"不实——不影响结论（IA 工作名＋departments 注释已足支撑）。

### 裁决② 活动字段按板块必填化（events 起止必填、线下时地点必填；chronicle 强制全空）——**合理自裁**

实证：PRD §7.3（L127-136）要求活动内容支持结构化字段但**未规定分板块必填性**；必填性本属 M3.2 冻结范围（阅读约定"只冻结字段位与必填性"）。理由链成立：CM-03 三态计算需起止（02 L434 断言原文即"按时间正确计算"）；IA §2 #2 定位 events 板块承载"活动预告、进行中与回顾"（板块语义即活动）；双向规则（events 必填＋chronicle 强制空＋面板隐藏）比单向更自洽，纪事下防误填的双保险合理；IA §5 L167"活动结构化字段板块级限制＝校园活动"同向。不属四种升级情形。**留痕边缘情形**（非问题）：性质上非活动的通知（如"活动场地系统维护公告"）若归 events 板块则须填起止——编辑治理上应归 chronicle；PRD 无相反条款，属产品可接受约束，随 §7.2 冻结。

### 裁决③ 学习资料摘要字段来源 02 规格而非 PRD §7.4 逐字——**合理自裁（实为 02 逐字合规）**

实证：PRD §7.4（L138-146）仅列三个受控维度，无摘要句；02 §5 S3.2 §8 L428 规格行明文"标题/摘要/正文"；00 M3.2 输入（L578）含"02 §5 S3.2"。§8.1 摘要行诚实标注"PRD §7.4 未逐字单列，沿用通知/文章摘要语义（§7.1/§7.2）"，引源纪律（L21"每字段逐项引源 PRD 或经 02 计划规格明确列出"）覆盖此形态。无越权、无发明。

## 3. 发现清单（P4×3；无 P2/P3）

| # | 级 | 位置 | 发现 | 处置建议 |
| --- | --- | --- | --- | --- |
| P4-1 | P4 | §1.2 L56 | 证据失实："`departments/models.py`、`home/models.py` 代码注释均按此预留 M3 扩展位"——`home/models.py`（45 行全文亲读）无任何内容页名注释，仅 `departments/models.py:22-24` 有。终判结论不受影响（IA §5 工作名＋departments 注释已足），但冻结文档中的事实陈述应准确 | 后续批次触碰本文时修正为单文件表述 |
| P4-2 | P4 | 全文结构 | 02 §5 S3.1/S3.2 编排的 §25 可追溯性表/§26 断言附录未按字面落位（断言并入 §13.5、可追溯性内联为字段表"引源"列）。因 00 M3.1/M3.2 允许修改范围仅 §1–§4/§5–§13，字面照抄 02 反会越界——本文处理与 00 相容且功能等价（CM-00–CM-08 齐、逐行引源可 grep）。遗留：02 文本未同步，M3.3/M3.4（§14–§19/§20–§24）派发时编号口径需总控明确，防 §25/§26 悬空或重复建 | 总控在 M3.3/M3.4 批令中注记编号口径 |
| P4-3 | P4 | §12 校验闸 L343 | 机制时序存疑：页面 `clean()` 拦截"所提交"未知标签，依赖 modelcluster 表单→集群的标签施加时序（实例 clean 时新标签是否已可见需实测）。CM-08 由权限闸＋仅选择器表单闸两道结构性闸已足成立，风险低；若实例 clean 不可见，第二道闸落点应改为表单校验层（本文 L343 括注"表单校验层，非事后"已暗示此意但载体写的是 clean()） | B 阶段实测后把落点写实 |

## 4. 升级情形核查（四种，零触发）

1. Accepted ADR 与 PRD 实质冲突：无（§1.2/裁决①②③核）。
2. EventFieldsMixin 无法在官方机制下合理实现：无（抽象 Mixin＋MRO 为官方惯用，§1.8）。
3. 需改变 Page/Snippet 架构：无（载体表与 ADR-0005 逐行一致）。
4. 需改变公开 URL/页面树：无（§1.4 挂载表与 IA §5 及现状代码逐行一致，无 URL 方案改动）。

**无需 Human 裁决点。**

## 5. 合规与提交

秘密扫描（00 §4.5 口径 rg）：`docs/CONTENT_MODEL.md` 零命中 ✓。审查产物：仅本报告（单一新增文件）；单提交至 `arch/a3.1-design-review`，未 push/未 PR/未 force/未 reset --hard/未 clean -fd。旧仓零触碰；旧库（peiligo_dev/peiligo_test）零触碰（纯文档审查，未连库）。
