# M1.3 独立审查报告（ADR-0002 前端架构：服务端模板）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M1.3 步骤全部产物：`docs/adr/0002-server-side-templates.md`（新建 72 行）+ `docs/adr/README.md`（0002 行 1 处改动）（分支 `arch/m1.3-adr-0002` @ `e74c3bb`，worktree `.worktrees/m1.3` 检出；`git log --oneline -3` 首位即实现提交） |
| 所属步骤 | M1.3 · ADR-0002 前端架构：服务端模板（= C2）（00_MASTER_PLAN §8 M1.3；引源 02 §3 S1.3） |
| 风险级 | R3（派发令 RISK：ADR 冻结；GPT 压缩 Risk/Architecture Review 按计划留 G1 门统一执行，本报告为独立 GLM 新会话审查） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M1.3 实现及任何基线文件起草；未读实现 transcript、不信实现者自述；除本报告外一切文件与 git 状态只读（仅运行只读 git/rg/sed/grep/test 取证）；未读 01/03/PRD 全文，仅按派发令读取允许段落（00 §8 引言 SE-A 断言定义表、§8 M1.3 步骤块、§2.1/§2.2 决策表、§4.5/§4.6、02 §3 S1.3、综合审计 §1–§2 引用段落）与两份待审文件全文 |
| 权威对照源 | 00_MASTER_PLAN §8 M1.3 步骤块、§8 引言 SE-A 断言定义表（v1.2，SE-A03）、§2.1（C2 冻结决策）、§2.2（D4 处置行）、§4.5（秘密纪律与扫描契约）、§4.6（独立审查规则）；02 §3 S1.3（引用核对）；`docs/PEILIGO_REBUILD_AUDIT.md` §1–§2（证据编号核对，仅取被引段落） |
| 审查方法 | 双口径一次覆盖（A 需求/架构=清单 2/3 + B 实现/测试/安全=清单 1/4/5）；验收命令、§4.5 正则扫描、越界 diff、证据编号回溯全部由审查者亲自复跑并记录（§3） |

## 2. 结论

**通过**。派发令 5 项审查清单全部 PASS（§3）；无 P0 阻断项、无 P1 批准前修复项、无 P2 观察项；2 项 P3 观察记录于 §5（均不构成修改条件）。M1.3 完成条件「验收绿 + 独立审查通过」满足（G1 签字按 FIX-04 在 G1 门完成；R3 的 GPT 压缩审查按派发令留 G1 门）。

## 3. 逐项核对证据（派发令清单 1–5）

### 3.1 清单 1 · 验收命令复跑（B 口径）— PASS

审查者于 worktree 内亲自复跑 00 §8 M1.3 块验收命令原文：

```
$ test -f docs/adr/0002-server-side-templates.md && grep -q '服务端模板' docs/adr/0002-server-side-templates.md && grep -q 'SPA' docs/adr/0002-server-side-templates.md && echo ACCEPTANCE_OK
ACCEPTANCE_OK exit=0
```

分解核对：文件存在（`ls docs/adr/` 实测含 `0002-server-side-templates.md`）；`服务端模板` 命中标题、决策节引文与全文多处；`SPA` 命中决策边界 1、后果与两个替代方案。三个条件全部满足，退出码 0。

### 3.2 清单 2 · SE-A03 断言：固化 C2（服务端模板、无 SPA），拒绝项=SPA/前后端分离并附审计证据理由（A 口径）— PASS

SE-A03 唯一定义源（00 §8 引言断言定义表 v1.2，L443）：**「ADR-0002 固化 C2（服务端模板、无 SPA），拒绝项=SPA/前后端分离并附审计证据理由」**。

**固化 C2**：00 §2.1 C2 行（L39）原文「前端架构：Wagtail 服务端模板（Django 模板，无 SPA）」；ADR 决策节（L21–23）以引文块逐字引用并标注出处「逐字固化 C2（00 §2.1，冻结不得重开）」。一致性成立（引文末尾多一个句号，见 §5 P3-2，00 §8 M1.3 口径为「与 C2 一致」，非 M1.2 的「逐字一致」）。

**无 SPA（边界条件）**：ADR L25–30 四条边界——① 前台全部页面由 Django/Wagtail 模板服务端渲染，不引入 Vue/React 前端框架基座、不设独立 SPA 工程与前端构建链；② 静态资源经 Django static，渐进增强脚本不构成 SPA；③ Wagtail 后台内置 JS 不视为 SPA 引入；④ 旧 Vue 资产不作架构基线整体迁移。语义与 C2 括注「无 SPA」闭合。

**拒绝项=SPA/前后端分离 + 审计证据编号**：替代方案 1「演进旧 Vue 3 SPA」、替代方案 2「前后端分离（独立前端工程 + Wagtail 作纯 API 后端）」，均含否决理由与遗留风险。所引证据编号由审查者逐一回溯 `docs/PEILIGO_REBUILD_AUDIT.md` 核对：

| ADR 引用编号 | 审计原文（审查者实测） | 核对 |
| --- | --- | --- |
| §1 结论 2 | 「前端架构证据倾向 Wagtail 服务端模板。V1 是公开、内容驱动、低交互网站，服务端模板在预览、SEO、部署、缓存、无障碍和权限一致性方面路径更短。旧 Vue 前端仍有选择性复用价值，但不应成为架构基线」 | 一致（六维路径更短 + 选择性复用判定均被 ADR 忠实引用） |
| §1 结论 4 | 「PRD V1 未发现必须修改 Wagtail 核心的需求」 | 一致（ADR 合规性第 4 条引用准确） |
| §1 结论 6 | 「本轮没有发现需要扩大 V1 的理由。旧路线图中的 AI 搜索、复杂审核、学生账号、推送等均不得顺带进入 V1」 | 一致（替代方案 2 反扩围论证与合规性第 3 条引用准确） |
| §2.1 | Vue 3 + TypeScript + Vite SPA；前端搜索为全量拉取 + 浏览器 `includes()` 过滤；SPA 缺少可验证 SSR/meta/noindex 方案；Element Plus 全量引入存在体积风险 | 一致（背景 2 与替代方案 1 否决理由引用准确） |
| §2.2 | 技术基座行「FastAPI/Vue 与 Wagtail/Django 基线不同」；搜索行「不可作为 V1 搜索实现」；SEO/无障碍行「部分资产可复用，能力未达标」；部署运维行「无 CI、容器、备份、监控」 | 一致（背景 3、后果 3、代价 1 引用准确） |

另核对：替代方案 2 引「Wagtail 7.4 API v2 为只读接口，不足以支撑 SPA 形态的治理后台（02 §3 S1.3 论据②）」——02 §3 S1.3（L234）原文含「7.4 API v2 只读不足以支撑 SPA 治理后台」，引用准确；后果节「G1 架构冻结门检查表第 2 项」——02 S1.3 步骤块自身「阶段门：G1 检查表第 2 项」（L242），指针准确。

### 3.3 清单 3 · README 共享文件边界（A 口径）— PASS

`git diff rebuild/v1..HEAD -- docs/adr/README.md` 实测仅 1 行变更：0002 行日期单元格 `—（待落盘）` → `2026-08-18`，其余（0001/0003–0006 行、表头、收录范围声明、状态看板）零改动。与 00 §8 M1.3「允许修改的文件范围：该 ADR 文件 + `docs/adr/README.md` 状态行（共享）」一致；日期值与 ADR 状态字段头日期（L8）及执行当日（2026-08-18）一致。

### 3.4 清单 4 · §4.5 秘密零命中（B 口径）— PASS

审查者按 §4.5 扫描契约原文（v1.2 双分支正则，含裸 `dcap_` 分支）对 `docs/` 亲自复跑：

```
$ ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/; echo "SCAN_EXIT=$?"
SCAN_EXIT=0
```

零命中（无输出、取反后退出码 0），通过判据满足。ADR 合规性第 5 条（L69）亦声明提交前自检，与本审查独立复核结果一致。

### 3.5 清单 5 · 越界核对（B 口径）— PASS

```
$ git log --oneline rebuild/v1..HEAD
e74c3bb M1.3: ADR-0002 前端架构：服务端模板（Proposed）
$ git diff --stat rebuild/v1..HEAD
 docs/adr/0002-server-side-templates.md | 72 ++++++++++++++++++++++++++++++++++
 docs/adr/README.md                     |  2 +-
 2 files changed, 73 insertions(+), 1 deletion(-)
$ git branch --show-current
arch/m1.3-adr-0002
```

`rebuild/v1..HEAD` 恰好 1 个提交（`e74c3bb`，即 M1.3 实现提交，`git show --stat` 同两文件）；改动文件集 = 允许范围两文件，无任何其他文件（无 `poc/`、无配置、无 01/02/03/PRD 触碰）。分支名 = 00 §8 M1.3 规定 `arch/m1.3-adr-0002`。审查开始时 `git status --porcelain` 干净。

## 4. 附加核对（结构一致性，A/B 口径补充）

- **模板五节（M1.1 `_template.md` 结构）**：`rg '^## '` 实测 ADR-0002 含 状态字段头/背景/决策/后果/替代方案/合规性，与模板逐一对应（替代方案含 2 个方案、各含描述/否决理由/遗留风险三要素）。
- **FIX-04 状态模型**：状态=Proposed（L9）；L70 决策人记录正确——C2 由项目负责人确认（Human Confirmed，冻结），代理以 Proposed 落盘，转 Accepted 决策人为项目负责人（引用 README §4 硬规则，代理不得代决）。
- **触发与引源**：背景 4（L17）正确引用 00 §8 M1.3 与 OR-4「只落 ADR，不重新选型」；合规性第 1 条逐条对照 OR-1..OR-6 声明不违反，未发现反证。
- **复核时点**：L47 声明 G1 门转 Accepted 及重估条件（V2 高交互需求/交付瓶颈），与 00 §6 G1 行（五 ADR 以 Proposed 落盘、G1 门项目负责人设 Accepted）一致。

## 5. P3 观察（不构成修改条件）

1. **P3-1 · 02 §3 S1.3 增量要求未全量落位（记录 + G1 建议，不要求本步骤修改）**：02 S1.3 的验收命令与「必含」清单较 00 §8 M1.3 更细：三档收割关键词（`直接复用|改造复用|重新实现`）实测 0 命中、`audit-report-old-peiligo` 文件名引用 0 命中（ADR 改引综合审计 `PEILIGO_REBUILD_AUDIT.md` 编号，编号本身全部核实为真）、`PRD §` 引用 1 处（02 期望 ≥3）、学校 VI 色值未决（旧站审计 U2）未登记。**约束口径**：执行契约为 00 §8 M1.3 块（00 §1 文档层级 00 先于 02，且 §8 阶段头明示「引源=02 §3，按 §3 修订清单修正」，修订清单无 S1.3 专项修订，00 块验收命令为落定口径），该契约已全绿；02 增量语义（四态规格/URL 筛选语义等直接复用资产 → B 阶段模板移植落点）本质属 B 线模板实施步骤（后续计划 B4 一线）与 G1/G2 可追溯性表的自然输入。**建议**：G1 门核对时将 02 S1.3 三档收割清单登记为 B 线输入（直接复用=设计令牌/a11y 模式/四态规格/URL 筛选语义→B 阶段模板；改造复用=结构/文案→重写参考；重写=路由/EP/内存搜索→不迁移），防止复用意图在 00→02 粒度差中丢失。
2. **P3-2 · C2 引文末尾补句号**：决策节引文较 00 §2.1 C2 原文末尾多一个「。」。00 §8 M1.3 口径为「与 C2 一致」（区别于 M1.2 的「逐字一致」），语义无偏差，仅记录备查。

## 6. 风险建议

- **R3 双审补位**：GPT 压缩 Risk/Architecture Review 按派发令留 G1 门统一执行（§4.6 规则 2；v1.3 口径落 `docs/reviews/M1.3.gpt-review.md`）；建议其输入含本报告 §5 P3-1 与 `git diff --stat rebuild/v1..HEAD`。
- **G1 状态流转**：0002 转 Accepted 的决策动作为项目负责人（ADR L70 已正确声明）；G1 门同时按 §5 P3-1 登记 B 线收割输入。
- **无返工要求**：本步骤不需要任何修改；M1.3 完成条件满足。
