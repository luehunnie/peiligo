# A2.1（M2.1 结构实现）实现报告与证据

| 项 | 值 |
| --- | --- |
| 任务 | A2.1：在 A1/A1.1 干净 Wagtail 骨架上落地已冻结 M2.1 稳定结构（docs/INFORMATION_ARCHITECTURE.md §1–§5+§13） |
| 分支/基线 | `arch/a2.1-structure`（派发 worktree a2.1-impl），基于 rebuild/v1@035c5ab；本地提交，未 push |
| 环境 | 复用 ../a1.1-impl/.venv（Wagtail 7.4.2 / Django 5.2.17 / Python 3.13，与 ADR-0001 钉版一致，零新依赖）；`.env` 按 ENV 指令复制自 a1.1-impl，不入库 |
| 数据库 | 仅 peiligo_restart_dev（migrate/bootstrap）与 peiligo_restart_test（pytest，TEST_DATABASE_URL 指名）；旧库零触碰零 DROP（见 §5） |

## 1. 落地内容

| 文件 | 内容 |
| --- | --- |
| `home/models.py` | `SECTIONS` 冻结清单（中文名=PRD §6 一字不差，slug=IA §4.3 默认值）；`HomePage` 增挂载约束（父=仅站点根，子=仅 SectionPage）；新增 `SectionPage`（父=仅 HomePage，子=仅 DepartmentContainerPage）。复用既有 HomePage（M1 迁移产物，未重建） |
| `departments/models.py` | 新增 `DepartmentContainerPage`（工作名）：父=仅 SectionPage；`subpage_types=[]`（M3 内容页类型到位前不允任何子页）；`serve()` 抛 `Http404`（前台 404/等效不可达，ADR-0004 决策 2）；`preview_modes=[]`（无前台页面可预览，关闭后台预览）；`search_fields=[]`（索引级排除——容器标题不入搜索索引 ⇒ 默认搜索/自动补全查不到容器，IA-04）。最小结构壳：零内容字段、零 M3 部门字段体系 |
| `home/migrations/0003_sectionpage.py` | makemigrations 生成（SectionPage 表） |
| `departments/apps.py`、`departments/migrations/0001_initial.py` | AppConfig（与 home 同款）与首个迁移（容器表） |
| `home/templates/home/section_page.html` | 板块页结构壳模板：仅板块名标题，零假业务内容（列表/入口留 M2.2/B 线） |
| `home/management/commands/bootstrap_sections.py` | 五板块 bootstrap：安全（默认站点根非 HomePage 即报错；同 slug 非板块页仅告警不触碰）、幂等（同 slug 板块页跳过，二次执行零新建零删除零改动）、零假业务内容（仅建五板块并发布；不建任何部门容器——IA §1.3 建容器规则） |
| `tests/test_ia_structure.py` | 新增 20 测试（+8 子测试），覆盖 IA-01..05 当前可验证部分、层级限制矩阵、bootstrap 幂等/零假内容、树形/URL 同构（详见 §3） |
| `README.md` | 本地启动增 `bootstrap_sections` 步骤；验收 pytest 计数 14→34；treebeard.E001 说明 3→5 条（新增两个 Page 模型各 +1，上游已知）；浏览器预期增五板块与容器 404 说明 |

**类型落位依据**（工作名，M3.1 终判前不新增 ADR）：`DepartmentContainerPage` 落 `departments/`（03 计划 §2：该 app 承载"部门模型、组、权限容器树"）；`SectionPage` 落 `home/`（站点树入口 app，03 §2 未为其另设 app）。

## 2. 语义落实对照（IA §3.2 四类排除）

| 硬约束 | 本步落实 | 留给后续 |
| --- | --- | --- |
| 容器前台 404 | `serve()` 抛 Http404，pytest 实测（经完整路由/中间件栈） | M4.3 越权矩阵复测 |
| 不进 sitemap | 当前站点无 sitemap 发射器（空真）；测试守护：无 `wagtail.contrib.sitemaps` 且 `/sitemap.xml` 404 | M2.2 §10 实现 sitemap 时按本类型排除并替换守护 |
| 不进默认搜索 | `search_fields=[]` 索引级排除 + 行为实测（按容器标题/slug 检索零命中；对照板块页可检索证明链路活着） | M2.2 §6 细化排除规则 |
| 不进导航/默认列表 | 前台无任何导航/列表消费容器节点；bootstrap 不产生容器 | M2.2 §6/IA-06 |

## 3. 测试清单（tests/test_ia_structure.py，34=14 基线+20 新增全绿）

- **IA-01**：五板块标题与冻结中文名逐一相等（测试内独立字面量，非同义反复）；旧站 3 命名未复入任何板块标题；源码级扫描（home/departments/search/src/templates 的 *.py+*.html）零旧站命名字面量
- **IA-02**：容器 URL 直达 GET=404；容器存在时父板块页仍 200
- **IA-03**：无 sitemap 发射器守护（见 §2）
- **IA-04**：按容器标题/slug 默认搜索零命中；对照板块页可检索；`search_fields==[]` 机制自证
- **IA-05**：slug 集合恰为 chronicle/events/materials/software/guide；五 URL 各 200；`get_url_parts` 站点相对路径 `/<slug>/`
- **层级限制**（IA §5 矩阵）：HomePage 仅根下；SectionPage 仅 HomePage 下（不在根/板块/容器下）；容器仅 SectionPage 下（不在根/首页/容器下）；`creatable_subpage_models()==[]`（不得跳容器建内容页/容器套容器）
- **树形同构**：Home→Section→Container 相邻三层，depth+1/+2，url_path=树径（方案 A）
- **bootstrap**：五板块就位且 live、复用唯一 HomePage/站点；二次执行 pk 集合不变；零容器/首页下恰五子页；预览关闭（`preview_modes==[]`、`is_previewable()` False）

## 4. 验收命令结果（全部亲跑，2026-08-18，worktree a2.1-impl）

| 命令 | 结果 |
| --- | --- |
| `manage.py check` | exit 0；5 条 treebeard.E001=上游已知（M1 基线 3 条+新 Page 模型 2 条，README 已更新说明） |
| `manage.py makemigrations --check --dry-run` | `No changes detected`，exit 0 |
| `manage.py migrate` | `No migrations to apply`（首次已应用 departments.0001+home.0003），幂等 |
| `pytest -q` | **34 passed, 8 subtests passed**（A1.1 基线 14 零回归+新增 20） |
| `ruff check .` | `All checks passed!` exit 0 |
| `ruff format --check .` | `64 files already formatted` |

runserver 冒烟（127.0.0.1:8765，curl 实测）：

| URL | 状态 | 证据 |
| --- | --- | --- |
| `/` | 200 | `Welcome to your new Wagtail site!`（既有首页复用未动） |
| `/chronicle/` `/events/` `/materials/` `/software/` `/guide/` | 各 200 | h1 依次=校园纪事/校园活动/学习资料/软件与工具/校园指南（slug 正确） |
| `/admin/login/` `/search/` | 200 | 零回归 |
| `/chronicle/jwc/` | 404 | 路由级 404；容器 serve 级 404 由 pytest 建真实容器实证（不向 dev 库写假部门数据，见 §5） |

## 5. 数据红线与 DO_NOT 合规

- **DB**：仅连 peiligo_restart_dev/test（socket /tmp）；前后快照比对 `peiligo_dev 8054 kB / peiligo_test 8134 kB / postgres 7774 kB / template0 7617 kB / template1 7854 kB` 完全一致（/tmp/a21_db_before.txt vs after）；全仓无 DROP。
- **dev 库终态**：首页下恰五板块（depth 3、live、slug/title 正确）；`departments_departmentcontainerpage` 计 0——容器 404 的 live 验证选择走 pytest（测试库内建真实容器经完整 HTTP 栈断言 404），不在 dev 库造临时假部门节点。
- **DO_NOT**：①未建 NoticePage/ArticlePage/MaterialPage/SoftwareToolPage/GuidePage/EventFieldsMixin（grep 零命中）；②零伪造业务数据；③零新增 ADR（类型名=工作名，落位依据记于本报告）；④旧库零触碰；⑤未入 M3（容器子页白名单留空待 M3 扩充）。
- **秘密纪律**：diff 秘密扫描零命中；`.env` 不在 Git（status 干净）。

## 6. 遗留与风险（移交后续批次，非本步缺口）

1. 容器 `subpage_types=[]` 为 M2.1 过渡态：M3 落地内容页类型时须扩充白名单（IA §5 表）并同步更新 `test_container_allows_no_subpages_yet`。
2. sitemap/导航/默认列表排除的实现位点在 M2.2（IA §6/§10）：当前为空真+守护测试，M2.2 须落实容器排除（IA-03/IA-06）。
3. `Page.url` 在多 Site 缓存态下退化为绝对 URL（wagtail site-root-paths 类级缓存不随测试回滚失效，A1 既有测试建多 Site 触发）——测试改用 `get_url_parts()` 断言，非产品缺陷。
4. treebeard.E001 5 条为上游弃用提示，升级 Treebeard 6 前无动作（README 已记）。
