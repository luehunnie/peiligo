# M2.1 独立审查报告（信息架构 §1–§5：站点树 / 五板块 / 部门容器树）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M2.1 步骤全部产物：`docs/INFORMATION_ARCHITECTURE.md`（新建，204 行，单文件）；实现提交 `2339f5a`（分支 `arch/m2.1-site-tree`，基于 `rebuild/v1@ff1e1dd`）；被审面 = `git diff ff1e1dd..HEAD`（实测仅该一个新增文件，+204/-0） |
| 所属步骤 | M2.1 · 站点树、五板块与部门容器树结构（00_MASTER_PLAN §9 M2.1；引源 02 §4 S2.1 L330–362） |
| 风险级 | R1（00 §4.6 规则 7 缺省参考：M2 = R1，普通文档步骤；无 GPT 参与项） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M2.1 实现；未读取实现 transcript、不以实现者自述为证据；全部结论基于对原始文档（00/02/PRD/ADR/旧站审计）的独立读取与命令亲自复跑；除本报告文件外一切文件与 git 状态只读（仅运行只读 git/grep/sed/test/python 取证）；报告中的命令输出均为本会话真实输出 |
| 权威对照源 | 00 §9 M2.1 步骤块（验收命令原文）、§4.5（扫描正则 v1.2 双分支）、§4.6（双口径规则）、§4.7（分级 A 三件套）；02 §4 S2.1（输出清单 + 验收命令 + 断言全集 IA-01..05）、§0.2（全局约束 2/3/5/7）；`docs/PRODUCT_REQUIREMENTS.md` §6（逐字）、§4.3、§7.1–§7.6、§10、§20；ADR-0004、ADR-0005（均 Accepted）；旧站审计 §5.1、R12 |
| 审查方法 | 口径A 需求/架构八项清单（§3）+ 口径B 验收命令/比对/扫描亲自复跑（§4）+ §12/§13 越界专项裁定（§5）一次覆盖 |

## 2. 结论

**通过**。无 P0 阻断项、无 P1 批准前修复项、无 P2 修改条件项；2 项 P3 观察 + 1 项 P4 观察记录于 §6（均不构成修改条件）。M2.1 完成条件「验收绿 + 独立审查通过」满足，可按 00 §4.6 由总控合并入 `rebuild/v1`。专项裁定：实现者在 §1–§5 之外落盘的 §12 追溯骨架与 §13 断言附录（IA-01..05）**属分级 A 要求的合理载体，非越界扩围**（裁定依据见 §5）。

## 3. 口径A · 需求/架构逐项核对（派发令清单 1–7）

### 3.1 板块命名冻结：与 PRD §6 一字不差 — PASS

PRD §6（L97–101）五板块：**校园纪事、校园活动、学习资料、软件与工具、校园指南**。IA §2 表冻结名列、§1.1 树图、§13 IA-01 三处与 PRD 提取名单做排序 diff，**输出为空（一字不差，字节级）**（实测记录见 §4.3）。§2 表头自我声明「中文名的唯一权威为 PRD §6」，定位列注明为归纳表述、语义引源 PRD §7——归纳与权威的边界交代清晰。

### 3.2 与 ADR-0004（容器树不构成前台部门主页）一致 — PASS

| ADR-0004 要件 | IA 落点 | 实测 |
| --- | --- | --- |
| 决策 1：容器承载页面树权限（Add/Edit/Publish/Lock/Unlock）向子树传播；内容页以本部门容器为唯一父级；不自写权限逻辑 | §3.1「容器 = 权限挂载点」；§1.3 第 2 条 | ✅ |
| 决策 2：前台 404（或等效不可达）；不入导航/sitemap/默认列表/默认搜索；URL 可含部门段但不渲染任何部门聚合页 | §3.2 三条硬约束逐项；§4.1 容器段「不渲染任何页面」 | ✅ |
| 决策 3：V1 部门账号不持任何 Snippet 权限；词表/部门数据由总管理员管理 | §3.4（引 ADR-0004 决策 3 + ADR-0005 载体表 #7） | ✅ |
| 「代价与权衡」遗留：建容器规则留给 S2.1/M2.1 | §1.3 落定「仅为确有内容的部门建容器」+ 清空后保留/清理两可处置（均不影响前台，容器本就 404）——处置逻辑自洽 | ✅ |
| 结构纪律：容器只挂板块页下、不嵌套、不挂首页/根 | §1.3 第 2 条 + §5 补充约束第 2 条（subpage_types 白名单不含容器/板块类型） | ✅ |

### 3.3 与 ADR-0005（Page/Snippet 载体分配）一致 — PASS

- §5 表：NoticePage/ArticlePage/MaterialPage/SoftwareToolPage/GuidePage 五类内容页 parent 均为 DepartmentContainerPage、载体均 Page——与载体表 #1–#6 一致；
- 活动行：「非独立 Page 类型：预期经 NoticePage/ArticlePage 上的 EventFieldsMixin 承载，挂载方式与类型组织由 M3.1 终判，载体仍为 Page」——与 ADR-0005 载体表 #3「预期而非终判」措辞严格同构，未提前终判（ADR-0005 替代方案 2 明示此为红线）；
- SoftwareToolPage 行「外链经统一确认跳转页（自定义视图，不改载体）」= 载体表 #5 原口径（PRD §7.5）；
- §5 补充约束第 3 条：Snippet 与设置项（部门、词表、受控标签、FeaturedItem、站点级配置）不入页面树——与载体表 #7–#14 一致，且与 §3.4（Department Snippet 承载部门清单）闭环。

### 3.4 无部门主页形态 — PASS

§3.3 引 PRD §6 两句原文并展开四条（作者元数据展示 §4.3、搜索/筛选维度 §10、筛选结果 ≠ 部门主页、无部门主页为 V1 硬约束引 PRD §20「部门独立主页」不做项 + 02 §0.2-3/§0.2-5）；§4.4 URL 一览表中「部门筛选结果页 = query string 形态、200、不是部门主页」——全文**不存在任何部门聚合页/部门简介页/部门门户形态的页面定义**；§3.3 明确筛选页的 URL 形态与标题文案留给 M2.2 §8/§9 且冻结「不得呈现为部门主页/门户形态」语义。

### 3.5 旧站 3 个不一致命名（审计 R12）未复入 — PASS

R12 原文（旧站审计 L64/L174）：`content-types.ts:14-36` vs PRD:97-101，3/5 板块名不一致（院内逸事/软件资源/学院指南）。IA 文中三词**各出现恰 2 次**：§2 弃用声明句（02 S2.1 §2 输出显式要求「明确弃用旧站 3 个不一致命名，引用旧站审计 R12」）与 §13 IA-01 断言复述——均为元语言语境（记录禁令本身），非用作板块名、slug、代码标识或界面文案；五个新 slug（chronicle/events/materials/software/guide）均不派生自旧命名。语义层禁令（「不得复入任何界面、URL、代码标识或文案」）已冻结，界面/文案全量核对按 IA-01 挂交付线。

### 3.6 URL 方案与 02 S2.1 断言全集逐条核对 — PASS

| 02 S2.1 §4 规格 | IA §4 落点 | 实测 |
| --- | --- | --- |
| 方案 A 默认——URL 含部门段如 `/chronicle/<dept>/<slug>/` | §4.1 形态 + 示例；§1.2 示例路径表 | ✅ |
| 容器段不渲染任何页面 | §4.1 第 2 条 + §4.4 表（404） | ✅ |
| 方案 B——无部门段需覆写 URL 生成、复杂度高，记录为否决候选 | §4.2 否决理由三条 + 处置（后续重开须新开 ADR，沿 ADR-0005 硬规则变更纪律） | ✅ |
| slug 用 ASCII 拼音/英文，规范细则 | §4.3 七行规范表（字符集 `[a-z0-9-]`/来源/唯一性/长度/板块 slug/部门 slug/尾斜杠） | ✅ |
| 板块 slug 建议 `chronicle/events/materials/software/guide`，标记为可被 B 阶段微调的默认值 | §2 表列头「slug（默认，B 阶段可微调）」+ §4.3 板块 slug 行 + IA-05「默认值；B 阶段微调须留痕」 | ✅ |
| 测试要求断言 IA-01..05 | §13 五条逐一对应（名称一致/容器 404/不入 sitemap/不入默认搜索/slug 命中 §4 表），每条有引源与验证位点（M4.3 实测、M2.2 位点、M3.1/M5.x 实现） | ✅ |

### 3.7 §1–§5 覆盖 M2.1 全部规定产出 — PASS

逐项对 02 S2.1「输入 → 输出」清单：§1 树形 ASCII 图 + 示例路径 + 四层冻结（含扩层走变更流程的反静默条款）✅；§2 冻结名 + 一句话定位 + 弃用 3 名 + R12 引用 ✅；§3 每板块按部门建容器 / 权限挂载点 + 路由分组 / 前台 404 / 四类排除 / 部门字段仅元数据与筛选 ✅；§4 URL 方案全要素 ✅；§5 挂载约束表（parent_page_types 目标 = 容器、subpage_types 白名单、直接喂 S3.1/S4.2）✅；§12 骨架（§1–§5 行）+ §13（IA-01..05）✅（裁定见 §5）。边界合规：未写字段表（S3.2 边界）、未改 ADR、只新建一个文件（02「单步骤对话边界」）；§6–§11 显式留白并标注 M2.2 增补，文档头「待增补」行自我锁定范围。

### 3.8 越界四查（00 §4.6 口径A） — PASS

- **不改 Wagtail 核心**：无（方案 A 选定理由第 1 条即「零 URL 生成覆写」；确认跳转页为自定义视图、不改载体，与 ADR-0005 #5 同口径）；
- **不扩 V1**：无（筛选结果页属 PRD §10 既有要求；无 PRD §20 不做项引入；§3.3 反向收紧「不提供任何部门聚合页」）；
- **不破坏权限模型**：无（§3.1/§3.4 全按 ADR-0004 决策 1/3，零自写权限逻辑）；
- **不偏离信息架构**：无（与 PRD §6 逐句对齐；四层为 02 S2.1 规定层数）。

## 4. 口径B · 实现/测试/安全实测（审查者亲自复跑，真实输出）

### 4.1 00 §9 M2.1 验收命令 — 全 PASS

```
$ test -f docs/INFORMATION_ARCHITECTURE.md
PASS: 文件存在 (exit 0)

$ for s in 校园纪事 校园活动 学习资料 软件与工具 校园指南; do grep -q "$s" docs/INFORMATION_ARCHITECTURE.md || echo "缺板块: $s"; done
PASS: 含板块 校园纪事
PASS: 含板块 校园活动
PASS: 含板块 学习资料
PASS: 含板块 软件与工具
PASS: 含板块 校园指南        # 五名逐一命中，零缺失输出

$ grep -c 'IA-' docs/INFORMATION_ARCHITECTURE.md
9                              # ≥ 断言清单数 5（IA-01..05）PASS

$ grep -qE '404' docs/INFORMATION_ARCHITECTURE.md
PASS: 含 404 (exit 0)

$ ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
PASS: 零命中（grep 退出码 1 且无输出）   # §4.5 v1.2 双分支正则原文，对 docs/ 全目录
```

### 4.2 02 S2.1 验收命令（引源断言全集） — 全 PASS

```
$ grep -qE '不是部门主页|不构成部门主页|无部门主页' docs/INFORMATION_ARCHITECTURE.md
PASS (exit 0)   # 4 处命中：L96（PRD 引文）、L102、L103、L154

$ grep -q 'parent_page_types' docs/INFORMATION_ARCHITECTURE.md
PASS (exit 0)

$ grep -c 'PRD §' docs/INFORMATION_ARCHITECTURE.md
28             # 期望 ≥ 8，实测 28，条款引用充分

$ git status --porcelain
(空输出，exit 0)  # HEAD 已提交故工作区净

$ git diff ff1e1dd..HEAD --stat
 docs/INFORMATION_ARCHITECTURE.md | 204 +++++++++++++++++++++++++++++++++++++++
 1 file changed, 204 insertions(+)
                # 与 02「允许修改文件范围：仅新建该文件」精确一致，零越界文件
```

### 4.3 板块名与 PRD §6 diff 实测 — PASS

PRD §6（L95–101）编号列表提取五名 vs IA §2 表（`^\| [0-9] \|` 行第 3 列）提取冻结名，各自排序后 `diff`：

```
$ diff /tmp/prd_names.txt /tmp/ia_names.txt
（无输出）
PASS: diff 为空——五个板块中文名与 PRD §6 一字不差
```

加强证据：§3.3 引文与 PRD §6 原句（L103/L105）逐字对照——L105 句逐字节一致；L103 句经 python 逐字符比对，唯一差异为引号字形（PRD 全角弯引号 U+201C/U+201D vs IA 直引号 U+0022，共 4 处），归一化引号后**逐字节一致、其余字符零差异**（见观察项 P3-1）。

### 4.4 安全面 — PASS

§4.5 秘密扫描零命中（见 §4.1 末条，实测）；全文仅含路径、条款号与文档名引用，无凭据、无令牌、无环境变量值；本报告自身落盘后复扫 `docs/` 仍零命中（commit 前复跑）。

## 5. 专项裁定：§12 追溯骨架与 §13 断言附录——合理载体，非越界扩围

**裁定：属 00 §4.7 分级 A 要求的合理载体，不构成越界。** 依据链（按权威层级）：

1. **02 §4 S2.1 输出清单明文列有**「§12 可追溯性表（本步骤先建表骨架并填 §1–§5 行）」——即 §12 骨架本就是本步规定产出，非实现者自创；
2. **02 S2.1 测试要求明文列有**「断言清单（入文档附录 §13，本步骤先建附录并写入）：IA-01..IA-05」——§13 附录与五条断言同样是规定产出；
3. **00 §9 M2.1 输出行自含**「§1–§5（**含断言 IA-01.. 系列**）」——00 自身已把断言系列计入本步输出；
4. **00 §4.7 分级 A 的验收三件套** = 验收命令（存在性 + 断言 grep）+ **可追溯性表检查** + **断言清单核对**——后两者必须有文档载体，§12/§13 正是该载体（否则分级 A 验收在本步无物可查）；
5. **文件面零越界**：diff 仅新建 `INFORMATION_ARCHITECTURE.md` 一个文件，满足 02 §0.2-9 文件互斥；§6–§11 未预写（不抢 M2.2 范围），§12 表仅填 §1–§5 行、§13 仅写 IA-01..05 并显式声明 IA-06..09 归 M2.2——扩的是载体不是内容范围。

**内容核对**：§12 五行的「源计划 / PRD 条款 / ADR·审计」列逐格与实际一致（含 R12、ADR-0004 决策 1/2、ADR-0005 载体分配原则表）；§13 五断言与 02 S2.1 测试要求逐条同义且各附引源与验证位点，验证位点挂接正确（IA-02/03/04 → M4.3 越权矩阵，位点规则 M2.2 §6/§10；IA-05 → M3.1/M5.x；IA-01 → 交付线全量核对）。

**字面张力记录**（观察项 P3-2）：00 §9 M2.1「允许修改的文件范围：该文件 §1–§5」字面窄于 02 S2.1 的 §12/§13 明文要求；按引源关系（00 M2.1 块引源 = 02 §4 S2.1）与 00 同块输出行「含断言 IA-01.. 系列」作调和读法，裁定实现者遵循直接源计划、无越界之实——张力属主计划措辞问题，非本步产物缺陷。

## 6. 观察项（不构成修改条件）

| # | 级别 | 观察 | 建议 |
| --- | --- | --- | --- |
| P3-1 | P3 | §3.3 引文（L94）将 PRD §6 原文的全角弯引号（U+201C/U+201D）转写为 ASCII 直引号（U+0022），共 4 处；逐字符比对确认除引号字形外零差异、语义零漂移。「一字不差」冻结仅涉板块中文名（02 §0.2-7，已字节级通过），引文受「原文（逐句对照）」表述约束，属标点规范层面瑕疵。 | M2.2 增补 §6–§13 时顺带统一全文引号风格（正文他处两种引号混用，可一并梳理）；不阻断、不需 Fix Session。 |
| P3-2 | P3 | 00 §9 M2.1「允许修改的文件范围：该文件 §1–§5」与 02 S2.1 输出清单（§1–§5 + §12 骨架 + §13 附录）存在字面不一致；实现按 02 执行（见 §5 裁定）。 | 建议 00 下一次修订时将该行对齐为「该文件 §1–§5、§12 骨架、§13 附录（IA-01..05）」，消除后续审查的裁定歧义；主计划措辞问题，本步产物不改。 |
| P4-3 | P4 | 02 S2.1 依赖行列有「PRD §6、§9」，而 IA 文档引源仅列 PRD §6/§7——与 00 §9 M2.1 输入（v1.1 修正：仅 PRD §6；§9 首页与导航归 M2.2）一致，无实质缺口（本步产出不含任何 §9 范围内容）。 | 无需动作；供后续步骤引用口径参考（00 v1.1 修正后的分工为准）。 |

## 7. 风险建议

1. **M2.2 衔接清单**：§6–§11 + §12 对应行 + IA-06..09；其中面包屑「含/不含容器段的展示规则」须按 §4 方案 A 写死、筛选结果页 URL 形态与标题文案（「XX 部门发布的内容（筛选结果）」类）按 §3.3 冻结语义展开；容器不进导航/默认列表的排除规则与 noindex 位点（§3.2 已登记的落实位点分工）为 M2.2 义务。
2. **M3.1 义务承接**：页面类型工作名终判 + §5 表「板块级归属限制」须以页面级校验（clean/validator）落实并纳入 M4.3 测试——§5 表头注释已自我登记该边界，M3.1 不得遗漏；EventFieldsMixin 挂载终判须与 ADR-0005 #3 零冲突核对。
3. **实测挂接**：IA-02/03/04（容器 404、不入 sitemap、不入默认搜索）由 M4.2 原型与 M4.3 越权矩阵实测并回写 ADR-0004「验证证据」节；IA-01 的界面/文案全量核对义务在交付线——建议 MC 阶段验收清单显式登记，避免文档期断言到交付期失挂。
4. **文风卫生**：P3-1/P3-2 均为轻量观察，可分别并入 M2.2 增补与 00 下一次修订，不单开 Fix Session。
