# A3.3（M3.4 实现）独立审查报告

- **审查对象**：`arch/a3.3-search-mapping-impl@e70822e`（基线 `rebuild/v1@8d76502`）
- **审查载体**：`arch/a3.3-impl-review@7123470`（`--no-ff` 合并节点；审查产出单 commit，不改实现、不 push）
- **自报告**：docs/reviews/A3.3.impl.md（已通读，零采信——全部验收由本审查独立重跑）
- **环境纪律**：本 worktree 自建 venv（Python 3.13.11；`peiligo.__file__` 亲证指本 worktree）；Wagtail 7.4.2／Django 5.2.17／modelsearch 1.3.2；PG postgresql@18 /tmp:5432，应用连接仅 `peiligo_restart_dev`（migrate no-op）与 `peiligo_restart_test`（pytest 建弃）；旧库 `peiligo_dev`/`peiligo_test` 零 DROP、访问面仅 pg_dump 只读（快照手法缺陷见 §10 O-2，如实登记）
- **方法**：①diff 13 文件逐 hunk 亲读（改动面＝自述：五 models＋services/views/两模板＋四测试文件＋自报告，零私加）；②27 例独立探针（两套，repo 根跑后即删不入提交）——期望值全部按 CONTENT_MODEL §20–§24／IA §8–§11 由审查者手写推导，fixture 独立构造（自有 slug/标题/部门/词表名，与 impl 测试集零重名）；③全量回归独立复跑；④静态面 grep/亲读。

## 1. 必查 1：search_fields 反射对照表 A/B 零漂移 — PASS

- 探针 P1 三例（反射读 `model.search_fields`，期望表手写非抄 impl 测试）：
  - **基类原状**：`Page.search_fields`＝title 三件套（SearchField boost2＋AutocompleteField＋FilterField）＋FilterField×13（含 `first_published_at`、无 `expired`）——与 §20.0 事实 1 逐项吻合。
  - **五页逐列对照**：NoticePage/ArticlePage（表 A＋summary/body 中＋event_location 0.5＋TAG_SEARCH_FIELDS）、MaterialPage（＋discipline/material_type RelatedFields[name Search∪Filter]）、SoftwareToolPage（body＋license_note 0.5＋platforms）、GuidePage（location/opening_hours 中＋contact/extra_notes 0.5＋category）——`_spec()` 逐字段名×类型×boost 全等，含表 A 六行骨架（title/live/expired/path/first_published_at/department[name Search, slug Filter]）与 F-1 修复行 `FilterField("first_published_at")`。
  - **无私加第六声明**：全模型 `__dict__` 扫描——仅五内容页自定义；`DepartmentContainerPage.search_fields=[]`（A3.1 既有）；SectionPage/HomePage 未遮蔽（解析到基类）。
- 声明载体：`notices/models.py:94-130`（boost 常量 2/0.5、`TAG_SEARCH_FIELDS`:99、`content_search_fields`:105）、五页挂接点 notices 259/317、resources 132/190、guides 96。零新增模型字段（makemigrations --check 无漂移亲证）。

## 2. 必查 2：过滤层契约 — PASS

- **type 五值单射**：`TYPE_IDENTIFIERS`（services.py:27-33）双射探针＋`model_for_type` 未知值→None。
- **section 五冻结 slug＋伪板块双重防线**：`SECTION_SLUGS`（services.py:24）与 `home.models.SECTIONS` 同源探针；`_resolve_section` 先闸 `value not in SECTION_SLUGS → None`（services.py:142-144）再查树内实存。树内实存伪板块页（slug="news"）探针：`?section=news` 视同未提供（q 在场＝与纯 q 同集）；内容页挂伪板块被 `NoticePage.clean` §1.4 白名单拒（ValidationError 亲证）——参数闸＋结构闸双防线成立。
- **dept 语义**：`_resolve_department` 限 `is_active=True`（services.py:147-151）——停用部门实测退出词表（视同未知值）。
- **tag 终判**：`resolve_tag_value`（services.py:59-76）＝Tag slug∪name∨四类别词表 name；`_tag_condition`（:179-194）按类型通道 OR 展开、同名并集无优先级——slug 通道/name 通道/词表通道×类型不符＝合法空结果逐项探针通过。
- **filter-first 组合**：可见性→path→dept→tag→排序→`.search(q)` 执行序（services.py:209-221）；q＋板块/部门/类型组合结果集探针全对。

## 3. 必查 3：可见性 CURRENT_DEFAULT 同谓词 — PASS

- `_current_default_search_queryset`＝`live().filter(expired=False)`（services.py:197-202）＝§16.4 同谓词逐类型形态。
- 探针 P5：draft／scheduled 未到点／unpublished／expired 四排除（q、板块列表、type 三入口皆不返回）；live 对照在场。结构页（SectionPage/HomePage）、容器（DepartmentContainerPage）、按其名/词表检索零入结果——搜索对象域＝五类型结构性达成（逐类型入口＋`subpage_types` 链）。
- 泄漏面：旧 `current_default_pages` 全量列表入口已从生产路径移除（grep 零引用）；sitemap 未变。

## 4. 必查 4：URL 参数契约 — PASS

- /search/ 五参（q/section/dept/type/tag）全 GET——同参两次请求同结果（可分享/刷新，探针＋既有 L160-162）；`resolve_search_filters` 恰读五参（services.py:154-162）。
- 板块页四参：`resolve_section_filters`（services.py:165-176）`?section=` 不读即忽略（含合法值，探针三态）；板块维度由路径锚定（`section_from_path=True` 不计入 filter_active——板块页零内容＝"全空"而非"筛选无匹配"，IA §11 两空态分界正确）。
- V1 零时间参数：参数读取面 grep 恰五枚，无任何时间参数。

## 5. 必查 5：非法值处置 — PASS

- 未知 section/dept/type/tag、大小写敏感（CHRONICLE/AUDIT-DEPT/NOTICE）、空串五参、词表外英文 tag——全部 200 且结果集与去该参严格相等（探针 11 组逐一）；全非法组合同；无 404/5xx。
- 重复参数（QueryDict 取末值）200 不炸；空白 q＝表单态不加条件（services.py:157 `.strip()`）。
- 非法参数＋无有效条件＝表单态不查全量（views.py:17 `filter_active` 闸）——与 IA §9.2 #1 一致。

## 6. 必查 6：SEO — PASS

- /search/ 全部状态（无参/q/dept/section 非法/type/tag）无条件 noindex＋canonical 去参指 `/search/`（模板亲读＋探针六态）。
- 板块页：默认态可收录（无 noindex）；任一 GET 参数态 noindex＋canonical 指板块基础 URL——由 base.html 既有 `request.GET` 全局位点承载（templates/base.html:19-27，M2.2 零新增）。
- §8.2 标题模式：dept 筛选态「校园纪事·审计处发布的内容（筛选结果）」／「站内搜索·审计处发布的内容（筛选结果）」H1＋`<title>` 双载体各 ×2；IA-08 禁用措辞四词零出现。

## 7. 必查 7：中文质量诚实 — PASS

- impl 测试与探针全部整 token 断言；E2（PG FTS＋simple）CJK 连续串单 token 语义如实（子串不命中＝文档化行为）；零质量指标宣称、零 AI 搜索措辞（grep 亲证）。

## 8. 必查 8：D7 未预决＋M6 未越界 — PASS

- 零 ADR-0006、零新依赖（requirements/pyproject/settings/urls 零触碰，diff 亲读）；后端配置原状（base.py:217-221 `wagtail.search.backends.database`＝modelsearch 兼容 shim，亲读 site-packages 转发行）。
- 金标集仅 `GOLDEN_SET_ENTRY_FIELDS`/`POC_FIELD_CONTRACT` 常量占位（services.py:247-248，§23 字段名逐字）；零数据集、零 PoC 产物、零中文分词质量声称。

## 9. 必查 9：三条自登记裁决独立复核＋全量回归 — PASS

1. **`path__startswith` 等价性（自报告 §2.1）成立**：结构性证明——五内容页 `subpage_types=[]`＋SectionPage→DepartmentContainerPage→内容叶链＋各内容页 clean 板块白名单 ⇒ 五类型对象域上 path 前缀区间≡子树；锚点 SectionPage/容器不在域内。探针 P8：`descendant_of` 经 `.search()` 被 check() 编译闸拒（FilterFieldError 提及 'depth'——表 A 无 `FilterField("depth")`，其弃用前提亲证）；等价性双查询集全等。
2. **W001×5（§2.2）亲跑同数**：`manage.py check` exit 0，15 issues＝10 treebeard.E001（A2.1 既有）＋5 wagtailsearch.W001；选 (a) 文档化告警与"严格表 A/B 精确声明"批令一致（承接基类形态反而违事实 2 逐项自含口径）。
3. **M2M 发布期索引缺口（§2.3）行为面独立复现**：探针 P7（E2 索引表经 `wagtail.search` 应用承载——base.py:60；故以检索可达性为观测面）打出 `indexed_at_publish_m2m=False / indexed_at_publish_fk=True`：发布后再设 tags 再发布的页面，发布时点 q=标签名不命中，update_index 后命中（SearchField name 通道）且 tag= 双通道可筛；FK 词表（discipline）发布时点即命中——与自报告 dump 结论两侧互证。生产 update_index 接线归 MB10 不越界判定成立。
4. **全量回归复跑（数字与自报告逐字比对）**：pytest **274 passed + 132 subtests**（同）；check exit 0/15 issues（同）；makemigrations --check No changes（同）；migrate no-op；ruff check All checks passed（同）；ruff format 全已格式化——文件数实测 **108** vs 自报告 107（O-1）；§4.5 v1.2 双分支正则 docs/ 零命中（含本报告落盘后复扫）；新文件行数 services 248／views 27／test_search_contract 210／test_search_fields_reflection 117（≤250，同）。
5. **陈旧测试更新五处**（自报告 §5）逐条 diff 亲读——均系 M2.2 时代口径→M3.4 对象域的必要跟随，期望值独立判定合理。

## 10. 发现

### F-1（P3）：PS-35 排序子句「默认排序发布时间倒序」全测试套件零锚定

- **事实**：grep 全 `tests/*.py`——搜索/板块测试无任何排序断言；两新测试文件唯一相关命中＝反射规格声明行（test_search_fields_reflection.py:36 `FilterField("first_published_at")`，声明≠行为）。行为断言均为集合比较（`_pks` 构造 set，test_search_contract.py:78）序不敏感；仅有的两处列表断言＝空列表（:153）与 GET 二次同序（:160-162）——后者锚定确定性而非排序契约（rank 序同样确定，无法拦截回归）。自报告 §4 将 PS-35 映射至 FilterDimensionTests 五子句，第六子句（排序）无落点。
- **风险机理**：设计审查 F-1 已亲证——E2/E3 默认调用（`order_by_relevance=True`）以相关度 rank 整体替换 queryset 排序且零告警（modelsearch postgres backend rank 替换位点；§21.6/CONTENT_MODEL L779 保序口径）。实现本体正确（services.py:216-221 显式 `order_by_relevance=False` 保序旋钮在位），但该旋钮若被移除/回归，**测试全绿、行为静默变更**——恰为冻结断言面（§24 PS-35 含排序子句；IA §9.2 #4）唯一的静默失效模式。
- **行为独立验证（审查者探针，均绿）**：三条同 token 命中页 first_published_at 错峰 -48/-24/0h——`/search/?q=` 严格 [新,中,旧]；q+tag 组合（slug/name 双通道经 M2M FilterField 编译闸）保序；板块页 `?q=` 同保序。
- **修复建议**：test_search_contract.py 增一条 q 在场多命中倒序断言（列表序敏感，靶法可复用上述错峰三页），可另加板块页变体。一处测试，零实现改动。
- **定级**：P3——行为本体经独立验证正确，缺陷在冻结断言的回归保护缺口＋自报告映射表漏项，非用户可见故障。

### 观察（不阻断）

- **O-1**：自报告 §3「ruff format 107 files already formatted」实测 108（impl worktree 同 108）——计数笔误，零行为影响。
- **O-2（审查者自查，如实登记）**：审前旧库快照取 pg_dump 原始 SHA256，而 PG18 pg_dump 每次输出含随机 `\restrict`/`\unrestrict` 令牌（同库连 dump 两份 diff 恰为该两行，239/239 行余逐字同）——原始 SHA 不可复现，审前/审后回溯比对在方法上不成立。补救与边界：本会话对旧库的全部访问＝pg_dump 只读（快照两轮），所有应用连接串（.env）指 peiligo_restart_dev/test；已改用规范化指纹（剥离 restrict 行后 SHA256）双跑稳定落档（peiligo_dev `a3f74913…`／peiligo_test `30456aa6…`，/tmp/a33-ir-snap/post-normalized.sha）。零 DROP、零写。

## 11. 结论

**VERDICT = FIX（F-1，P3×1）**

实现与契约（§20–§24／IA §8–§11）逐项吻合：反射零漂移、过滤层五维语义正确、可见性同谓词、URL 契约/非法值/SEO/诚实性/越界防御全过；三条自登记裁决独立复核全部成立；回归数字逐字复现。唯一缺口＝PS-35 排序子句零测试锚（F-1，P3，修复面一条测试）。修复后单点复审即可收口。

---

## 12. 修复复审附记（A3.3-IR2 单点复审，F-1 闭合确认）

- **复审对象**：`arch/a3.3-impl-fix@0241f21`（基线 `arch/a3.3-impl-review@0450c1d`）；载体 `arch/a3.3-impl-rereview`（分支已改名），产出本节单 commit，不改实现、不 push。
- **环境纪律**：全新会话自建 venv `.venv-rereview`（Python 3.13.11；`peiligo.__file__` 亲证指本 worktree）；应用连接仅 `peiligo_restart_dev`（migrate no-op）与 `peiligo_restart_test`（pytest 建弃，跑毕已由 pytest-django 删除）；旧库 `peiligo_dev`/`peiligo_test` 访问面仅 pg_dump 只读，规范化指纹（剥 `\restrict`/`\unrestrict` 行）全程零变化、双跑稳定；零 DROP 零写。

### 必查 1：diff 逐段亲读 — PASS

`git diff 0450c1d..0241f21` 恰两文件：`tests/test_search_ordering.py`（新增 96 行 ≤250）＋`docs/reviews/A3.3.impl.md`（§4 映射表 PS-35 行单行补第六子句落点，指向 OrderingTests 四例）。实现（services/views/models/模板）与 CONTENT_MODEL §1–§24 零改动（name-status 亲证）。

### 必查 2：排序锚独立性 — PASS（附 O-3 观察）

- **四例亲跑全绿**（`pytest tests/test_search_ordering.py`）：/search/?q= 严格倒序、q+tag slug/name 双通道（2 subtests）、板块页 ?q= 同锚、跨类型合并（guide/material/notice 错峰）；TOKEN「错峰排序靶点通知」与既有 fixture 标题零碰撞（严格列表等值含恰好三命中，行为面自证）。
- **锚几何亲证**（探针跑后即删）：pk 随创建递增 [new<mid<old]、first_published_at 错峰 0/-24/-48h 递减，期望序 [new,mid,old]＝pk 升序＝创建序（`_stagger` 直改列后 `refresh_from_db` 复核，防内存旧值误导）。
- **突变四向独立复跑**：①`merged.sort` reverse 翻转→**5 failed**（与修复者登记同数：三直断＋双 subtest）；②去保序旋钮（`.search(q)` 默认 True）→**4 passed 仍绿**；③合并键改 `key=pk` 升序→**4 passed 仍绿**（O-3，见下）；④整体删除 `merged.sort`→**仅跨类型例 FAIL**（单类型三例由 per-type `-first_published_at` 保序承担，跨类型合并锚独立承担类型串接序——该例不可删）。
- **层1 机制亲证**：绕过 merged.sort 直测 per-type `.search()` 输出——旋钮在位 [new,mid,old]；旋钮去除反转为 [old,mid,new]，与 modelsearch `postgres.py` `_apply_ordering_and_scoring` 的 `order_by(rank_expression.desc(), "-pk")`（同 token 标题 rank 平局→-pk 兜底）逐字吻合——修复者「-pk 兜底可区分」的靶法正是对此真实回归方向。

### 必查 3：修复者风险①裁量 — 成立

本后端（settings `wagtail.search.backends.database`＝modelsearch shim；vendor=postgresql→`PostgresSearchBackend`，亲读 dispatch）下，可观察序唯一载体＝`search_pages` 的 Python `merged.sort`（services.py:237-240，全量重排序非假设有序归并）——旋钮回归经层2矫正于输出不可见（突变②实证），与修复者登记一致。**判定：锚定面＝§21.6/L779 冻结子句的可观察行为（两载体四入口列表序），旋钮属纵深防御不属本测试义务，裁量正确且如实登记**。补强事实：旋钮若回归，层1 输出恰为 [old,mid,new]（-pk 方向）——即未来排序载体下移 SQL（如 MB10 分页）时，同一锚仍咬合该方向，锚对两种载体形态均有效。

### 必查 4：全量回归独立复跑 — 数字逐字吻合

pytest **278 passed＋134 subtests passed**（=基线 274+132＋4 测试＋2 subtests，零回归）；`manage.py check` exit 0＝15 issues（10 treebeard.E001＋5 wagtailsearch.W001 同数）；`makemigrations --check` No changes；`migrate` no-op（peiligo_restart_dev）；ruff check 全过＋ruff format 110 files already formatted（数字对账：108→110＝＋新测试文件＋0450c1d 落档的审查报告 .md——前审 108 系报告文件落档前所测，O-1 笔误谱系闭合非异常；ruff 0.16.3 format 计数含 docs/*.md 代码块）；§4.5 v1.2 双分支正则 docs/ 零命中（含本附记落盘后复扫）。

### 观察（不阻断）

- **O-3（锚几何残余面）**：批令必查口径「创建序≠期望序」在实现中为**创建序＝期望序**（修复者提交信息已明示，系为咬合 -pk 真实兜底方向的刻意取舍——反向几何将漏检 -pk 反而漏掉真实回归方向）。突变③实证残余洞：全局改按 pk 升序排序（`key=pk`）与本 fixture 期望序重合→四例仍绿。单调三页错峰下 ±pk 必有一向重合（结构性）；**非单调错峰**（创建序 [A,B,C] 而发布序期望 [A,C,B]）可两向皆咬合。不阻断理由：①该回归方向不属 F-1 登记的风险机理（旋钮/rank 替换），需刻意改键方触发；②已超出审查修复建议面（「靶法可复用上述错峰三页」同几何）；③四突变方向中三向咬合＋载体两形态有效。留痕供 MB10 分页改造时顺带收紧。
- **O-4（复现环境注）**：本会话首轮全量跑 1 failed＝`test_settings_module_importable[production]`，系 shell 未导出 `ALLOWED_HOSTS`（production.py:10 `env_required` 快速失败）——按 `.env.example` 五变量补齐后全绿。非回归，登记供后续会话省一轮排查。

### 结论

**VERDICT = PASS——F-1（P3）闭合确认**。修复面恰为审查建议的一条测试（超额交付：三探针全落地＋跨类型合并锚＋双通道 subtests），零实现改动；锚独立性经四向突变＋两层机制探针独立亲证；修复者风险①裁量与自报回归数字全部复核成立。A3.3 实现链（设计→设计审查→实现→实现审查→修复→本复审）收口。
