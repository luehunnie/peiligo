# A3.2 实现独立审查报告（M3.3 状态机/修订预览/CURRENT_DEFAULT/删除政策）

- **步骤**：A3.2-IR · M3.3 实现独立审查（实现者分支 `arch/a3.2-state-machine-impl`＝impl `ff82790`＋自检报告 `ce6ac8d`，基线 `rebuild/v1` @ `d1adb7b`）
- **审查对象**：13 文件（六改＋七新，+1294/−16）＋自检报告 `docs/reviews/A3.2.impl.md`（+84）——`git show --stat` 亲证与声称面一致
- **审查会话**：Orca `arch/a3.2-impl-review`（全新会话，不继承实现者上下文；不信自述、全部验收自跑；不改实现、不 push）
- **审查日期**：2026-08-21
- **VERDICT**：**FIX**（1×P2 / 0×P3 / 3×P4）——F-1（PS-29/§17.4 非空结构页拒删约束在后台**批量删除**动线被绕过，已运行时实证非空板块整子树硬删）；其余九项必查全过、回归八项独立复跑全绿、零越界零文档改动。

## 0. 审查环境与边界

worktree 守卫：`git rev-parse --git-common-dir` → `<LOCAL_PROJECT_PATH>_restart/.git` 归属主仓 ✓；到岗分支已为 `arch/a3.2-impl-review` @ `d1adb7b`（clean）；`git merge --no-ff arch/a3.2-state-machine-impl` 零冲突 → `d73e1e5`（改动面 14 文件与两提交合计一致，兼验合并干净）。自建 venv `/tmp/a32ir/venv`（Python 3.13.11，requirements-dev.txt 钉版安装＋`-e .`，`peiligo.__file__` 亲证指本 worktree `…/A3.2-review/src/peiligo/__init__.py`；wagtail 7.4.2 / Django 5.2.17）。**Wagtail 机制事实全部在本 venv 已安装包内亲读**（非复用任何前会话解包物）：`models/draft_state.py`、`actions/publish_revision.py`、`actions/unpublish.py`、`management/commands/publish_scheduled.py`、`admin/views/pages/delete.py`、`admin/views/pages/bulk_actions/delete.py`、`admin/views/bulk_action/base_bulk_action.py`、`admin/views/generic/models.py`、`models/pages.py`、`admin/views/pages/create.py`。

**PG 禁区纪律（审前/审后双快照）**：peiligo_dev（peiligo_dev_user/8246975B/3表）、peiligo_test（peiligo_test_user/8328895B/3表）、postgres（<USER>/7968447B/0表）owner/size/表数逐项零变化，pg_dump 全文快照经 `\restrict` 随机令牌归一后 **sha256 四库全部 IDENTICAL**（含 peiligo_restart_dev 13940415B/74表——migrate 为 no-op 零写入实证）；peiligo_restart_test 系 pytest-django 自建自清（跑后 `pg_database` 零命中）。全程零 DROP、零写禁区库。审查用临时实证脚本 `tests/test_zz_independent_review.py` 跑后已删、未入提交（A3.1.review 同做法）。

## 1. 九项必查逐项

### 1.1 diff 逐段亲读＋声称面核对——PASS

`ff82790`＝六改（`departments/wagtail_hooks.py`＋31 / `guides/models.py`＋9 / `home/models.py`＋19 / `notices/models.py`＋22 / `resources/models.py`＋16 / `tests/helpers.py`＋127）＋七新（`notices/lifecycle.py` 99 行＋六测试文件 204/250/98/151/193/91）；`ce6ac8d`＝仅 `docs/reviews/A3.2.impl.md`。与声称面（lifecycle 新增＋五页接线＋SectionPage/FeaturedItem 消费＋before_delete_page 钩子＋六测试）逐项吻合；`tests/helpers.py` ＋127 为纯测试数据构造器（五类页 ORM 构造＋future/past＋`_save` 发布/预约路径），零生产面，自检报告"六文件改"口径已含。**无私加面**。NoticePage 原内联 CM-01 检查改走 `clean_publish_window(…, EXPIRE_REQUIRED)`，错误文案逐字节保留（"通知必须填写有效期（CONTENT_MODEL §5.2/CM-01）"）。

### 1.2 §14–§19 逐条映射（PS-10..29 / E1–E11 / 非法闭集 / S1 修订级口径）——PASS（PS-29 见 F-1）

- **S1 修订级口径与 wagtail 7.4.2 实际行为一致**：`lifecycle.py` S1 判 `scheduled_revision`（本 venv 亲证 `draft_state.py` L175-177＝`revisions.filter(approved_go_live_at__isnull=False).first()`，官方 `approved_schedule`/`status_string` 同一谓词；`cached_property` 实现者测试纪律注记属实）。残留三序列闭合亲证：`publish_revision.py` 调度分支读**修订内容** `go_live_at`（`revision.as_object()` 重建实例）、全程不清对象级列、`object.expired = False` 在 if/else 两分支共行（E11-S4 留档观察的源码事实独立坐实）；`unpublish.py` `_unpublish_object` 仅 `if object.live:` 生效＋仅清修订级；`publish_scheduled.py` 到期工作集 `filter(live=True, expire_at__lt=now)`（E7 仅 live 页）、发布工作集 `Revision.objects.filter(approved_go_live_at__lt=now)`（与 S1 同数据源）。
- **PS-10..29 逐条**：对照自检 §3 映射表逐断言落到测试——PS-10/21（LegalEdgeTests 11 边＋IllegalEdgeTests 六反例，§14.3 五组显式反例**无缺项**：draft→expired/unpublished、scheduled→expired/unpublished、expired→unpublished、any→draft、live→scheduled 各有专测）；PS-11（修订 user/created_at、live_revision 标记、回退另存不覆写 v2 快照）；PS-12（设计层冻结＋M4.3 登记，零代码零测试＝符合 §17.2/§19.2 登记）；PS-13（CASCADE 归零＋四条件逐一破坏）；PS-14（first_published_at 跨改版/下线/重发布持久）；PS-15（三残留序列＋仅存草稿填预约判 S0/S4＋路由 404＋不入列表）；PS-16（e3 到点转 live＋未到点不转＋残留 IsNotNull 断言仍 S2）；PS-17（draft/下线起回落＋**E11 重预约路径**＋内建 unschedule 后台视图实测——A3.2-D 复审建议已兑现）；PS-18（draft/scheduled 携过期 expire_at 任务执行不变＋set_expired 区别手动）；PS-19（unpublished 404＋列表不入＋sitemap）；PS-20（Notice 必填/必未来、Article 可选填则未来、E9/E10/E11 携新有效期）；PS-22（四条件＋数量/权限归 IA §7.2/M4.3 登记）；PS-23（三类强制空填值拒/留空过）；PS-24（记录/修订/slug/URL 保留＋E9 恢复 200）；PS-25（路由/列表/sitemap 三消费位）；PS-26（M5.2 登记，代码零归档声称，见 1.6）；PS-27（ProtectedError 双词表＋无引用放行＋is_active 退场）；PS-28（through 归零、页零影响）；PS-29（单页动线全过，**批量动线缺口＝F-1**）。
- **E1–E11 逐边**：e1/e2/e3×2/e4×4/e7/e8/e9/e10/e11＋E5/E6（revisions）全覆盖；非法闭集经 wagtail 结构性保证（unpublish 仅 live 生效、到期工作集 live=True）非纸面约定。

### 1.3 零新字段亲证——PASS

`makemigrations --check --dry-run` → **No changes detected**（exit 0，本 venv 亲跑）；`LifecycleStateMixin` 声明层零 `models.Field`（测试 `test_mixin_declares_no_model_fields` 亦有断言）；migrate no-op。§14.1 兑现。

### 1.4 CURRENT_DEFAULT 消费真实生效（路由与列表双口径实测）——PASS

独立实证脚本（非复用实现者测试）全过：四态路由 404／live 200；板块列表 HTML 仅含 live 标题；sitemap.xml 同口径；预约页到点（mock 时间＋`publish_scheduled`）路由翻 200 且入列表。`FeaturedItem.is_on_display` 四条件逐一破坏均 False（含所指页到期退出 CURRENT_DEFAULT 的第四条件半边）。**漏网入口排查**：全仓前台 Page 查询仅四处——SectionPage 列表（已接 `current_default_pages()`）、HomePage 入口网格（`get_children().live().type(SectionPage)` 仅结构页，结构页 live 恒 T 不属内容消费位）、search（`Page.objects.live().search()`，见 P4-3）、wagtail 内建路由/sitemap（live-only 与 CURRENT_DEFAULT 在可达状态上等价：expired ⇒ live=F，live∧expired 不可达）。无第四内容消费位漏网。

### 1.5 删除政策实测——单页动线 PASS；批量动线 FAIL（F-1）

非空容器/板块/首页后台单页删除动线 POST 实测拒删（302＋页存续）、空容器可删、Department/MaterialType 被引用 ProtectedError＋无引用放行＋`is_active` 退场、删 Tag 仅 through 级联页零影响、FeaturedItem CASCADE 归零——均独立复测通过。**但后台列表勾选＋Delete 批量动线绕过钩子整子树硬删**（F-1，§2 证据链）。

### 1.6 零越界——PASS

CONTENT_MODEL §1–§19 `git diff d1adb7b..impl -- docs/CONTENT_MODEL.md`＝**0 行**（逐字节零改动；docs/ 仅新增自检报告 A3.2.impl.md）；templates/ 与 static/ 零触碰（M2 视觉合同＝未触模板即无违例）；零 EventPage（全仓 5 处命中皆系既有 `tests/test_event_fields.py` 负向守护测试，diff 零引入）；零权限组/Permission 配置（M4）、零 cron/celery/任务部署代码（M5.1；测试内 `call_command("publish_scheduled")` 系内建命令调用，§16.3 明文允许）、零 `search_fields` 增改（M3.4；`departments/models.py:188` 系基线既有）；HISTORICAL/归档零实现零声称（`lifecycle.py`/`home/models.py` 注释均为 M5.2 边界指针，PS-26 兑现）。

### 1.7 全量回归独立复跑——PASS（全绿，数字与自检逐项吻合）

| 项 | 命令（本 venv 亲跑） | 结果 |
| --- | --- | --- |
| check | `manage.py check` | 10 issues（0 silenced）＝10×treebeard.E001，exit 0 |
| 迁移零漂移 | `makemigrations --check --dry-run` | No changes detected |
| migrate | `manage.py migrate` | No migrations to apply |
| 测试 | `pytest -q` | **249 passed + 112 subtests**（与自检逐字一致） |
| lint | `ruff check .` | All checks passed |
| 格式 | `ruff format --check .` | 101 files already formatted（自检记 100，计数口径差异见 P4-4，双绿） |
| §4.5 | `grep -c "v1\.2" docs/CONTENT_MODEL.md` | 0 |
| 行数 | `wc -l` | lifecycle 99；测试最大 250（≤250 达标） |

### 1.8 测试质量抽读——PASS

六文件 987 行逐行读毕：断言均为实义（状态＋底层列＋真实 HTTP 响应/HTML/SQL 计数，无空转测试）；时间机制（mock `django.utils.timezone.now` 包 `call_command`、queryset update 回拨绕 clean）留有注释依据；`refresh_from_db`/`scheduled_revision` cached_property 两处踩坑纪律以注释留痕且测试确实执行新实例取值；E11 重预约路径（PS-17 建议）专测坐实 S4 非 S3 且定性"留档观察不阻断"与 A3.2-D 口径一致。

### 1.9 M2 视觉合同——PASS（N/A）

未触任何模板/CSS/静态资源（diff 零文件），无新增视觉面即无违例。

## 2. F-1（P2）证据链：PS-29 非空结构页拒删在批量删除动线被绕过

- **冻结约束**：§17.4"DepartmentContainerPage 非空（其下有任何内容页）时**拒绝删除**……约束在此冻结，实现（clean/hook 拦截）B 阶段留痕"；PS-29"非空容器/板块页删除被拒"（无限定语）；§17.2 硬删不可恢复。
- **实现现状**：钩子仅挂 `before_delete_page`（`departments/wagtail_hooks.py` L163-184），全 wagtail 7.4.2 该钩子**唯一消费点**＝单页删除视图 `admin/views/pages/delete.py` L24（返回响应即取消，亲证）。
- **绕过路径（本 venv 源码亲证）**：后台列表批量删除 `DeleteBulkAction`（wagtail 默认注册：`admin/wagtail_hooks.py` L49/L1184；URL `/admin/bulk/wagtailcore/page/delete/`）`execute_action` 直调 `page.delete(user=user)`（`admin/views/pages/bulk_actions/delete.py`）——**不经 `before_delete_page`**；全仓无任何 unregister/禁用（grep 亲证）。项目侧亦无第二道闸（`clean` 不在删除链上；M4 权限组未配置，当前唯一操作者即超级管理员，恰是钩子要防的误操作主体）。
- **运行时实证（临时脚本，跑后已删）**：超级管理员 `POST /admin/bulk/wagtailcore/page/delete/?id=<非空 SectionPage pk>`（子树 2 页＜`WAGTAILADMIN_UNSAFE_PAGE_DELETION_LIMIT`=10，**无 type-to-confirm**）→ 302，板块/容器/内容页三行 **Page.objects 全部不存在**——冻结约束"删除被拒"在产品内路径上实际不成立。对照同会话同数据单页动线拒删（302＋页存续）成立。
- **为何 P2 非 P3**：数据丢失级（treebeard 子树硬删＋修订连带，§17.2 产品内不可恢复）；约束为 §17.4 冻结项且 PS-29 系 B 阶段断言，实现者自检报告 §1.2/§3 声称 PS-29"落地/后台删除动线 POST 实测"，但只测了单页动线、未披露批量动线缺口（自检 §4.5"直接 ORM delete() 属产品外路径"的辩护不覆盖批量动线——它是后台 UI 一等公民路径）；当前超级管理员单一操作者属缓解而非豁免（§17.4 的"防误删"语义本就包含管理员误操作，且 M4 落地后总管理员仍持有该动线）。
- **修复建议（供修复批次，本审查未改实现）**：wagtail 提供 `before_bulk_action` 钩子（`admin/views/bulk_action/base_bulk_action.py` L83-87/L147-153：返回响应即在事务内取消整个动作）——在 `departments/wagtail_hooks.py` 增同名谓词拦截 `action_type=="delete"` 且对象含非空结构页；配一条镜像本实证的测试（批量动线拒删＋空容器批量放行）。亦可顺带评估同文件既有 move/copy 钩子的批量对应面（本审查未查 move/copy 批量路径，非本批断言范围）。

## 3. P 级留档（不阻断）

- **P4-1（措辞，沿 A3.2-D P4-1）**：§17.2/§18 相关表述与 02 CM 映射措辞问题已在设计审查留档，实现无新引入。
- **P4-2（测试建议）**：PS-20 的发布路径负向（E9/E10/E11 携过期 expire_at 经 `save_revision()`→`full_clean()` 抛 ValidationError）目前由"full_clean 直测（pw-n-past/pw-a-past）＋save_revision clean=True 缺省（`models/pages.py` L951/L968-969 亲证）"结构性地保证，未有一条穿过真实 publish 动作的负向测试；B 阶段可补一条（一行断言即可闭合）。
- **P4-3（指针）**：`search/views.py` L19 直用 `Page.objects.live().search()` 而非 `current_default_pages()`——可达状态上谓词等价（expired ⇒ live=F；live∧expired 不可达），今日无缺陷；M3.4/M5.2 触碰搜索时按 §16.5"不得另立可见性语义"显式改挂 §14.2/§16.4 锚点。
- **P4-4（口径）**：自检报告 ruff format 记"100 files"、本审查同命令得"101 files"（双双绿；本仓 `.py` 实际 68 文件，计数含 src 根重复发现，ruff 版本/环境差一）——数字口径差异留档，无实质影响。
- **E11-S4 观察**：A3.2-D 留档的"预约前原态"措辞对该路径不严格一节，实现以专测坐实真实行为（S4）且注释引 publish_revision.py L135 语义——本审查独立重证该行确系两分支共行，"仅观察不阻断"定性维持，B 阶段 PS-17 复核建议继续有效。

## 4. 结论

九项必查八项 PASS、一项（1.5 删除政策）半边 FAIL；F-1（P2）为唯一须修项：把 §17.4/PS-29 的拒删约束从"单页动线"补齐到"批量删除动线"（`before_bulk_action` 钩子＋镜像测试）。修复面预计仅 `departments/wagtail_hooks.py`＋一个测试文件，不触模型/迁移/文档。其余实现（五状态纯推导、三政策单闸口、CURRENT_DEFAULT 三消费位、残留三序列、E1–E11 全边＋非法闭集、PROTECT/CASCADE/through 三路删除政策）与 §14–§19 逐条吻合，回归八项独立复跑全绿（249+112），PG 禁区四库快照零变化。

## 5. 审查方法与留痕

静态逐段（14 文件 diff 全读＋六测试 987 行逐行）＋wagtail 7.4.2 已安装包十处源码亲读＋动态独立复跑（§1.7 全表）＋独立实证脚本（C4 双口径/F-1 批量绕过/PROTECT，5 passed + 5 subtests 后已删）＋PG 审前审后双快照 sha 对比。未改任何实现文件；单提交仅新增本报告；未 push。

---

# 修复复审附记（A3.2-IR2 · F-1 闭合单点复审，2026-08-21）

- **对象**：`arch/a3.2-impl-fix`（fix `c320fb5`＋自检 `d768465`），基线＝本报告 `d430f90`
- **会话**：Orca `arch/a3.2-impl-rereview`（全新会话独立亲证；自建 venv `/tmp/a32ir2`（Python 3.13.11，requirements-dev 钉版＋`-e .`），`peiligo.__file__` 亲证指本 worktree；wagtail 7.4.2 / Django 5.2.17；不复用修复者环境与脚本、不信自述）
- **VERDICT**：**PASS**——F-1 闭合实证，无新须修项；批量 move/copy 留档面确认仅观察不阻断（R-1.6）

## R-1 必查六项逐项

1. **diff 逐段亲读——声称面吻合、无私加**：`git diff d430f90..c320fb5 --numstat`＝`departments/wagtail_hooks.py` **+53/−16**＋`tests/test_bulk_deletion_policy.py` **+112（新）** 两文件，与自检 F-4 逐字一致；`docs/`、`templates/`、`static/`、migrations 零触碰（含 `d768465` 全程 `docs/CONTENT_MODEL.md` 0 行）。全 diff 亲读：改动仅模块 docstring 更新、谓词提取、统一拒删响应、单页钩子改调共享谓词、新 `before_bulk_action` 钩子＋七例镜像测试；拒删文案与重定向目标与原单页钩子逐字节一致（原两段隐式拼接＝现单 f-string，终串相同）。
2. **拦截逻辑亲证——谓词真单点、仅拦 delete、事务内整动作取消**：`_is_nonempty_structure_page`（hooks L166-175）为全仓唯一拒删判定，`before_delete_page`（L196）与 `before_bulk_action`（L219）同点调用——结构性共用非复制漂移。本 venv wagtail 7.4.2 源码亲证：钩子签名 `(request, action_type, objects, bulk_action)`（`base_bulk_action.py` `__run_before_hooks`）；`form_valid` 在 `transaction.atomic` 内先跑钩子，带 `status_code` 的返回先于 `execute_action`＝整动作取消、同批零部分删除；`DeleteBulkAction.action_type="delete"`、`execute_action` 直调 `page.delete()`（不经 `before_delete_page`，F-1 机制再坐实）；页面批量动作全集＝delete/move/unpublish/publish（7.4.2 无页面批量 copy，rename 非批量动作）——非 delete 一律 L213 早退放行，非 Page 对象（词表/媒体批量删）经 `isinstance(obj, Page)` 守卫跳过。
3. **运行时实测（独立临时脚本 8 例，跑后已删未入提交）——全过**：①非空板块批量删拒＝302＋板块/容器/通知 Page＋specific＋Revision 全存续（零残留）＋拒因消息＋回父级 explore；②≥10 口径＝11 页子树 GET 确认页含 `confirm_site_name` 字段、携正确站点名 POST 仍拦（10 后代原样），错名被 wagtail 自身确认闸挡（200 重渲染零删除）；③空容器批量删放行；④两内容叶批量删不受扰、批外容器零波及；⑤单页动线回归仍拒；⑥词表 Department 真表单批量删照常（isinstance 守卫真流程实证）；⑦真表单批量 publish 两草稿照常发布（非 delete 动作真流程实证）；⑧红证（下条）。<10 口径（无确认直 POST 被拦）含于①④。
4. **红绿复核（独立手法）——测试确测钩子非偶然绿**：不动源码，注册表层（`hooks._hooks` 条目替换为空转函数，单页钩子不受扰）绕过批量钩子——修复者七例恰三拒例红（重定向落批量动作自身 next URL＝删除照走的原缺陷形态）、其余四例绿（含单页回归，证绕过面精确）；专用红证例同期实证：绕过期非空板块 2 页子树 POST **整子树硬删**（F-1 原缺陷路径复活），复原注册条目后同会话同路径恢复拒删。与修复者"临时注销注册＋复绿"手法互异、结论互证。
5. **全量回归独立复跑——八项与自检逐字吻合**：`pytest -q`＝**256 passed + 112 subtests**（基线 249+112＋新 7，零回归；初跑 1 failed 系本会话漏设 `ALLOWED_HOSTS` 的环境项，补齐后全绿）；`check`＝10 issues（0 silenced）＝10×treebeard.E001；`makemigrations --check --dry-run`＝No changes detected；`migrate`（dev 库）＝No migrations to apply；`ruff check .` All checks passed＋`ruff format --check .`＝103 files already formatted；§4.5 `grep -c "v1\.2" docs/CONTENT_MODEL.md`＝0；行数 hooks 221／测试 112 均 ≤250。**PG 纪律**：审前审后快照 peiligo_dev（8246975B/3表）/peiligo_test（8328895B/3表）/peiligo_restart_dev（13940415B）owner/size/表数逐项零变化；peiligo_restart_test 系 pytest-django 自建自清、跑后无痕；全程零 DROP、零写禁区库。
6. **留档面确认——批量 move/copy 仅观察不阻断（定性维持＋源码锐化）**：修复者 F-5 明示"未查未动、留 M3.4/后续批次评估"，无覆盖声称，不阻断本 VERDICT。本复审顺带源码级锐化供后续：`MoveBulkAction.execute_action` 直调 `page.move()`，`before_move_page` 唯一消费点仍仅单页 `move.py:83`——批量 move 同构绕过项目 §4 部门撞车＋§7.2 子树复检两钩子；但父级类型合法性经各页型 `parent_page_types`×`allowed_parent_page_models()`/`can_move_to` 结构性把守，且 move 非破坏性（无数据丢失，异于 F-1 硬删）、7.4.2 无页面批量 copy——"仅观察留档"定性恰当，建议后续批次评估在 `before_bulk_action` 增 move 分支（与本次 delete 分支同谓词单点口径）。

## R-2 结论

F-1 闭合成立：§17.4/PS-29 拒删约束在单页与批量两条删除动线均运行时实证生效（两 confirm 口径含），镜像七例经独立绕过证明确测钩子，全量回归零回归，PG 禁区零扰动。无新发现须修项。本复审零实现改动；临时实证脚本与绕过插件（居 /tmp）跑后已删/未入库；单提交仅新增本附记；未 push。
