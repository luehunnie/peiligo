# A3.2 独立设计审查报告（M3.3 状态机/修订预览锁/预约到期映射/删除政策）

- **步骤**：A3.2 · M3.3 状态机、修订、预览与删除政策（设计者分支 `arch/a3.2-state-machine-design` @ `bb0e86a`，单提交，基于 `rebuild/v1` @ `c89acd6`）
- **审查对象**：`docs/CONTENT_MODEL.md` §14–§19（+231/−3，纯设计文档——`git show --stat` 亲证仅此一文件，零代码/迁移/测试/视觉改动；−3 行经逐行核对＝文档信息表三行元数据，非 §1–§13 正文）
- **审查会话**：Orca `arch/a3.2-design-review`（全新会话，不继承设计者上下文；只审查文档与只读取证，不改设计、不写代码、不 push）
- **审查日期**：2026-08-21
- **VERDICT**：**FIX**（1×P2 / 0×P3 / 3×P4）——F-1（§14.2 S1 推导条件过宽，可达序列将 S3/S4 误判为 S1，并传导 §16.4 HISTORICAL 口径）须于 B 阶段实现前修正本文；其余零冲突、零越界、验收全绿。

## 0. 审查环境与边界

纯文档审查：不建 venv、不触数据库、不跑项目代码（对照事实只读 `requirements.txt`、五个 `models.py`、`tests/` docstring）。Wagtail 机制事实**不依赖审计转述**，另行亲证：`pip3 download wagtail==7.4.2 --no-deps` 至 `/tmp/wagtail-check`（wheel 解包只读，未安装、不入库），对 `wagtail/models/draft_state.py`、`wagtail/actions/unpublish.py`、`wagtail/actions/publish_revision.py`、`wagtail/management/commands/publish_scheduled.py` 逐段亲读；`docs/reference/management_commands.md` 不随 wheel 分发，经 GitHub `v7.4.2` tag 原文核验。worktree 守卫：`git rev-parse --git-common-dir` → `<LOCAL_PROJECT_PATH>_restart/.git` 归属主仓 ✓；到岗首查分支已为 `arch/a3.2-design-review`（规范化完成）；基线归属 `git merge-base --is-ancestor rebuild/v1(c89acd6) bb0e86a` ✓；`git merge --no-ff arch/a3.2-state-machine-design` 零冲突（改动面 1 file +231/−3 与设计提交一致，兼验合并干净）。引源纪律：00 M3.3 节、02 §5 S3.3/S5.1/S5.2、PRD §5/§7/§8/§9/§11/§21、CONTENT_MODEL §1–§19 全文、ADR-0001..0005 决策节、上游审计 §3.3/R4、IA §7.2 均只读所指范围。未进旧仓 `vscode_projects/peiligo`；§4.5 禁区零触碰。

## 1. 十项必查逐项

### 1.1 §14–§19 与 §1–§13 零冲突（含 −3 行定性）——PASS（附 P4-2）

- **正文逐字核验**：`git show c89acd6:docs/CONTENT_MODEL.md` L15–409 与 `bb0e86a` 版同区间 `diff` **逐字节相同**（BODY-IDENTICAL）；文件 411→639 行＝411−3+231 恰闭合。−3/+3 行＝文件头"文档信息"表三行（日期/本步范围/分支），系批次增补后的文档自述更新（旧文"本文件当前不含 §14–§19 章节"已变假话，不改即自相矛盾），无任何内容模型语义。
- **终判不翻案逐项**：§16.1 保留 §5.3 PublishingPanel 位点（口径不变，值层拦截）；§15.4 保留 §13.3 FeaturedItem 起止字段与三要素；§17.3 保留 §4/§13.1 PROTECT、§13.3 CASCADE 原样引证并代码复核（`guides/models.py:52`、`resources/models.py:91-101`、`departments/models.py:180`、notices through 模型 tag FK CASCADE——"现状代码实证"属实）；PS-14 与 §5.1 `first_published_at`"落定并持久"零冲突（且经 publish_revision.py:135/144-147 亲证 Wagtail 结构性满足：仅 None 时落定）；§16.2 未来性校验是 §5.2"非空"的**加严延伸**而非翻案（到期行为本就 DEFERRED_TO_M3_3）；§16.1 Material/Software/Guide `expire_at` 强制空与 §8.1"无有效期字段"相容（官方面板位点 §8.2"同 §5.3 口径"本就含位点，拦截的恰是"无有效期语义"的落值）。
- **P4-2（程序性）**：00 M3.3/02 S3.3 允许修改范围字面为"该文件 §14–§19"，元数据三行在字面范围外。属必要且无语义影响的文档自洁，建议总控在 M3.4 批令口径中追认"文档信息表随批次更新"为惯例。

### 1.2 PRD §7/§8 覆盖完整（状态机/到期/删除/修订预览全有落点）——PASS

| PRD 条款 | 本文落位 | 判定 |
| --- | --- | --- |
| §5 修订句（逐字）"Wagtail 自带的基础修订历史，记录操作者和时间" | §15.1 引用与原文逐字一致；V1 不自建修订/审计模型 | ✓ |
| §7.1 预约发布 | E2/E3＋§16.1（PS-15/16） | ✓ |
| §7.1 到期退出首页/默认列表 | E7＋PS-25（CURRENT_DEFAULT） | ✓ |
| §7.1 保留在历史归档及站内搜索 | §16.4 R4 两层处置：数据层即时成立（PS-24）＋查询层 M5.2 契约（PS-26"禁止假装已解决"） | ✓ |
| §7.2 预约发布/不默认设下线时间 | E2＋§16.1 ArticlePage 可选默认空 | ✓ |
| §7.3 活动状态三态 | §14.4 正交声明（PRD 无"活动结束即下线"句，亲核 PRD 原文属实） | ✓ |
| §8 无审批发布 | E1 执行者列 | ✓ |
| §8 默认下线/归档不删除 | §17.1 默认路径＝E8/E7，零删除 | ✓ |
| §8 通知到期自动归档；文章和资源手动下线 | E7＋E8（附 P4-3） | ✓ |
| §8 永久删除仅总管理员＋三情形（逐字） | §17.2 三情形原文照录"不得扩围" | ✓ |
| §11 末段投诉立即隐藏（逐字） | §17.2 纠错快速通道（unpublish＋内建留痕＋E10/删除分流） | ✓ |
| §21"通知有效期、自动归档和排序符合规则" | §19.1 引源行登记，验收落点 M5.1/M5.2 | ✓ |

02 S3.3 规格行（§14 转换表五组边＋非法列举、§15 mixin 取舍结论、§16 一览表三口径、§17、§18 断言、§19 可追溯性）逐项有落位，无缺项。

### 1.3 五状态由内建列纯推导、零新字段——PASS（载体亲证）；但推导条件本身见 F-1

- **内建列亲证**（wagtail 7.4.2 wheel）：`draft_state.py` L14 `live`、L15 `has_unpublished_changes`、L20-22 `first_published_at`、L24-26 `last_published_at`、L36 `go_live_at`、L38 `expire_at`、L41 `expired`——§14.2 推导的全部输入恰为 Page 内建列，与"零新增字段"声明一致。`requirements.txt` L35 `wagtail==7.4.2` 冻结属实，与 ADR-0001 版本组合相容。
- **代码亲证**：五个 `models.py` 零状态/生命周期字段（grep 全量，仅内建用法如 `home/models.py:73` `.live()`）；本批零代码改动（diff 仅 1 文件）。
- **与 §7.1 同款"纯计算不落库"口径**：§14.2 自述与 §7.1 `event_status` property 先例一致，B 阶段落实为只读 property——口径统一。
- 推导规则的**载体层**无误，但 S1 的条件式与语义括注不符（F-1，P2）：见 §2 证据链。

### 1.4 E1–E11 迁移表完备、每边有 PS 断言、非法转换闭集——PASS（结构）；F-1 属推导而非边表

- 11 条边逐行核验：E1→PS-14、E2→PS-15、E3→PS-16、E4→PS-17、E5/E6→PS-11、E7→PS-18、E8→PS-19、E9/E10/E11→PS-20，**每边恰有断言**（`sed -n '437,449p'` 计 11 行含 PS）。
- 出边完备性按状态盘点：S0{E1,E2}、S1{E3,E4}、S2{E7,E8}（E5/E6 为子态非状态转换，已显式声明正交）、S3{E9,E11}、S4{E10,E11}——无缺失合法出边；非法闭集 PS-21 全称断言＋五组显式反例。
- **非法边经 Wagtail 源码结构性亲证**：`unpublish.py:53` "Does nothing if live is already False"——draft/scheduled 页的 `expire_at` 到点不产生任何动作（`publish_scheduled.py:46` 到期查询集 `filter(live=True, …)`），"仅对 live 页生效"（E7）与"scheduled→expired/unpublished 非法"均由官方实现保证，非纸面约定。
- `live → scheduled` 非法的声明亦与源码一致：`publish_revision.py:120-125`，对已有 live 版本的页，未来 `go_live_at` 仅登记待发布修订并**提前 return（live 不变）**——live 态设预约不构成状态转换，"官方面板位点保留"的处置准确。

### 1.5 删除政策偏离 02 CM-13"阻止删除默认"——论证成立，无需升级（附 P4-1 措辞）

- **授权依据亲核**：02 S3.3 L460 原文"CM-13 推荐位引用被删内容时的行为规则（**本步骤定**，默认：阻止删除并提示）"——"本步骤定"即把终判权交给本步骤，"默认"仅为未论证时的缺省。偏离有明文授权，非擅翻。
- **不翻 M3.2 冻结亲证**：§13.3 `content` FK `on_delete=CASCADE`＋"推荐位随所指内容删除而失效（运营位无独立保留价值）"为 M3.2 已冻结并经 A3.1 实现落地（代码实证在库）；采纳"阻止"须改冻结字段（PROTECT/拦截层），本批禁改 §1–§13——设计者论证①属实。
- **论证②③核验**：②违法内容快速净除（PRD §8 三情形之一）动线不被运营位阻塞——产品推理成立；③CASCADE 后零悬挂引用，CM-13 草案所虑"引用完整性缺口"不存在——属实。"删除前影响面提示"（§17.2）承接 02 §17 规格行"删除前需确认无关联推荐位引用"的确认语义（确认知情而非阻断），操作者保护未弱化。
- **定级**：文档级、有授权、不触冻结面——按 00 §4.6 风险矩阵属 R0/R1，无需 Human 裁决或升级；本审查确认该偏离为正当终判。
- **P4-1（措辞）**：§18 编号口径"CM-10–CM-13 逐条映射…**内容一字不丢**"对 CM-13 不确——CM-10/11/12 内容确逐字保留（PS-12 且为超集），CM-13 草案缺省（"阻止删除并提示"）已被终判**改写**（ CASCADE 同删＋影响面提示）。改写正当但应表述为"CM-13 按本步骤终判改写，草案缺省不采纳并留痕"，防后续读者误以为草案语义仍在。

### 1.6 PS 编号与 02 S5.1 预留 PS-01–05 消歧——PASS

全库扫描亲证：`PS-0x/1x/2x` 仅出现于 02 S5.1（PS-01–05，属 `PUBLISH_ARCHIVE_SCHEDULING.md`，M5.1 未启动、文档尚不存在）、02 L825 交叉引用（PS-05 同 S5.1）、本文 PS-10–29。数值零重叠，PS-06–09 留缓冲；00 M3.3"断言 PS-xx 系列"的指定得到满足；跨文档同名前缀问题在 §19.3 显式登记并给 M5.1 两个消歧选项（改号 PAS-xx/文档前缀），处置稳妥。CM/PS 断言族无编号冲突（CM-00–08 与 PS-10–29 无交集，CM-09 空号无占用）。

### 1.7 Expired/历史查询 deferred M5.2、CURRENT_DEFAULT/数据保留语义——PASS（F-1 有传导注记）

§16.4 三口径表：CURRENT_DEFAULT=V1 即生效、HISTORICAL/ARCHIVE-SEARCH=M5.2，批次归属逐行明确；R4 张力两层拆解（数据保留层即时为真/查询可见层缺口登记不关闭）语义无歧义，与上游审计 R4 原文（L165"漏做会直接违反 §7.1 验收"）对齐；unpublished 恒不入三口径与 02 S5.2 §7 总表同口径。§16.5 给 M3.4 的三点锚点仅语义定义、零字段映射。**注**：HISTORICAL 以 §14.2 推导状态 S3 定义，而 S3 推导受 F-1 影响存在误判序列——修 F-1 后该传导自然消除（详见 §2.c）。

### 1.8 与 ADR-0001..0005 零冲突——PASS

0001 版本（7.4.x LTS 引用属实）；0002 服务端模板（零触碰）；0003 干净骨架（零触碰）；0004 决策 3"部门账号不持有任何 Snippet 权限"与 §15.4"仅总管理员即时运营"、§17.2 执行权一致，容器非空拒删（§17.4）与容器树权限锚点语义相容；0005 #13 FeaturedItem"起止时间控制展示窗口"——§15.4"展示窗口≠生命周期"的终判与 ADR 原表述同构（ADR L17"可按需混入"是能力陈述非要求，不混入为正当取舍且系 02 S3.3 显式交办项）。

### 1.9 零越界——PASS

`git show --stat bb0e86a`：仅 `docs/CONTENT_MODEL.md`，零代码/迁移/测试/视觉。§20+ 章节计数 0（M3.4 未预写）；`EventPage` 在新增区零出现；无权限矩阵编制（PS-12 仅断言冻结＋M4 指针）；无 M5.1/M5.2 实现（仅指针与缺口登记）；无 `search_fields` 映射（§16.5 仅语义锚点，02 S3.4 认可"只留指针"）。分支名 `arch/a3.2-state-machine-design` 与 00 批令示例 `arch/m3.3-state-machine` 不同——系 A3.x 批次命名惯例（与 A3.1 同款先例），不作为越界。

### 1.10 验收命令亲跑——PASS

| 命令组 | 结果 |
| --- | --- |
| 00 M3.3：`grep -q 'go_live_at' && grep -q 'expire_at' && grep -q 'DraftStateMixin'` | 三连全命中 PASS |
| 02 S3.3 五条（draft/live/expired、go_live_at/expire_at、RevisionMixin\|修订、总管理员、publish_scheduled） | 全 PASS |
| §4.5 v1.2 秘密扫描（对 docs/ 全量正则） | 零命中 PASS |
| PS 断言齐全（PS-10..29 逐号 grep） | 20/20 无缺号 |
| 00 M3.3"每条边有断言"分级 A | E1–E11 计 11/11 |

## 2. Wagtail 7.4.2 亲证附录与 F-1 证据链

**已证事实**（全部出自 /tmp 解包 wheel 源码逐行亲读，行号为 7.4.2 实测）：

a. `publish_revision.py`：未来 `go_live_at` → 登记 `revision.approved_go_live_at`（L113-118）；**发布动作全程不清除 `object.go_live_at`**（全库 grep 无 `go_live_at = None` 于对象层；仅修订级 `approved_go_live_at` 被清，L134）。
b. `unpublish.py` `_unpublish_object`：置 `live=False`＋（`set_expired` 时）`expired=True` 于**同一次 save**（原子性，§14.2 注"LIVE=T∧EXPIRED=T 不出现"亲证成立）；同样**不清 `object.go_live_at`**（L78 仅清修订级 approved）。
c. **F-1 可达序列（全部常规 UI 动线，无任何 hack）**：
   - **序列①（主线性）**：通知 E2（预约周一）→ E3 `publish_scheduled` 到点发布（对象 `go_live_at`＝周一残留为过去值，亲证 a）→ 期间无人编辑（表单保存才会重写该列）→ E7 周五到期 `unpublish(set_expired=True)`（亲证 b 不清残留）→ 终态 `live=F ∧ expired=T ∧ go_live_at 非空`。按 §14.2 优先级（S2 失败后 **S1 先于 S3** 命中）推导为 **S1"预约发布中"**——真实语义 S3 已到期。此即"预约发布＋自动到期"通知的最寻常运营路径。
   - **序列②**：同上残留经 E8 手动下线 → `live=F ∧ expired=F ∧ go_live_at 非空` → S4 条件（要求 go_live_at 空）不满足 → 误判 **S1**（真实 S4）。
   - **序列③**：编辑者仅"保存草稿"并填写预约时间而未点发布（Wagtail 表单即持久化 `go_live_at`，但 `approved_go_live_at` 仅发布动作设置，L116）→ `live=F ∧ go_live_at 非空 ∧ 无待执行修订` → 按字面判 S1，但任务永不发布。
d. **影响定级**：V1 前台零影响（S1/S3/S4 均不入 CURRENT_DEFAULT，PS-25 不破）；数据零影响（PS-24 不破）；编辑触碰排程字段后自愈。**实害传导**：§16.4 HISTORICAL＝CURRENT∪{S3}、§16.5 锚点"M3.4/M5.2 一律以 §14.2 推导为准"——若下游按冻结口径实现，序列①之通知**漏出历史归档与过期标注**，正撞审计 R4"漏做即违反 §7.1 验收"；另后台状态标签失真。§14.2 括注（"未到点，或到点后任务窗口内待执行"）表达的才是正确语义，条件式是它的过近似。
e. **修复建议（一行条件＋一条注记，纯文档）**：S1 条件收紧为"live=F ∧ **存在 `approved_go_live_at` 待执行修订**"（或等价 guard：go_live_at 非空 ∧（go_live_at > now ∨ 尚无发布动作落地该值）），并注记"对象级 go_live_at 可残留过去值，不构成 scheduled 态依据；该列仅在编辑表单保存时重写"。收紧后 S1/S3/S4 判定均归正确，序列①②③全闭合，且不产生新歧义（真预约页必有待执行修订，亲证 L116）。

**附注（不作发现）**：设计引"审计 §3.3 引证 unpublish.py:56-57"——7.4.2 实测 L56-57 为 live 守卫行，`set_expired` 落在约 L58-59；系审计原文行号微偏，设计如实转引、语义无误。"每小时运行"经 GitHub v7.4.2 tag `docs/reference/management_commands.md` 原文亲证（"We recommend running this command once an hour"）。

## 3. 发现清单（1×P2 / 3×P4）

| 编号 | 级 | 位置 | 发现 | 处置建议 |
| --- | --- | --- | --- | --- |
| F-1 | **P2** | §14.2 S1 行（L425 附近）＋传导 §16.4/§16.5 | S1 推导条件"live=F ∧ go_live_at 非空"过宽：E3 发布不清对象级 go_live_at、unpublish 亦不清（wheel 源码亲证），"预约发布→自动到期/手动下线→期间无编辑"常规序列将 S3/S4 误判 S1；HISTORICAL 若按冻结谓词消费则过期通知漏出归档（PRD §7.1 验收风险）；"存草稿＋填预约未发布"亦按字面误判 S1 | B 阶段实现前修正本文：S1 条件改挂钩待执行修订（approved_go_live_at）或等价 guard，并注记对象级 go_live_at 残留语义（建议文见 §2.e）；PS-15/PS-16 断言文字随之同步 |
| P4-1 | P4 | §18 编号口径（L578） | "CM-10–CM-13 逐条映射…内容一字不丢"对 CM-13 不确：草案缺省"阻止删除并提示"已被终判改写（改写本身正当且有授权，见 §1.5） | 触碰本文时改为"CM-10–12 逐字保留；CM-13 草案缺省不采纳、按 §17.3 终判改写并留痕" |
| P4-2 | P4 | 文档信息表（L8-9 等） | 元数据三行编辑在 00/02"允许修改范围：该文件 §14–§19"字面之外；系必要自洁（旧文自述已失真），无语义影响 | 总控在 M3.4 批令注记"文档信息表随批次更新"为惯例，消解字面越界 |
| P4-3 | P4 | §14.3 E8 执行者（L446） | E8 授予部门对五类内容（含通知"先于到期手动下线"）的 unpublish，而 PRD §8 字面仅点名"文章和资源"；PRD 对通知下手沉默非禁止，推断合理但属外推 | M4.1 权限矩阵编制时显式确认"部门可下线本部门通知"这一外推并回写 PRD 溯源 |

无 P3。F-1 为唯一阻断项：属本批核心交付物（分级 A 状态推导表）的形式化缺陷，修复为纯文档一行级；不涉任何冻结面翻案、不涉代码。

## 4. 结论

十项必查中九项 PASS；第 3 项（五状态纯推导）载体层 PASS、条件层坐实 F-1（P2）。设计与 §1–§13 冻结面零冲突（正文逐字节相同、终判零翻案、−3 行仅元数据）；PRD §7/§8 覆盖完整；E1–E11 每边有断言且非法闭集经官方源码结构性亲证；CM-13 偏离有明文授权（"本步骤定"）且论证成立、无需升级；PS 编号消歧稳妥；M5.2 deferral 与 R4 处置语义无歧义；ADR 五项零冲突；零越界；验收命令（00/02 grep 组＋§4.5＋断言齐全）全部亲跑通过。**VERDICT=FIX**：修正 F-1（§14.2 S1 条件收紧＋注记，连带 PS-15/16 文字同步）后本设计即可放行 B 阶段实现；P4×3 不阻断、留后续批次处置。本审查未改设计文件、未写代码、未 push；仅产出本报告并单 commit。

## 5. 修复复审附记（F-1 单点复审，2026-08-21）

- **复审对象**：`arch/a3.2-design-fix` @ `5a3d820`（链 `bb0e86a` 设计 → `83e986d` 合并 → `ed92001` 本审查 FIX → `5a3d820` 修复）；复审分支 `arch/a3.2-design-rereview`（到岗 `git branch -m` 亲执）。
- **复审环境**：全新会话零继承，不采信修复者自述，全部事实独立重证。wagtail 7.4.2 源码经 `pip3 download wagtail==7.4.2 --no-deps` 至 `/tmp/wagtail-dr2` **自行下载解包**（非复用前审查会话的 `/tmp/wagtail-check`）。范围仅 F-1 修复闭合；未改实现、未 push。
- **VERDICT**：**PASS**——F-1 闭合，无新引入缺陷，原九项 PASS 未破，设计放行 B 阶段实现。

### 5.1 diff 逐段亲读（与声称一致）

`git diff ed92001..5a3d820` 亲证仅 `docs/CONTENT_MODEL.md` **+10/−8**，六处改动面：§14.2 前言（"内建列"→"Page/Revision 内建列"）、S1 行（改挂钩修订级 `approved_go_live_at`）、S3/S4/S0 三行（删"go_live_at 空"合取）、新增残留语义注记一段、E4 行（机制改"内建 unschedule 动作"）、PS-15/16 两行（断言文字同步）。**删合取的必要性独立确认**：S1 不再消费对象列后，若 S3/S4/S0 保留该合取，序列①②的残留态在五状态上全不命中——推导无解、自毁"总可判定"注；删后 live=F 页在 {待执行修订?, expired?, first_published_at?} 三问上先命中先得，**全判定且互斥**，优先级链闭合（"总可判定"自述保留成立）。

### 5.2 wagtail 7.4.2 独立亲证（引证逐条重跑，全部吻合）

| 引证（修复注记） | 独立亲证结果 |
| --- | --- |
| `publish_revision.py` L113-119：未来 `go_live_at` 仅登记修订级 | ✓ L116 `revision.approved_go_live_at = object.go_live_at` 落发布动作内、L119 清他修订——"登记仅随发布动作"成立 |
| L134：转 live 清**全部修订级** approved、全程不清对象级 | ✓ 且全库 grep 对象级 `go_live_at = None` **零命中**（E3/E7/E8 不清对象列坐实；该对象列仅编辑表单经 model-form 机制持久化，无任何 action 写点） |
| `unpublish.py` L78 仅清修订级 | ✓ L57 `object.live = False`、L78 `revisions.update(approved_go_live_at=None)`；L52 live 守卫（E7/E8 仅 live 生效）复核不变 |
| `publish_scheduled.py` L91-93 引擎按修订级取件 | ✓ `Revision.objects.filter(approved_go_live_at__lt=now())`——S1 判定与官方调度引擎**同一数据源** |
| `draft_state.py` L73-92/L175-177 官方谓词同源 | ✓ L74 `approved_schedule`、L78 `status_string`、L176-177 `scheduled_revision`＝`revisions.filter(approved_go_live_at__isnull=False).first()`——官方"scheduled"判定本就以修订级为准，新 S1 条件与其同源 |
| `revisions.py` L113 系内建列 | ✓ §14.1"零新增字段"声明不破 |
| `generic/models.py` L1729-1733（commit 引证）＝`admin/views/generic/models.py` unschedule `post()` | ✓ L1730-1731 仅清修订级、不触对象列——E4 新机制描述源码级准确 |

### 5.3 三序列推导复核（新口径全部闭合）＋传导消除

- **① E2→E3→E7（预约发布→自动到期）**：终态 live=F∧expired=T∧对象级残留∧修订级已清（L134＋L78 双清）→ S1 不命中（无待执行修订）→ **S3** ✓。§16.4 HISTORICAL＝CURRENT∪{S3} 系对 §14.2 推导的间接消费（§16.5 锚点①"状态谓词一律以 §14.2 推导为准"），亲证 §16.4/§16.5 **无独立 go_live_at 谓词**——S3 判定归正即漏归档传导消除，该两节无需改动且实际未改，处置正确。
- **② E2→E3→E8（预约发布→手动下线）**：终态 live=F∧expired=F∧fpa 非空∧残留 → **S4** ✓。
- **③ 存草稿＋填预约未点发布**：对象列表单持久化、修订级未登记（L116）→ **S0/S4** ✓；且引擎按修订级取件（L91-93）→ 该页永不被任务发布，判非 S1 与运行时行为严格一致。live 页编辑后预约（S2b）后续经 E7/E8 时 L78 清待执行修订 → S3/S4 无悬挂 pending ✓。
- **次序注记（独立核验，非发现）**：§14.2 S1 先于 S3，与官方 `status_string`（live=F 时 expired 先于 scheduled）在**可达状态上等价**——expired=T∧pending 经标准动作不可达（发布动作 L135 置 expired=F 于两分支、unpublish L78 清 pending），两口径无分歧；设计表序在假想态上亦有定义，全判定性不受影响。

### 5.4 九项 PASS 未破抽查（全绿）

- **正文零触碰**：`c89acd6..5a3d820` 全文件仅两 hunk（L5-14 元数据三行、L409 起 §14+ 追加）；L15-409 与 `c89acd6` 版 `diff` 逐字节相同（BODY-IDENTICAL 亲跑）。
- **断言齐全**：PS-10..29 逐号 grep 20/20 无缺号；E1-E11 边断言 11/11（11 行 E 行全含 PS）。
- **验收亲跑**：00 M3.3 三连 grep PASS；02 S3.3 关键词组（draft/live/expired/go_live_at/expire_at/修订/总管理员/publish_scheduled）全 OK；§4.5 v1.2 双分支正则对 `docs/` 零命中（退出码 1）。
- **P4×3 未动**：P4-1/P4-2/P4-3 维持本审查原处置，"零扩 diff"约束下正确。

### 5.5 E11 留档观察复核——"仅观察、不阻断"定性成立

- **源码事实独立坐实**：`publish_revision.py` L135 `object.expired = False` 缩进在 if/else **之外**＝预约/立即两分支均执行，故 E11（expired→scheduled）在预约动作时即置 expired=F；于是 expired→E11→E4（unschedule 清修订级）后推导 live=F∧expired=F∧fpa 非空＝**S4** 而非预约前 S3，E4 行"预约前原态"括注对该路径不严格。
- **先在性比对亲证**：旧口径同路径（E11 已置 expired=F＋E4 清对象级 go_live_at）同样落 S4——**非本修复引入**。
- **定性同意**：单边措辞精度问题、无断言破坏（PS-17 按 §14.2 推导落点断言即可成立）；留档不阻断正确。建议 B 阶段实现 PS-17 测试时显式覆盖 expired→E11→E4 路径并顺手收紧 E4 括注措辞，无需为此返工本批。

### 5.6 与本审查 §2.e 修复建议对照

建议三件套**全交付**：①S1 条件收紧（采主案"挂钩待执行修订"，未用等价 guard——更简且与官方谓词同字段）②对象级残留语义注记（含全部源码行号，均经本次独立重证吻合）③PS-15/16 断言文字同步。闭合完整，无缺项、无夹带。

**最终结论**：F-1（P2）修复闭合——条件、注记、断言同步、传导消除四位一体；九项原 PASS 未破；E11 观察定性正确且系先在。**本设计放行 B 阶段实现**。本复审未改设计文件、未写代码、未 push；仅追加本节并单 commit。
