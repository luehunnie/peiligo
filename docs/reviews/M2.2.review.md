# M2.2 独立审查报告（信息架构 §6–§11：导航 / 首页优先级 / 部门筛选 / SEO-noindex）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M2.2 步骤全部产物：`docs/INFORMATION_ARCHITECTURE.md` §6–§11 增补 + §12 追溯表增补六行 + §13 断言附录增补 IA-06..14；实现提交 `d34259f`（分支 `arch/m2.2-nav-seo`，基线=批次分支 `7dd9398` 含 M2.1 已验收产物）；被审面 = `git diff 7dd9398..HEAD`（实测仅该一个文件，+204/−2） |
| 所属步骤 | M2.2 · 导航、首页信息优先级、部门筛选与 SEO/noindex 位点（00_MASTER_PLAN §9 M2.2；引源 02 §4 S2.2 L363–388） |
| 风险级 | R1（00 §4.6 规则 7 缺省参考：M2 = R1，普通文档步骤；无 GPT 参与项） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话） |
| 独立性声明 | 本审查者未参与 M2.2 实现；未读取实现 transcript、不以实现者自述为证据；全部结论基于对原始文档（00/02/PRD/ADR/旧站审计）的独立读取与命令亲自复跑；除本报告文件外一切文件与 git 状态只读（仅运行只读 git/grep/sed/test/python 取证）；报告中的命令输出均为本会话真实输出 |
| 权威对照源 | 00 §9 M2.2 步骤块（验收命令原文）、§4.5（扫描正则 v1.2 双分支）、§4.6（双口径规则）、§4.7（分级 A 三件套）；02 §4 S2.2（输出清单 + 验收命令 + 断言点名 IA-06..09）；`docs/PRODUCT_REQUIREMENTS.md` §6（逐字）、§7.1、§8、§9（逐层）、§10、§12、§13、§20；ADR-0002/0004/0005（均 Accepted）；旧站审计 §5.1/§5.2 |
| 审查方法 | 口径A 需求/架构七项清单（§3）+ 口径B 验收命令/保全 diff/扫描亲自复跑（§4）+ 边界合规专项（§5）一次覆盖 |

## 2. 结论

**通过**。无 P0 阻断项、无 P1 批准前修复项、无 P2 修改条件项；2 项 P3 观察 + 2 项 P4 观察记录于 §6（均不构成修改条件）。M2.2 完成条件「验收绿 + 独立审查通过」满足，可按 00 §4.6 由总控合并入批次分支。要点：§6–§11 对 02 S2.2 与 00 M2.2 全部规定产出逐项有落点；首页四层与 PRD §9 逐字节一致（实测 diff 为空）；筛选结果页 vs 部门主页以 §8.3 七维禁令对照表显式区分；diff 实测仅 §6–§11 新增 + §12 增补六行 + §13 增补九断言 + 删除占位 2 行，§1–§5 与 §12/§13 既有行零改动。

## 3. 口径A · 需求/架构逐项核对

### 3.1 §6–§11 覆盖 02 S2.2 与 00 M2.2 全部规定产出 — PASS

逐项对 02 S2.2「输入 → 输出」与 00 §9 M2.2「输出/验收/测试要求」：

| 规定产出（02 S2.2 原文要点） | IA 落点 | 实测 |
| --- | --- | --- |
| §6 全局导航与面包屑：五板块+首页+关于/反馈入口；面包屑含/不含容器段的展示规则按 §4 方案 A 决定并写死 | §6.1 导航两区（页头一级导航=首页+五板块+搜索入口，「仅此七类」；页脚=关于+反馈工具入口）；§6.3 面包屑四行规则表 + 写死理由三条（容器 404 不可点 / 部门不构成导航层级 / 部门以元数据展示）——**决定=含板块不含容器段**，理由链完整 | ✅ |
| §7 首页四层优先级：1 紧急/重要提示→2 最新有效通知→3 近期活动→4 板块入口与其他；推荐位与紧急提示的展示位点、数量固定原则 | §7.1 顺序表（推荐位为「—」序运营区块不占四层序号，位置写死=紧急提示之下、最新通知之上）；§7.2 四条规则（FeaturedItem 载体含起止时间 / 仅总管理员+邮箱申请 / 槽位总数固定常量不自动增长 / 置顶与推荐语义区分） | ✅ |
| §8 部门筛选语义：筛选结果 URL 形态、标题文案「XX 部门发布的内容（筛选结果）」、PRD §6 原文引用 | §8.1 语义冻结+PRD §6 两句原文引用（实测见 §4.5）；§8.2 标题模式两行（板块页+`?dept=`、搜索页+`?dept=`，必含「（筛选结果）」后缀）+ 禁用措辞清单；§8.3 七维禁令对照表；§8.4 空态 | ✅ |
| §9 URL 状态规格：`?q=&section=&dept=&type=&tag=` 五参语义、非法值回退全部、可分享可刷新——移植旧站规格并中文化 | §9.1 载体×参数双表（五参数逐行：语义/取值/非法处置；板块页四参、`?section=` 仅 `/search/` 生效）；§9.2 五条状态语义（全参可选 / 非法值回退 200 不 404 / GET 可分享可刷新可复原 / 排序默认中立 / 分页留实现）；引源=旧站审计 §5.1 原行号 | ✅ |
| §10 SEO/noindex 位点：默认可收录、单篇 noindex 字段位点、sitemap/redirects 贡献位 | §10 八行位点清单（每行挂断言）+ §10.1 实现位点四条（单篇布尔字段+promote 编辑位 / query 态 noindex+canonical 去参基础 URL / sitemap 贡献=仅 #1–#3 / robots.txt 不依赖）+ §10.2 redirects 核对（兑现 §4.1 预留，结论=依赖 Wagtail 原生）+ §10.3 分析位点注记 | ✅ |
| §11 无结果/空态规格落点（引用旧站四态） | §11 四态表（载入中 / 出错+重试不回退假数据 / 全空 / 筛选无匹配含清除筛选）+「全空 ≠ 筛选无匹配」区分 + 容器 404 用通用 404 页不做部门化引导 | ✅ |
| §12 可追溯性行补 PRD §9/§10/§12 行 | 增补六行（§6–§11），源计划/PRD 条款/ADR·审计三列逐格核对与实际内容一致 | ✅ |
| §13 断言：IA-06 导航不含部门入口 / IA-07 面包屑与 §4 方案一致 / IA-08 筛选页 title 不含「主页」/ IA-09 noindex 单篇生效 | IA-06..09 四条逐一落盘且与 02 措辞同义（§3.6 详表）；另按 00「§6–§11 断言 ID 齐全」「SEO 位点逐条有断言」增补 IA-10..14 | ✅ |

关于/反馈入口的实现裁定（页脚工具入口、V1 不建独立「关于」页）与 02 措辞（「全局导航」含页头页脚两区）相容，且是更严格的选择——不新增任何页面树节点，保住 §1.1 四层冻结；PRD §8「通过每页提供的统一工作邮箱反馈」「邮件链接应自动带上内容标题和页面地址」两条逐句有落点（§6.1 反馈条），PRD 全文亦无「关于页」要求。裁定合理。

### 3.2 首页优先级对照 PRD §9（逐层） — PASS

PRD §9 四层（L208–211）与 IA §7.1 表（L227–231）提取后逐字节 `diff` **输出为空**（实测记录见 §4.4）：紧急或重要提示 / 最新且仍有效的通知 / 近期校园活动 / 五个一级板块入口与其他内容。附加要件逐句核对：「首页主体自动展示最新内容」→ §7.1 尾段；「总管理员可管理数量固定、具有开始与结束时间的推荐位或置顶位」→ §7.2 三条对应（管理权引 PRD §8 原句「只有总管理员可以设置全站重要内容或首页置顶」「部门如需置顶，通过统一工作邮箱申请」，实测与 PRD §8 逐字一致）。第 2 层「仅含未过期——到期自动退出首页与默认列表，保留历史归档与站内搜索」与 PRD §7.1 到期句逐字对应。

### 3.3 筛选结果页语义与「部门主页禁令」区分清晰 — PASS

§8 是全文最强的区分载体：① §8.1 冻结「筛选=过滤维度，不是页面形态」，显式声明「不存在任何承载它的独立页面树节点」并回扣 §3.2 容器 404；② §8.2 标题必含「（筛选结果）」后缀 + 五类禁用措辞清单；③ **§8.3 七维禁令对照表**（URL / 页面性质 / 标题文案 / 内容组成 / 导航地位 / 收录 / 权限语义）把「允许的唯一合法形态」与「禁止形态」逐维并排，「右列任一形态出现即违规」——审查者按 00 §9 M2.2 独立审查项「筛选结果页语义与部门主页禁令区分清晰」逐维走查，未发现任何维度上语义含混或留有部门主页化的后门；④ §8.4 空态文案同受禁令约束，§11 容器 404 页「不针对部门段做部门化引导」堵住最后一条变相部门页观感路径。与 PRD §6「部门筛选结果不是部门主页」、PRD §20「部门独立主页」不做项、ADR-0004 决策 2 三方闭合。

### 3.4 SEO 对照 PRD §12 — PASS

PRD §12 两要件逐条有落点：「默认允许搜索引擎收录」→ §10 #1–#3（首页/板块页×5/内容页默认态=index+入 sitemap，IA-10）；「单篇内容可设置 noindex」→ #4（布尔字段置位→meta noindex+同步退出 sitemap，IA-09），编辑位（后台 promote 侧）与输出位（全局模板 meta）均写死。扩展项（#5 容器排除、#6–#8 筛选态/搜索页/跳转页 noindex+canonical）为 00 §9 M2.2 阶段目标明文要求（「含容器节点与筛选结果页的 noindex/排除策略」），非实现者自创越界；canonical 指向去参基础 URL 与「参数组合不构成独立收录单元」的理由自洽，并与 §8.3 收录维度、「部门维度永不产生可收录页面」闭环。§10.3 分析位点注记把 PRD §12 数据收集四项与 §11 空态一一登记、正式规格显式留给 M7.2——边界交代清晰。

### 3.5 与已 Accepted 的 §1–§5（站点树/URL 方案 A/容器 404）无冲突 — PASS

| §1–§5 冻结项 | §6–§11 落点 | 一致性 |
| --- | --- | --- |
| §3.2 容器不进导航/sitemap/默认列表/默认搜索（语义已冻结，落实位点登记给 M2.2） | §6.2 四行排除位表（导航数据源/板块默认列表/搜索索引/Snippet 天然排除），显式「承接 §3.2，不重定义 404 语义」 | ✅ 语义零漂移 |
| §3.3 筛选页 URL 形态与标题文案留给 M2.2 §8/§9 | §8/§9 按预留展开；§4.4 表「部门筛选结果页=query string 形态，由 M2.2 §9 定义」被 §9.1 兑现（`/<板块>/?dept=` 与 `/search/?dept=`） | ✅ |
| §4 方案 A：URL 含部门段、容器段 404 | §10 #3 内容页 URL `/<板块>/<部门>/<slug>/` 与 §4.1 形态一致；#5 容器 404+sitemap 排除；§8.3 URL 行重申 `/chronicle/jwc/` 维持 404 | ✅ |
| §4.1 预留「redirects 贡献位由 M2.2 §10 核对」 | §10.2 显式兑现（核对结论：原生机制即可，两条场景均无需自定义代码） | ✅ 预留闭环 |
| §4 路径唯一决定板块 | §9.1 板块页四参数、`?section=` 板块页一律忽略——无第二条板块决定路径 | ✅ |
| §1.1 四层冻结 / §5 挂载约束 | §6.1 关于/反馈零新增树节点；§9.1 `/search/` 为自定义视图不入页面树（载体先例=ADR-0005 #5 跳转页，非新页面树形态）；§7.1 第 3 层活动列表引 §5 结构化字段承载 | ✅ |

ADR 交叉核对：ADR-0004 决策 2（404/四类排除/不渲染聚合页）§6.2、§8.1、§10 #5 三处引用零冲突；ADR-0005 载体表 #5（跳转页自定义视图）、#8–#11（受控类别词表）、#12（受控标签）、#13（FeaturedItem 含起止时间）、#14（紧急提示/统一反馈邮箱/跳转文案 settings）逐条与引用处一致；ADR-0002 服务端模板前提（§6.1 全站统一渲染、§9.2 无 JS 依赖、§10.1 meta 输出位）成立，§9.1「不做前端过滤」引 ADR-0002 否决理由（旧站全量拉取+includes 过滤不可用）属实。唯一瑕疵为 §10.1 一处 ADR-0002 内部定位引用不准（观察项 P3-1，实质文本逐字命中）。

### 3.6 IA 断言 ID（§6–§11 对应）齐全 — PASS

| 02 S2.2 点名断言 | IA §13 落盘 | 同义核对 |
| --- | --- | --- |
| IA-06 导航不含部门入口 | IA-06（一级项仅=首页+五板块+工具入口；默认列表亦无容器条目） | ✅ 且并入了板块默认列表维度 |
| IA-07 面包屑与 §4 方案一致 | IA-07（含板块不含容器段；除当前页每段可点且指向 §4.4 表 200 URL） | ✅ |
| IA-08 筛选页 title 不含「主页」字样 | IA-08（H1 与 `<title>` 含「（筛选结果）」且不含「主页/门户/空间/专区」） | ✅ 强于原要求（加正反两面） |
| IA-09 noindex 单篇生效 | IA-09（meta noindex + 同步退出 sitemap） | ✅ |

00 附加要求覆盖：IA-10（默认收录面）、IA-11（§9 URL 状态）、IA-12（#6–#8 noindex+canonical）、IA-13（§7 首页四层+推荐位）、IA-14（§11 四态）——「§6–§11 断言 ID 齐全」「SEO 位点与 noindex 清单逐条有断言」均满足（§10 表八行逐行挂断言）。每条断言有引源与验证位点（MB8–MB13/交付线），§6–§11 各节均有「本节断言」指针行，全文 IA-01..IA-14 无缺号（实测计数见 §4.2）。

### 3.7 越界四查（00 §4.6 口径A） — PASS

- **不改 Wagtail 核心**：无（`/search/`、外链跳转页均为自定义视图先例内；redirects 用原生机制；URL 零覆写沿方案 A）；
- **不扩 V1**：无（筛选=PRD §10 既有要求；推荐位/置顶=PRD §9 既有能力；§6.1 反向收紧「不建独立关于页」）；
- **不破坏权限模型**：无（§8.3 权限语义行显式禁止把后台权限容器前台化；零自写权限逻辑）；
- **不偏离信息架构**：无（四层树、方案 A、容器 404 全部沿 §1–§5；见 §3.5）。

## 4. 口径B · 实现/测试/安全实测（审查者亲自复跑，真实输出）

审查环境：独立 worktree（`m2.2-impl`），实测前 `git status --porcelain` 为空、`git rev-parse HEAD` = `d34259f`、分支 `arch/m2.2-nav-seo`——被审面与工作区一致。

### 4.1 00 §9 M2.2 验收命令 — 全 PASS

```
$ grep -q 'noindex' docs/INFORMATION_ARCHITECTURE.md && grep -q '首页' docs/INFORMATION_ARCHITECTURE.md && grep -q '部门筛选' docs/INFORMATION_ARCHITECTURE.md
PASS: noindex+首页+部门筛选 三连命中 (exit 0)

$ §6–§11 断言 ID 齐全（00 原文条目）
IA-01..IA-14 全部在册（见 §4.2 计数），§6→IA-06/07、§7→IA-13、§8→IA-08、
§9→IA-11、§10→IA-09/10/12、§11→IA-14 指针行齐备   PASS

$ ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
PASS: 零命中（grep 退出码 1 且无输出）   # §4.5 v1.2 双分支正则原文，对 docs/ 全目录
```

### 4.2 02 S2.2 验收命令 — 全 PASS

```
$ grep -q '紧急' docs/INFORMATION_ARCHITECTURE.md && grep -qE '置顶|推荐位' docs/INFORMATION_ARCHITECTURE.md
PASS: 紧急+置顶/推荐位 (exit 0)

$ grep -qE '\?q=|query' docs/INFORMATION_ARCHITECTURE.md
PASS: ?q=/query (exit 0)      # §9.1 双表共 6 处 ?q= 形态

$ grep -q 'noindex' docs/INFORMATION_ARCHITECTURE.md
PASS (exit 0)                  # §10 清单 8 行 + §10.1

$ grep -q '面包屑' docs/INFORMATION_ARCHITECTURE.md
PASS (exit 0)                  # §6.3 规则表

$ test "$(grep -c '^## ' docs/INFORMATION_ARCHITECTURE.md)" -ge 13
H2 count = 14   PASS           # 文档信息+§1–§13

$ grep -oE 'IA-[0-9]{2}' docs/INFORMATION_ARCHITECTURE.md | sort | uniq -c
IA-01..IA-14 每号 ≥2 次命中；IA- 总计数 = 35   PASS
```

### 4.3 §1–§5 及 §12 既有行保全实测 — PASS（diff 逐行裁定）

```
$ git diff 7dd9398..HEAD --stat / --numstat
 docs/INFORMATION_ARCHITECTURE.md | 206 +++++...-
 1 file changed, 204 insertions(+), 2 deletions(-)     # 与派发令声明 +204/−2 一致

$ git diff 7dd9398..HEAD -U0 -- docs/INFORMATION_ARCHITECTURE.md | grep -c '^-[^-]'
2                                                     # 全部删除行恰为占位 2 行：
-## 6–11.（M2.2 增补，本步留白）
-> §6 全局导航与面包屑、§7 … §11 无结果/空态规格——由 M2.2（02 §4 S2.2）增补，本步不预写。

$ diff <(git show 7dd9398:docs/INFORMATION_ARCHITECTURE.md | sed -n '1,177p') <(sed -n '1,177p' docs/INFORMATION_ARCHITECTURE.md)
（无输出）   PASS: 文档信息+§1–§5 全部（至占位前）与新文件逐字节一致

$ diff <(git show 7dd9398:… | grep '^| §[0-9]') <(grep '^| §[0-9]' docs/… | head -5)
（无输出）   PASS: §12 既有 §1–§5 五行零改动；新增 §6–§11 六行

$ diff <(git show 7dd9398:… | grep '^| IA-0[1-5]') <(grep '^| IA-' docs/… | head -5)
（无输出）   PASS: §13 既有 IA-01..05 五行零改动；新增 IA-06..14 九行 + 尾注一行

$ git diff 7dd9398..HEAD --name-only  +  git status --porcelain
docs/INFORMATION_ARCHITECTURE.md（唯一）；工作区净   PASS
```

结论：改动面 = 删除 §6–11 占位 2 行 + §6–§11 新增 + §12 增补六行 + §13 增补断言，**与派发令允许清单逐项精确一致，零越界**。

### 4.4 首页四层与 PRD §9 逐字节比对 — PASS

PRD §9（L208–211）编号项提取 vs IA §7.1 表（L227–231）四行提取，`diff` 输出为空：四层名称逐字节一致（含全角字符）。

### 4.5 §8.1 引文与 PRD §6 原句比对 — PASS（引号字形差异同 M2.1 P3-1 类）

python 逐字符比对：句 2（「部门名称作为内容作者元数据展示…」）逐字一致；句 1（「板块描述…」）唯一差异为引号字形（PRD 全角弯引号 vs IA 直引号），归一化后逐字节一致、语义零漂移。

### 4.6 安全面 — PASS

§4.5 秘密扫描零命中（§4.1 末条，实测）；§6–§11 全文仅含路径、条款号、示例 slug（`jwc` 等明示为示意）与文档名引用，无凭据、无令牌、无环境变量值、无真实个人数据；本报告自身落盘后于 commit 前对 `docs/` 复扫仍零命中。

## 5. 边界合规专项

1. **单文件边界**：diff 仅 `docs/INFORMATION_ARCHITECTURE.md` 一个文件（02「允许修改文件范围」+ 00「该文件 §6–§11」）——实测通过（§4.3）。
2. **不改 §1–§5**：L1–177 逐字节一致（§4.3）——02「单步骤对话边界：只编辑 §6–§13；不改 §1–§5」实测通过。
3. **§12/§13 载体合法性**：M2.1 审查 §5 已裁定其为分级 A 三件套的合理载体；本步 §12 六行与 §13 断言是 02 S2.2 **测试要求明文义务**（「断言 IA-06–IA-09 写入附录；可追溯性表补 PRD §9/§10/§12 行」），非越界，且实测只增不改（§4.3）。
4. **占位删除**：M2.1 留下的「§6–11 占位 2 行」被本步替换为正式内容——这正是占位行的设计用途，删除在允许清单内（派发令明文）。

## 6. 观察项（不构成修改条件）

| # | 级别 | 观察 | 建议 |
| --- | --- | --- | --- |
| P3-1 | P3 | §10.1 称「ADR-0002 决策 2：meta/noindex 由模板与页面机制直接控制」——该句在 ADR-0002 中实际位于「后果·正面收益」第 1 条（「决策」节的边界条件 2 讲的是静态资源），定位引用不准；引文本身与 ADR 原文逐字一致，实质无漂移。 | 后续文档修订时更正为「ADR-0002 后果·正面收益 1」；不阻断、不需 Fix Session。 |
| P3-2 | P3 | 文档头「文档信息」表仍为 M2.1 状态：「待增补 §6–§11…」与「分支 `arch/m2.1-site-tree`」两行在 M2.2 完成后失真。该表位于 §6–§13 允许编辑范围之外，实现者依边界**不得**改——属计划边界的结构性残留，非实现者过失。 | 建议总控在合并本步时顺带更新文档头（本步范围/日期/分支三行），或登记到下一次文档卫生修订；不在本步返工。 |
| P4-3 | P4 | 非部门筛选态（仅 `?q=`/`?type=`/`?tag=`、无 `?dept=`）的 H1/`<title>` 文案模式未定义：§6.3 注「筛选/搜索态的标题随参数变化（§8.2）」指向的 §8.2 仅覆盖 `?dept=` 两行；IA-08 也只断言部门筛选态。02 S2.2 仅要求部门筛选标题规则，故非缺失义务。 | MB10 实现搜索/板块筛选视图时补一条通用标题规则（如「含筛选条件回显、可选『（筛选结果）』后缀泛化」），并在 M3.4/MB10 契约中登记；避免实现期自由发挥。 |
| P4-4 | P4 | 引号字形混用（直引号 vs PRD 全角弯引号）沿用 M2.1 P3-1 状态未统一（§8.1 句 1 实测仅此差异）。 | 与 P3-1 合并进下一次文档卫生修订；不阻断。 |

## 7. 风险建议

1. **M3.4 义务承接**：§9 五参数规格（含 `tag` 参数是否按词表细分的显式留判）与 §6.2 默认搜索排除规则是 M3.4「筛选维度契约」的直接输入——M3.4 派发契约应引用 §9.1/§9.2 原文，不得另立参数集；其依赖 M2.2（00 §5.2 v1.1 例外）现已解锁。
2. **实测挂接**：IA-06..14 验证位点分布于 MB8（FeaturedItem）/MB9（模板与首页）/MB10（搜索视图）/MB12（外链跳转）/MB13（SEO 实现）——建议各 MB 步骤验收命令显式引用对应 IA 编号，防止文档期断言到实现期失挂；IA-08 须含「板块筛选态与搜索页两载体」用例（§13 已写明）。
3. **文档头失真**：见 P3-2——总控合并时处理，避免 G2 门检查读到过期元信息。
4. **MB10 前置输入**：P4-3 的非部门筛选态标题规则建议在 M3.4 或 MB10 任务契约中立此存照，两次机会任一补上即可。
