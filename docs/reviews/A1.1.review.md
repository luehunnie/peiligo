# review(A1.1) · S1 环境依赖补齐独立审查报告

PASS

| 项目 | 内容 |
| --- | --- |
| 审查对象 | A1.1 · S1 环境依赖补齐（本地/测试统一 PostgreSQL、连接与敏感配置全环境变量外置、psycopg/gunicorn/pip-audit 钉版），提交 `84a9349`，分支 `arch/a1.1-s1-env`（审查 worktree `a1.1-review`，分支 `arch/a1.1-s1-review`） |
| 基线 | `rebuild/v1`（`e71f6ead`）——`git diff e71f6ead..84a9349` 单提交 8 文件（+160/−29），逐文件人工审阅 |
| 审查人 | 独立 Reviewer 会话（不继承实现对话上下文；只写本报告与提交，未改任何实现文件） |
| 审查日期 | 2026-08-18 |
| 结论 | **PASS**（0×P2 / 0×P3 / 5×P4；七项必查全部亲自重跑取证通过；A1 评审 P3-1 四子项 (a)(b)(c)(d) 经本次逐项核验全部收口） |
| 输入 | A1.1 提交与全量 diff；`docs/plans/03_DELIVERY_OPERATIONS_PLAN.md` §4 S1（219-243 行）；`docs/adr/0001`；`docs/decisions/ROLLING_IMPLEMENTATION_POLICY.md`；`docs/reviews/A1.review.md`（P3-1 对照） |
| 数据红线 | 同实现口径执行：仅触碰 `peiligo_restart_*` 库、零 DROP（ authored）、旧库（`peiligo_dev`/`peiligo_test`/`postgres`/`template*`）全程未触碰——取证见 §2.3 |

## 1. 前置状态核验

- 审查 worktree 干净（`git status --porcelain` 空），HEAD = `84a9349`；基线 `e71f6ead` 为本分支祖先。
- diff 范围 8 文件全为 M、无新增文件：`.env.example`、`README.md`、`requirements.txt`、`requirements-dev.txt`、`src/peiligo/settings/{base,dev,production}.py`、`tests/test_settings.py`——均在 S1「允许修改文件范围」（仓库根骨架与配置新文件 + `tests/` 基线）之内；**零 `docs/` 改动、零 `.github/`/`Dockerfile`/`compose*`/`scripts/`**。
- 本地无 remote（`git remote -v` 空）——「未 push」成立（审查亦未 push）。
- 环境重建：`.venv` 新建（Python 3.13.11），`pip install -r requirements-dev.txt` + `pip install -e .` 后跑全部验收命令；本地 `.env` 按README 记载形态新建（字段=DATABASE_URL/TEST_DATABASE_URL/SECRET_KEY/ALLOWED_HOSTS/MEDIA_ROOT，socket 连接形态；内容不引用——00 §4.5 仅记路径+字段名），已被 `.gitignore:3` 忽略（`git check-ignore -v` 证实，`git status` 全程零行）。

## 2. 七项必查亲自重跑结果

### 2.1 ① 原 10 tests 零退化（在 14 passed 中逐项核对）

```
$ .venv/bin/python -m pytest -v      # 环境变量经 set -a; source .env; set +a 导出
tests/test_home.py::HomeSetUpTests::test_homepage_create PASSED
tests/test_home.py::HomeSetUpTests::test_root_create PASSED
tests/test_home.py::HomeTests::test_homepage_is_renderable PASSED
tests/test_home.py::HomeTests::test_homepage_template_used PASSED
tests/test_smoke.py::test_home_page_returns_200 PASSED
tests/test_smoke.py::test_wagtail_admin_login_page_returns_200 PASSED
tests/test_settings.py::test_settings_module_importable[peiligo.settings.base] PASSED
tests/test_settings.py::test_settings_module_importable[peiligo.settings.dev] PASSED
tests/test_settings.py::test_settings_module_importable[peiligo.settings.production] PASSED
tests/test_settings.py::test_no_hardcoded_loopback_in_db_related_sources PASSED
tests/test_settings.py::test_dev_database_is_postgresql_via_env PASSED
tests/test_settings.py::test_test_database_name_from_env_variable PASSED
tests/test_settings.py::test_env_example_covers_required_keys PASSED
tests/test_settings.py::test_env_example_values_are_dummy PASSED
14 passed in 4.40s   (exit 0)
```

✓ 原 A1 基线 10 项（`git show e71f6ead` 固定清单：test_home 4 + test_smoke 2 + test_settings 4）逐一在列且全绿，`tests/test_home.py`、`test_smoke.py`、`__init__.py` 相对基线零改动（diff 0 行）；新增恰为 4 项（PG 基线/测试库名外置/.env.example 键覆盖/.env.example 哑值）→ 10+4=14 与自述一致。三套 settings 可导入由参数化测试实跑复验，另直接导入 production（`ALLOWED_HOSTS=['example.com']`、`DEBUG=False` 取值正确）。

### 2.2 ② SQLite 退出正式基线 + 全环境变量化 + 零硬编码

- `src/` 内零 sqlite 引擎引用：基线的 `else` 回退分支已删除，`base.py` 现为 `DATABASES = {"default": dj_database_url.parse(env_required("DATABASE_URL"))}`——缺失即快速失败，亲自复验：

```
$ env -u DATABASE_URL DJANGO_SETTINGS_MODULE=peiligo.settings.dev .venv/bin/python -c "import peiligo.settings.dev"
RuntimeError: DATABASE_URL 未设置：环境变量外置（03 计划 S1），哑值样例见 .env.example
$ env -u SECRET_KEY ... → RuntimeError: SECRET_KEY 未设置...
```

- 测试库：`DATABASES["default"]["TEST"]["NAME"]` 取自 `TEST_DATABASE_URL` 环境变量（运行时目击证据见 §2.3——测试库名确为 `peiligo_restart_test`，无 `test_` 前缀）。
- 零硬编码扫描：`git grep -nE "(localhost|127\.0\.0\.1|5432|://[^ ]*:[^ @]*@|PASSWORD|passw)" -- src/ tests/ pyproject.toml requirements*.txt`——settings/测试/配置内零主机/端口/口令/连接串字面量（命中仅 `.env.example` 哑值样例与测试断言用哑值标记元组、`AUTH_PASSWORD_VALIDATORS` 校验器名，均非配置值）；S1 防 R5 复发断言（无 `127.0.0.1` 字面量）实测通过。
- README 记载的 socket 连接形态（`postgres://<用户>@/<库名>?host=/tmp`）解析实证：`dj_database_url.parse` → `ENGINE=django.db.backends.postgresql`、`NAME` 正确、`OPTIONS={'host': '/tmp'}`（psycopg/libpq 合法形态）——文档声称与实际行为一致。

### 2.3 ③ PG 安全取证（数据红线）

库清单三时点快照 + 运行期间轮询目击（`psql -h /tmp -d postgres`，Homebrew PostgreSQL 18.4）：

```
BEFORE pytest:  peiligo_dev / peiligo_restart_dev / peiligo_test / postgres / template0 / template1
DURING pytest:  上述全部 + peiligo_restart_test        （0.2s 间隔轮询 pg_database 目击）
AFTER pytest:   peiligo_dev / peiligo_restart_dev / peiligo_test / postgres / template0 / template1
```

- **仅 `peiligo_restart_*` 被触碰**：`peiligo_restart_dev`（属主 chenjunxian，12 MB，已迁移；本次 `manage.py migrate` 复跑 "No migrations to apply" exit 0 幂等）+ `peiligo_restart_test`（pytest 会话内自建、会话末自清——Django/pytest-django 测试运行器标准生命周期，运行前后快照差集为空即为证）。
- **旧库未触碰**：`peiligo_dev`（属主 `peiligo_dev_user`，8054 kB，public 3 表）、`peiligo_test`（属主 `peiligo_test_user`，8134 kB，public 3 表）、`postgres`/`template0`/`template1`（public 0 表）——三时点在位、属主/大小/表数一致；tracked 文件内 `git grep "peiligo_dev\|peiligo_test"` 仅命中既有审计报告 `docs/audit-report-old-peiligo.md`（历史描述，本 diff 未触碰）。
- **零 DROP**：`git grep -niE "drop[ _]?(database|table)|truncate"` 全仓零命中；本次审查执行的 DROP 仅有一条——测试运行器对临时库 `peiligo_restart_test` 的会话末回收（允许命名空间内、标准行为，非 authored 语句）。

### 2.4 ④ 依赖零膨胀 + ADR-0001 逐项核对

- diff 增量恰为四处：`requirements.txt` +`gunicorn==26.1.0`、+`psycopg==3.3.4`、+`psycopg-binary==3.3.4`（33→36 包，全 `==`）；`requirements-dev.txt` +`pip-audit==2.10.1`。与 03 S1 输出清单（psycopg、gunicorn；dev：pip-audit）逐项对应；`psycopg-binary` 为声明依赖 psycopg 的二进制分发伴随包（免本机编译），README 已注明理由。
- pip freeze 逐行核对：requirements.txt 全部 36 个运行时钉版在 freeze 中精确一致（`grep -qixF` 逐行全中）；freeze 差集恰为 dev 工具链闭包（pip-audit/pytest 及其传递依赖：cyclonedx-python-lib、rich、pluggy、iniconfig 等）+ editable 安装标记——运行时零漂移。
- ADR-0001 四约束逐项：`wagtail==7.4.2`（`>=7.4.2,<7.5`）✓、`Django==5.2.17`（`>=5.2,<5.3` 且 ≥5.2.17 安全补丁）✓、Python 3.13.11 ✓、`modelsearch==1.3.2`（`>=1.3.2,<1.4`）✓。
- PyPI 当日复核（curl JSON API，亲自）：`psycopg` 最新稳定=3.3.4 ✓、`gunicorn`=26.1.0 ✓、`pip-audit`=2.10.1 ✓——README「同日定向复核补记」与事实一致。
- pip-audit 复跑：

```
$ .venv/bin/pip-audit
No known vulnerabilities found
（skip 行：peiligo 本地 editable 包不在 PyPI，预期跳过）  exit=0
```

### 2.5 ⑤ 零 M2/M3 越界

- diff 全量人工审阅：仅环境配置/依赖/settings 外置/测试强化/README 说明，零业务代码；`git diff | grep -iE "class .*\(Page\)|models\.Model|ForeignKey|ManyToMany|permission|register_snippet|docker|compose|github|workflow|backup|monitor|prometheus|nginx"` 零命中。
- 五个空壳 app（departments/notices/resources/guides/frontend）仍各仅 `__init__.py`；无 `.github/`、无 Dockerfile/compose、无 scripts/、无备份/监控；页面树/权限/发布/搜索实现零增量。

### 2.6 ⑥ Accepted ADR/PRD/00-03 计划零改动

```
$ git diff --name-only e71f6ead..84a9349 -- docs/
（空输出）
```

✓ PRD、ADR-0001..0005、00/01/02/03 计划、G1、decisions 台账全部零改动（name-only 全域核对）；改动 8 文件无一在 `docs/`——ROLLING_IMPLEMENTATION_POLICY §2-5「PRD 与 Accepted ADR 零改动」合规。

### 2.7 ⑦ 零真实秘密（00 §4.5 v1.2 双分支扫描原样复跑）

```
$ grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
（无输出）exit=1        # 两分支单独复跑亦各自 exit=1 零输出
$ git grep -InE '<同式>' -- .        # 扩展：全部 tracked 文件
（无输出）exit=1
```

- `.env` 不在 Git：`git ls-files | grep '\.env'` 仅 `.env.example`；本地 `.env` 被 `.gitignore:3` 忽略（`git check-ignore -v` 证实）。
- `.env.example` 逐项哑值：`CHANGE_ME`（口令占位）、`django-insecure-change-me-not-a-real-secret`（密钥）、`example.com`（允许主机）、`/tmp/peiligo_restart_media`（媒体根）、`localhost`（主机）——全部为哑值标记，且由新增测试 `test_env_example_values_are_dummy` 持续兜底；dev/production 缺失即快速失败（§2.2），杜绝以占位值上线。
- 注：哑值形态不触发 §4.5 正则（如 `SECRET_KEY=` 的 `_` 不属分隔符字符类）为契约 v1.2 既有设计，非本提交问题；哑值正确性由上述测试与人工逐项核对双保险。

## 3. 附加复验（S1 审查规格补充项）

- **`manage.py check --deploy`（production，需完整环境变量）**：exit 0，8 项告警=3×treebeard.E001（上游已知，README 在案）+ 安全差距逐项：

| 项 | 含义 | 定性 |
| --- | --- | --- |
| W004 | `SECURE_HSTS_SECONDS` 未设 | 部署环境项（TLS 拓扑确定后设，S16/S19） |
| W008 | `SECURE_SSL_REDIRECT` 未设 | 部署环境项（反向代理/负载均衡层处理） |
| W009 | SECRET_KEY 疑似样例（<50 字符/`django-insecure-` 前缀） | **本次复跑用 `.env.example` 哑值所致**；真实部署用足够长随机值即消（.env.example 已注明） |
| W012 | `SESSION_COOKIE_SECURE` 未设 | 部署环境项（HTTPS 上线时设） |
| W016 | `CSRF_COOKIE_SECURE` 未设 | 部署环境项（同上） |

  A1 评审时的 **W020（ALLOWED_HOSTS 为空）已消失**——本提交外置并强制 ALLOWED_HOSTS 后该项收口；差距全部属「部署环境才有的项」或哑值复跑伪影，满足 S1「差距仅限部署环境项并逐项列出」口径。
- **S1 验收命令组**：`python --version`（3.13.11）✓、`pip install -r requirements*.txt` ✓、`manage.py check` exit 0（3×treebeard.E001 已知）✓、`makemigrations --check --dry-run` "No changes detected" exit 0 ✓、`pytest -q` 14 passed ✓、`ruff check .` "All checks passed!" + `ruff format --check .` "57 files already formatted" exit 0 ✓——全绿。
- **A1 评审 P3-1 收口对照**：(a) 本地/测试统一 PostgreSQL+psycopg ✓（§2.2/2.3）；(b) gunicorn 钉版 ✓；(c) pip-audit 入 requirements-dev ✓；(d) `.env.example` 五键哑值 + production SECRET_KEY/ALLOWED_HOSTS 环境接线（W020 消失）✓——四子项全部落地，A1 遗留 P3 就此关闭。

## 4. 发现与分级

**0×P2、0×P3**（无合并条件级发现）。P4 观察（均不构成条件）：

1. **TEST_DATABASE_URL 仅取 NAME**：`base.py` 解析该 URL 后仅用其库名，主机/口令/端口沿用 default 连接（代码注释、README、.env.example 三处已如实声明）。若二者指向不同实例会以 default 连接参数静默连接——建议后续批次（CI 参数化，S15 前）改为整串解析或文档加显式警示。
2. **`settings/local.py` 覆盖钩子未被 gitignore**：production.py 保留官方模板 `from .local import *`（try/except）钩子，而 M0.2 基线 `.gitignore` 未含官方模板的 `settings/local.py` 忽略行——未来该文件若写入真实秘密存在误入库风险（当前文件不存在、无实际暴露面；§4.5 扫描与合并门可拦截）。建议后续基础设施批次补忽略行。
3. **gunicorn 26.1.0 为当日（2026-08-18）新发版**：最新稳定版钉版符合 S1「钉最新」口径且已经 PyPI 复核，但零日新鲜度带来早期采用者风险；PRD §17 季度升级/月度安全检查节奏可吸收。
4. **dev.py 保留 `ALLOWED_HOSTS = ["*"]` 字面量**：官方模板原样（DEBUG=True 专用），S1「允许主机外置」由 production 侧接线达成，.env.example 已注明 dev 不读取该项——口径自洽，记录备查。
5. **WAGTAILADMIN_BASE_URL 仍为官方缺省 `http://example.com`**（A1 评审 P4-2 遗留，本提交范围外，仍未设）。

## 5. 结论

**PASS**。七项必查全部亲自重跑取证通过：原 10 测试零退化（14 passed 逐项核对）、SQLite 正式退出基线且连接/敏感配置全环境变量化并快速失败、数据红线全程合规（仅 `peiligo_restart_*`、零 authored DROP、旧库三时点快照完好）、依赖零膨胀且与 ADR-0001/PyPI 当日事实逐项一致、零 M2/M3 越界、Accepted 文档零改动、秘密扫描双分支零命中。附加项（pip-audit 零漏洞、check --deploy 差距逐项、三 settings 可导入、freeze 逐行）全部核验。A1 评审 P3-1 四子项确认收口。发现 0×P2/0×P3/5×P4（均不构成条件）。本审查仅新增本报告文件，未触任何实现文件，未 push。
