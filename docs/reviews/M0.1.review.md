# M0.1 独立审查报告（D0_BASELINE_CONFIRMATION · G0 批准固化产物）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | `docs/decisions/D0_BASELINE_CONFIRMATION.md`（新建，2026-08-18 14:54 落盘，114 行） |
| 所属步骤 | M0.1 · 基线确认记录与决策台账（00_MASTER_PLAN v1.4 §7 M0.1；风险级 R1=§4.6 规则 7 缺省，本任务派发令 RATIONALE 确认） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发） |
| 独立性声明 | 本审查者为全新会话，未参与 M0.1 实现及 D0/PRD 微修订的任何起草工作，与实现对话无上下文继承；审查全程除本报告外一切文件只读，未创建/修改任何其他文件 |
| 权威对照源 | 00_MASTER_PLAN v1.4 §1（OR-1..OR-6）、§2.1（C1–C5）、§2.2（D0–D9）、§7 M0.1 步骤块、§4.5/§4.6；PRD 文档头（L3–L10）；M00.review.md §5/§6 |
| 禁区遵守 | 全程未进入、未读取、未对旧仓 `/Users/chenjunxian/vscode_projects/peiligo` 执行任何命令（OR-2）；未读取任何 §4.5 秘密禁区路径；未执行任何 git 写操作 |
| 本报告唯一写入文件 | `docs/reviews/M0.1.review.md`（本次新建） |

## 2. 双口径发现清单

共 3 项发现，全部为 **P2（观察记录，不阻塞、无需批准前修复）**；**未发现 P0（阻断）或 P1（批准前修复）级别问题**。

### F-01（A①-c）· C/D 表嵌入小节标题去掉了源文件节号前缀，§8「逐字一致」表述在整节口径上略有不精确

- **位置**：D0 §4 首行（L68）vs 00 §2.1 标题（L34）；D0 §5 首行（L84）vs 00 §2.2 标题（L46）；D0 §8-1（L111）。
- **问题**：D0 声明「以下自 00_MASTER_PLAN v1.4 §2.1/§2.2 逐字复制，表格原样」，但复制块的 ### 小节标题行将「### 2.1 已确认决策…」「### 2.2 决策门处置表…」改写为无节号形式（否则与 D0 自身 §4/§5 编号冲突）。因此 §8「§3/§4/§5 与 00 §1/§2.1/§2.2 逐字一致」在"含标题整块"口径下并非 100% 字节一致。
- **证据**：diff 显示两侧仅标题行 1 行差异（`### 2.1 已确认决策（C1–C5，冻结，不得重开）` vs `### 已确认决策（C1–C5，冻结，不得重开）`；§2.2 同理）；排除标题行后表格本体 diff 为空且 md5 相等（见 §4 命令 5–7）。
- **影响**：无——M0.1 测试要求为「OR/C/D **三表**逐条在案、与 §1/§2 逐字一致」，三表本体（OR 六条 bullet、C 表含"另"行、D 表十行）已字节级一致；去节号是跨文档嵌入的必要适配。
- **建议**：无需修改。如后续经 M0.6 变更管理流程触碰 D0，可在 §8 补一句「（嵌入标题去源节号，表格本体逐字一致）」，非必要。

### F-02（A①-a）· PRD 微修订的派发授权文本为仓库外事实，仓库内不可独立核验（仅追溯性观察）

- **位置**：D0 §2.1（L42–51）。
- **问题**：M0.1 步骤块「允许修改的文件范围」原为仅 `docs/decisions/D0_BASELINE_CONFIRMATION.md`；PRD 文档头「文档状态」行的修改超出该原始范围，其授权依据（总控 Coordinator 于 M0.1 派发令中显式授权）只记录于 D0 §2.1 自述，派发令原文不在仓库内，本审查者无法从仓库产物独立复核该授权文本。
- **证据（三点旁证链）**：① 本审查任务自身的派发令即将「PRD 微修订核查」列为预期核查项，佐证总控对该微修订知情且预期；② G0 裁决④语义本身要求该修改（M00.review.md §6-3：PRD 最终确认=清零文档头「待用户最终确认」状态）；③ 取证证明实际改动恰为 D0 §2.1 声明的唯一一行（见 §3 B③），无任何越界改动。
- **影响**：无执行风险；§3.0 裁决顺序第 1 级（项目负责人最新指令）覆盖第 2 级计划文本，程序正当。
- **建议**：无需修改；M0.2 入库后 D0 §2.1 即为该授权的固化记录。

### F-03（A①-a）· D0 引用的 M00 dispatch ID 在 M00.review.md 内无对应记录

- **位置**：D0 §1 条件①（L16）。
- **问题**：D0 记 M00 审查为「Orca dispatch ctx_2ece3894b991」，而 M00.review.md §1 元信息仅记「独立 GLM-5.3 新会话（Orca Dispatch 派发）」未含 dispatch ID，仓库内无法交叉验证该 ID。
- **证据**：M00.review.md L11；`grep -c 'ctx_' docs/reviews/M00.review.md` = 0。
- **影响**：无——Orca 控制面对仓库只读（§4.1），dispatch ID 属控制面事实，仓库产物本不承载。
- **建议**：无需修改；如需追溯以 Orca 控制面记录为准。

## 3. 逐项核对表

| # | 核对项 | 结论 | 一句证据 |
| --- | --- | --- | --- |
| A①-a | G0 四条件批准记录完整固化（含 R3_GPT_WAIVER 四要素与一次性语义、PRD 最终确认引用） | **通过** | D0 §1 条件①–④各一段在案：waiver 四要素 TASK=M00/REASON/HUMAN_APPROVAL/DATE 齐备且明示「仅对 M00 这一个 Task 有效，不可复用、不可泛化」（L18–23）；§2 六字段确认表完整；四条件事实与 M00.review.md §5（通过，0×P0/0×P1/5×P2，实测 P2-01..P2-05 共 5 项）与 §6 逐条吻合 |
| A①-b | OR/C/D 三表与 00_MASTER_PLAN §1/§2 逐字一致（diff 证明） | **通过** | OR 六条 bullet（00 L25–30 vs D0 L57–62）、C 表本体（00 L35–44 vs D0 L69–78）、D 表本体（00 L47–59 vs D0 L85–97）三组 diff 均为空、md5 两两相等；仅嵌入小节标题去源节号（F-01，P2） |
| A①-c | 无漂移、未引入新决策、未重开已确认决策 | **通过** | 三表字节一致即无表内漂移；§1「背景附注」（M0–M7 至 G2、A 阶段 29 步）与 00 §0-4「M 线 29」及 §15 G2 门定义事实一致且自声明不改变 §5.2 效力；全文未出现任何新增 OR/C/D 条目或对 C1–C5/D0–D9 处置的改写 |
| A①-d | M0.1 输出要件七项逐项在案 | **通过** | G0 批准引用=§1、PRD 最终确认引用=§2、覆盖规则全文=§3（OR-1..OR-6 六条）、已确认决策表=§4（C1–C5+D7 附注）、决策门处置表=§5（D0–D9 十行）、执行仓库声明=§6、旧仓只读声明=§7，与 M0.1 步骤块「输出」括号清单一一对应 |
| B① | 验收命令亲自复跑 | **通过** | test -f 通过；grep -c peiligo_restart=5（≥1）；grep -q G0/R3_GPT_WAIVER/OR-1/C1/D9 五项全过（§4 命令 1–2 实测输出） |
| B② | §4.5 秘密扫描对 docs/ 零命中 | **通过** | 双分支单命令 `grep -rInE '…|dcap_…' docs/` 无任何输出、退出码 1（§4 命令 3） |
| B③ | PRD 微修订核查（除文档头「文档状态」一行外无任何其他变化） | **通过** | 当前 PRD 461 行=M00 审查（14:44）实测 461 行（行数零变化）；M00 §6-3 证明 14:44 时「待用户最终确认」仍在、PRD mtime 14:53；当前 L5 与 D0 §2.1 diff 记录的修订后文本字节一致、与备份（v1.4 修复前）的修订前文本字节一致；对备份全量 diff 仅 4 个变更块且全部位于 v1.3/v1.4 授权区（文件头表格/§18/§19/§24/§25），冻结区 §1–§17、§20–§23 零变更块；`find -newer M00.review.md` 足迹仅 PRD+D0 两文件（§4 命令 4、8–10） |
| B④ | D0 引用路径有效性 | **通过** | docs/PRODUCT_REQUIREMENTS.md、docs/reviews/M00.review.md、docs/plans/00_MASTER_PLAN.md、D0 自身共 4 个相对路径 test -f 全过；D0 §8-2 声明不存在的 M00.gpt-review.md 经测确实不存在（§4 命令 3 附带断言）；旧仓绝对路径仅作声明引用、按 OR-2 未对其执行任何命令 |
| B⑤ | 无 git 写操作痕迹 | **通过** | `test ! -e .git` 通过，仓库根仅有 docs/、.local_backups/、.DS_Store（§4 命令 3） |

## 4. 验收命令实际运行记录（审查者亲跑，2026-08-18，仓库根执行）

1. `test -f docs/decisions/D0_BASELINE_CONFIRMATION.md` → 通过（文件存在，114 行）。
2. `grep -c 'peiligo_restart' docs/decisions/D0_BASELINE_CONFIRMATION.md` → **5**（≥1 达标）；`for kw in 'G0' 'R3_GPT_WAIVER' 'OR-1' 'C1' 'D9'; do grep -q "$kw" …; done` → 五项全部命中（退出码 0）。
3. `grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/` → **零输出，退出码 1**（§4.5 通过判据达成）。附带：`test ! -e .git` 通过；4 个引用路径 test -f 全过；`test ! -f docs/reviews/M00.gpt-review.md` 通过（与 D0 §8-2「不存在 gpt-review.md」声明一致）。
4. `wc -l docs/PRODUCT_REQUIREMENTS.md` → **461**（与 M00.review.md §4-9 实测基线 461 一致，M0.1 触碰前后行数零变化）。
5. `diff <(sed -n '25,30p' docs/plans/00_MASTER_PLAN.md) <(sed -n '57,62p' docs/decisions/D0_BASELINE_CONFIRMATION.md)` → **空输出**（OR 六条逐字一致；两侧 md5 均 `081c62e1fc71b740b7715b5df46817cb`）。
6. `diff <(sed -n '35,44p' docs/plans/00_MASTER_PLAN.md) <(sed -n '69,78p' docs/decisions/D0_BASELINE_CONFIRMATION.md)` → **空输出**（C 表本体含"另："行逐字一致；两侧 md5 均 `4877151885da1751614aef39784d4cef`）。
7. `diff <(sed -n '47,59p' docs/plans/00_MASTER_PLAN.md) <(sed -n '85,97p' docs/decisions/D0_BASELINE_CONFIRMATION.md)` → **空输出**（D 表十行逐字一致；两侧 md5 均 `5dce38c48cab68cf684d0a4220ee71cf`）。（含标题整块 diff 仅示标题行 1 行差异=F-01。）
8. `diff -u .local_backups/governance-before-fix-20260818/PRODUCT_REQUIREMENTS.md docs/PRODUCT_REQUIREMENTS.md | grep -cE '^@@'` → **4 个变更块**，逐一核对内容：全部位于 文档头表格（L5 文档状态=M0.1 微修订、L7 更新日期/L9 项目仓库/L10 技术基座=v1.3/v1.4 授权修订）、§18、§19、§24（+1 行 GPT 可用性条款）、§25；冻结区 §1–§17、§20–§23 无任何变更块。备份 458 行 → 当前 461 行（+3 全部来自 v1.3/v1.4，M00 审查时点已为 461）。
9. `sed -n '5p' docs/PRODUCT_REQUIREMENTS.md` → `| 文档状态 | 产品需求基线（已最终确认：项目负责人 2026-08-18 批准，G0 通过） |`，与 D0 §2.1 diff 记录「修订后」文本及备份「修订前」文本（`产品需求基线草案（待用户最终确认）`）双向字节一致。
10. `find . -type f -newer docs/reviews/M00.review.md ! -name '.DS_Store'` → 仅 `./docs/PRODUCT_REQUIREMENTS.md`（14:53:26）与 `./docs/decisions/D0_BASELINE_CONFIRMATION.md`（14:54:58）两文件——M0.1 写入足迹与 D0 自述范围（新建 D0 + PRD 单行微修订）完全吻合，无第三文件被触碰。

## 5. 审查结论

# **通过**

（附 3 项 P2 观察记录——F-01 嵌入标题精度、F-02 派发授权仓库外事实、F-03 dispatch ID 不可仓库内验证——均不构成修改条件，仅登记备查；无 P0 阻断项、无 P1 批准前修复项。）

理由摘要：D0_BASELINE_CONFIRMATION.md 完整固化了 G0 四条件批准记录（含 R3_GPT_WAIVER 四要素与一次性不可复用语义）、PRD 最终确认引用、覆盖规则 OR-1..OR-6、已确认决策 C1–C5、决策门处置 D0–D9、执行仓库声明与旧仓只读声明——M0.1 步骤块要求的七项输出要件全部在案且与权威源逐字一致（三表本体 diff 空、md5 相等）；验收命令经审查者亲跑全绿（存在性、peiligo_restart 计数 5、五个关键字 grep 全命中），§4.5 秘密扫描对 docs/ 零命中，引用路径全部有效，无 .git 目录（git init 确未提前执行）；PRD 微修订经行数基线、mtime 序列、备份 diff 归因与写入足迹四重取证确认恰为声明的唯一一行，冻结区零触碰。M0.1 完成条件「验收绿 + 独立审查通过 + M0.2 可开工」三项均满足，M0.2 可以开工。

—— 审查完 ——
