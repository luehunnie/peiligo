# M00 独立审查报告（00_MASTER_PLAN v1.4 · G0 前最终稿）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | `docs/plans/00_MASTER_PLAN.md`（v1.4，2026-08-18，G0 前最终稿，1334 行） |
| 关联输入（均已完整读取） | `docs/PRODUCT_REQUIREMENTS.md`（PRD，461 行）；`docs/PEILIGO_REBUILD_AUDIT.md`（综合审计，208 行）；`docs/audit-report-old-peiligo.md`（旧仓审计，204 行）；`docs/audit-report-wagtail-upstream.md`（上游审计，219 行）；`docs/plans/01_PRODUCT_GOVERNANCE_PLAN.md`（519 行）；`docs/plans/02_ARCHITECTURE_DOMAIN_PLAN.md`（833 行）；`docs/plans/03_DELIVERY_OPERATIONS_PLAN.md`（769 行） |
| 审查任务 | M00-R3-REVIEW-v1.4（风险级 R3=00 §4.6 规则 7 缺省；GPT 压缩审查部分不在本任务内） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM-5.3 新会话（Orca Dispatch 派发） |
| 独立性声明 | 本审查者为全新会话，未参与 00/01/02/03、PRD 与三份审计报告的任何起草、修订或实现工作，与实现对话无上下文继承；审查全程只读除本报告外的一切文件 |
| 审查方式 | 双视角（A=计划 vs PRD/决策/结构一致性；B=修订落地/全局契约/可执行性/路径归属）；验收命令的静态可执行部分由审查者亲自运行并记录于 §4 |
| 本报告唯一写入文件 | `docs/reviews/M00.review.md`（本次新建 `docs/reviews/` 目录） |

## 2. 双视角发现清单

共 5 项发现，全部为 **P2（建议后续处理）**；**未发现 P0（阻断发布）或 P1（批准前修复）级别问题**。

### P2-01（B1-01/B4）· 文档头「必读输入」行数元数据陈旧

- **位置**：00 文档头第 9 行（必读输入表）。
- **描述**：v1.3/v1.4 对附件的横幅修订改变了行数，但文档头未同步刷新。
- **证据**：头部声明 `01…（560 行）…03…（761 行）…PRD…（457 行）`；实测 `wc -l`：PRD=461、01=519、02=833（一致）、03=769。三处失准（457→461、560→519、761→769）。
- **影响**：仅为编制期元数据，不影响执行语义（§23.1 覆盖矩阵与步骤引用均按文件路径而非行数工作）。
- **建议**：下一次 00 微修订（按 M0.6 变更管理流程）时刷新该行，或统一改为「行数以入库时 M0-2-BASELINE-MANIFEST 清单为准」。

### P2-02（A3-01/B1）· 01 的 PG-S4–S9 产物路径/文件名与 00 的 M0.3–M0.7 不一致，且无 §3.1 显式修订行

- **位置**：00 §5.2 第 249–253 行 vs 01 第 94、247–249、258–264、287–299 行（PG-S4/S5/S9 块）。
- **描述**：01 用 `docs/governance/AI_WORKFLOW.md`、`docs/governance/TASK_TEMPLATE.md`、`docs/governance/SECRETS_POLICY.md`、`docs/governance/CHANGE_MANAGEMENT.md`、`docs/governance/PHASE_GATES.md`、`docs/decisions/D8_AI_EVIDENCE_SCOPE.md`、`docs/decisions/GP-G2_GATE_PACKAGE.md`；00 的 M0.3–M0.7 对应为 `docs/ai/`、`docs/change/`、`docs/decisions/D8-EVIDENCE-POLICY.md`、`docs/decisions/G2-GOVERNANCE-PACKAGE.md`。§3.1 的 19 行修订清单无一显式覆盖这组改名。
- **证据**：00 第 249–253 行输出列；01 第 247 行（`docs/governance/AI_WORKFLOW.md`）、第 258–264 行（`test -f docs/governance/…` 验收命令——若按 01 字面执行将失败）。
- **影响**：已被 v1.4 的 FIX-07/FIX-10 横幅消解——01 三处横幅明确「步骤编号、仓库路径…以 00_MASTER_PLAN 为准（00 §3.0 六级层级）」，01 降为 Detail Specifications、其验收命令仅为参考；00 §5.2 输出列为唯一执行口径。故无执行歧义，但缺一行显式修订记录使追溯性打折。
- **建议**：G0 后随 M0.3 派发契约注明「01 §5 PG-S4–S9 的路径表述已被 00 §5.2 取代」；或在 00 下次微修订的 §3.1 追加一行登记该组差异。

### P2-03（A2-01/B1）· `DISPATCH_REGISTRY.md`（01 §3.3 派发登记表）在 00 中无落点

- **位置**：01 第 94 行（SCOPE 声明 + 总控 append-only 派发登记表）、第 247/249 行（PG-S4 产物三件套之一）；00 全文 `grep -c DISPATCH_REGISTRY` = **0**。
- **描述**：01 为关闭综合审计 §5「文件写入互斥未制度化」缺口设计的登记表产物，在 00 的任何步骤输出、§4 通用契约与 §3.1 修订清单中均未出现——该产物被静默丢弃，无显式处置记录。
- **影响评估**：**控制本身未丢失**——00 §4.4 已将互斥机制制度化（允许修改范围硬边界、共享文件清单与串行合并、并行文件集不相交、worktree+分支第二道防线），且 §4.1 定义总控（Orca 控制面）编排 Run/Task/Gate/Dispatch 并对仓库只读；丢失的仅是 01 设计的「仓库内可审计的派发登记产物」。M0.3 的 AI_WORKFLOW 章节要求「与本计划 §4 逐节对应」（00 第 367 行），而 §4 不含登记表机制，因此该产物不会随 M0.3 恢复。
- **建议**：二选一并留痕——① 在 M0.3 的派发契约中增补一句：AI_WORKFLOW 含「派发登记（SCOPE 台账）」章节，登记载体由 D8 裁决入库与否；② 在 00 下次微修订中显式登记「01 的 DISPATCH_REGISTRY 由 Orca 控制面派发记录等效承担，不再建仓库内登记表」。

### P2-04（A3-02）· 02 的 S3.4 依赖缺少 00 M3.4 已补的 S2.2 依赖（v1.1 修正确只落在 00）

- **位置**：02 §1.1 表 S3.4 行（依赖仅列 S3.2）与 §5 S3.4 块；00 §5.2 M3.4 行（依赖含 M2.2，v1.1 修订）。
- **描述**：00 v1.1 为 S7.2（非功能基线，消费 S2.2 的 SEO 位点）传递依赖而给 M3.4 补加了 M2.2；02 原文未同步。02 按其「输入」字段（引用 IA §6–§11）实际隐含了该依赖，且 FIX-07 横幅已声明依赖关系以 00 为准。
- **影响**：无执行影响（00 为 SSOT；02 降为明细附件）。
- **建议**：无需修改 02（OR-6 禁止改动附件）；在 00 下次微修订 §3.1 若补 P2-02 的行，可顺带注明此点已由横幅覆盖。

### P2-05（A4-01）· 55 步块中 14 块缺「单步骤对话边界」字段行、6 块缺「失败与回滚」字段行，与 §0 规则 3 的 12 项必填格式及 §23.3 自检声明存在形式偏差

- **位置**：§0 规则 3（第 18 行）；§23.3 自检（声明 55×12 字段齐备）；缺「单步骤对话边界」的 14 块 = M1.1–M1.6、M2.1、M2.2、M3.1–M3.4、M5.1、M5.2；缺「失败与回滚」的 6 块 = M1.1–M1.6。
- **证据**（审查者实测，命令见 §4-10）：12 个字段标签的全文计数为 阶段目标 56 / 前置依赖 55 / 输入 56 / 输出 55 / **单步骤对话边界 41** / 允许修改的文件范围 56 / 验收命令 56 / 测试要求 55 / 独立需求·架构审查 55 / 独立实现·测试·安全审查 55 / **失败与回滚 49** / 完成条件 55；按块归并后缺口如上（M0.x、M4.x、M6.x、M7.x、MB、MC 全部 12 项齐全，缺口集中在 M1–M3/M5 的明细块）。
- **影响评估**：**仅为形式偏差，无语义缺口**——§0 规则 3 第二句「§4 定义各字段的通用默认值，步骤块内只写步骤特定内容并引用默认条款」允许缺省吸收读法，且 §4.2 标题即「单步骤对话边界（默认，所有步骤适用）」、§4.8 标题即「通用失败与回滚（默认）」——两字段的默认值被 §4 明示为全局适用，任何派发者无论是否读到块内字段行都会得到同一执行语义。但 §23.3 声明「55 步 × 12 字段齐备」与实测不符，属自检表述失准。
- **建议**：G0 后随 00 下次微修订二选一：① 机械补齐 20 行（14 块补「单步骤对话边界：默认 §4.2」、6 块补「失败与回滚：默认 §4.8」）；② 将 §23.3 表述改为「10 项信息字段 55/55 齐备；对话边界与回滚两字段按 §0-3 缺省吸收条款由 §4.2/§4.8 全局默认覆盖」。

## 3. 逐项检查表（A1–A4 / B1–B4）

| 项 | 结论 | 一句话证据 |
| --- | --- | --- |
| A1 计划 vs PRD 冻结区 | **通过** | 逐域核对 PRD §1–§17 与 §20–§23：产品定位（校内非商业检索站）、四角色（§4）、五板块中文名（§6：校园纪事/校园活动/学习资料/软件与工具/校园指南，02 §0.2-7 同口径）、内容模型六类（§7.1–§7.7 逐字段落 M3.x/MB3–MB8 断言）、搜索维度（§10→M3.4/MB10）、权限语义（§5/§8→C3 容器树+T01–T16）、V1 In/Out（§20 全项在 00 附录防扩围对照与各步「不做」边界在场，`poc/` 迁入执行仓库属治理性修订 FIX-08 而非产品扩围）——未发现任何冻结区漂移或 OR-5 扩围。 |
| A2 决策基线一致性 | **通过** | §2.1 C1–C5 与 PRD §19（v1.4 修订后表述）逐项吻合且未被任何步骤重开（OR-4）；§2.2 D0–D9 十门全部处置且与综合审计 §8 一一对应、落点步骤全部存在（D0→G0+M0.1、D1→撤销〔修订 1/13〕、D2→C5/M1.4/G1、D3→C1/M1.2、D4→C2/M1.3、D5→C3/M1.5+M4.2/M4.3、D6→C4/M1.6+M3.1、D7→保留 M6.1–M6.3、D8→保留 M0.3 ask、D9→保留 §20/MC7）；唯一残留为 P2-03（DISPATCH_REGISTRY 无落点，控制已由 §4.4 等效承担）。 |
| A3 DAG 与覆盖完整性 | **通过** | §5.2 表 55 步（M 线 29=M0.x 7+M1–M7 22、MB 18、MC 8）零重复编号；表 ID 集与 §7–§18 明细块 ID 集排序后 `cmp` 完全一致（无孤儿、无幻影）；全部 55 行+4 门依赖逐条解析均落到既有步骤/门/外部输入（§20 外部输入、D 系列、学校 VI），手工遍历未见环；§23.1 覆盖矩阵与 01（PG-S1..S9：7 映射 M0.x、S2 删除、S7 并入 M4.1、G0 保留、G1 删除、G2 并入 G2）、02（S1.1–S7.2 一一对应 M1.1–M7.2）、03（S1–S18→MB1–MB18、S19–S23→MC1–MC8 含 S22→MC4+MC5、S23→MC6–MC8）三方源步骤清点 1:1 无遗漏；残留 P2-02/P2-04 均已被 FIX-07/FIX-10 横幅消解。 |
| A4 门定义与字段完备性 | **部分通过** | §6 五门+合并门与 §5 DAG、各步完成条件核验一致（G0=本审查+GPT/waiver+负责人书面批准+PRD 最终确认，G0 前仅 M0.1；G1=M1.6 后五 ADR Proposed→Accepted；G2=M7.2+M0.7 后、治理线 7 份审查记录；G3=MB18 后、18 份；G4=MC5=MC4 证据包+负责人 PRD §21 批准，批准前禁生产；合并门内嵌 §4.6 规则 6/7 口径与风险级评估一致）；扣分点为 P2-05——12 项必填字段在 20 个块位存在形式缺口、§23.3 自检声明与实测不符（语义由 §4.2/§4.8 全局默认全覆盖，无执行风险）。 |
| B1 v1.4 修订落地核查 | **通过** | §23.7 表恰 10 行（FIX-01..FIX-10）且逐项抽查落点属实：FIX-01 唯一仓库=peiligo_restart、旧仓操作命令块已从 01 PG-S2 删除（tombstone 在案）；FIX-02 §4.6 规则 6 与 PRD §24.3/01/03 横幅同文；FIX-03 M0.2 不预忽略 `docs/ai/`+M0.3 防死锁（00 第 348/360 行）；FIX-05 审查命名 `<步骤ID>.review.md`/`.gpt-review.md`（修订 11）在 00 §4.6-4、01 §3.4、02 横幅、03 §1.4 一致；FIX-06 双口径命名统一（01/02/03 横幅+步骤块改称「独立需求/架构审查」「独立实现/测试/安全审查」）；FIX-07 三附件均置「以 00 为准」横幅；FIX-08 `poc/permissions/`、`poc/search/` 位于 peiligo_restart 内（02 §0.2-6/附录 A），02 中 `peiligo_poc` 零命中，00 仅存 2 处自指性修订记录（§3.1 行 7、§23.7 FIX-08 行）属合法历史记载；FIX-09 §4.6 规则 7 逐 Task 评估+硬下限；FIX-10 三附件「第二控制面收敛」横幅在案——未发现 FIX 批次引入的新矛盾；残留 P2-01（行数元数据陈旧系附件修订的副产品）。 |
| B2 §4 全局契约内部一致性 | **通过** | §4.2 单步骤边界（一 Dispatch=一新对话=恰好一次 worker_done、返工新对话、ask 歧义处理、越界上报）与 §4.3 schema（subject/body 3 句/task-id+dispatch-id 双必填/outcome succeeded\|failed/files-modified/report-path）自洽且与 01 §3.2-5 同源、与 03 §1.1 模板一致；§4.4 互斥（硬边界+共享文件清单含 `docs/adr/README.md` 状态行+到达顺序串行合并+并行集不相交+M0.1 无 worktree 例外）无内部冲突；§4.5 v1.2 双分支正则（关键字分支+裸 `dcap_` 分支）经实跑零命中且规则文本不自匹配设计成立（本报告引用该正则后复扫仍零命中，见 §4-1b），v1.1 代理配置/transcript 禁区与生命周期命令「仅当次对话 TASK 注入原文」约束在案；§4.6 R0–R4 矩阵与规则 1–7（规则 6 GPT 可用性：R3 一次性 `R3_GPT_WAIVER` 四要素不可复用、R4 默认 BLOCKED；规则 7 逐 Task 评估+硬下限）与 PRD §24.3 v2.1 同口径；§4.7 测试分级 A–D 与各步「测试要求」字段引用一致；§4.9 授权矩阵与 03 §1.6、PRD §16 一致（G4 前生产禁令保留）。 |
| B3 验收命令可执行性 | **通过** | 静态可执行部分全部由审查者亲自运行并记录（§4 共 10 组：秘密扫描对 `docs/` 两次〔含本报告落盘后复扫〕均零命中；55 步计数与构成；表≡块集合同一性 cmp；FIX 行计数；旧仓路径语境核查；`peiligo_poc`/`DISPATCH_REGISTRY` 计数；行数实测；字段标签计数）——命令、期望与实际输出全部在案可复现。 |
| B4 路径与仓库归属 | **通过** | 全部可执行命令与产物路径以 peiligo_restart 为基准（§0-6）；旧仓绝对路径在 00/01/02/03 中共 5 处，逐处核对均为 OR-2 声明、Historical fact、tombstone 或已废止前置的陈述性语境，无一出现在任何可执行命令中（01 PG-S2 的旧仓命令块已按 FIX-01 整体删除并明令「不得在任何对话中恢复或执行」）；无任何对旧仓的写残留。 |

## 4. 实际验收命令运行记录（B3 证据，审查者亲跑）

以下命令均在 `/Users/chenjunxian/vscode_projects/peiligo_restart` 执行（macOS/BSD 工具链），2026-08-18。

1. **§4.5 秘密扫描（v1.2 双分支正则）——期望零命中**：
   ```bash
   ! grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
   ```
   - (a) 报告落盘前对 `docs/` 全量：**PASS——grep 退出码 1、无输出（零命中）**。
   - (b) 本报告写入后复扫（含本文件，验证引用正则文本不自匹配）：**PASS——零命中**。
   - 本报告全文不含任何令牌取值；涉生命周期命令仅以「路径+字段名/参数名」口径记载。
2. **步骤块计数与构成**：`grep -cE '^### (M[0-9]|MB[0-9]|MC[0-9])' docs/plans/00_MASTER_PLAN.md` → **55**；分线清点 M0.x=7、M1.1–M7.2=22、MB1–MB18=18、MC1–MC8=8，合计 55，与 §0-4「M 线 29 + MB 18 + MC 8」一致。
3. **表 ID 集合 vs 明细块 ID 集合**：分别抽取 §5.2 表 55 个 ID 与 §7–§18 明细块 55 个标题 ID，排序后 `cmp` → **完全一致**（IDENTICAL SETS）——无孤儿步骤、无幻影块。
4. **重复编号检查**：两份 ID 清单各自 `sort | uniq -d` → **空**（零重复）。
5. **依赖闭合**：对 §5.2 全部 55 行 + G1/G2/G3 门行的依赖列逐条解析，全部指向既有步骤 ID、门 ID 或登记在案的外部输入（§20 各项、D 系列、学校 VI）——**闭合，手工遍历未见环**。
6. **FIX 行计数**：`grep -cE '^\| FIX-' docs/plans/00_MASTER_PLAN.md` → **10**（FIX-01..FIX-10 与 §23.7 表一致）。
7. **旧仓绝对路径语境核查**：四份计划文件中该路径共 5 处命中，逐处上下文核对均为 OR-2 只读声明 / Historical fact / tombstone / 已废止（~~P2~~/~~PRE-2~~）陈述——**无可执行命令携带、无写语义**。
8. **`peiligo_poc` 与 `DISPATCH_REGISTRY` 计数**：00 中 `peiligo_poc` = 2 处，均为自指性修订记录文本（§3.1 行 7「PoC 目录迁入执行仓库」、§23.7 FIX-08 行），合法；`DISPATCH_REGISTRY` 在 00 中 = **0 处**（P2-03 证据）；02 中 `peiligo_poc` = 0 处（FIX-08 落地证据）。
9. **行数实测**：`wc -l` → PRD=461、综合审计=208、旧仓审计=204、上游审计=219（四处与文档头一致）、01=519、02=833（一致）、03=769；文档头第 9 行标注 PRD 457/01 560/03 761 → 三处失准（P2-01 证据）。
10. **12 字段标签计数**（P2-05 证据）：`grep -c '\*\*<字段>' 00_MASTER_PLAN.md` → 阶段目标 56 / 前置依赖 55 / 输入 56 / 输出 55 / 单步骤对话边界 41 / 允许修改的文件范围 56 / 验收命令 56 / 测试要求 55 / 独立需求·架构审查 55 / 独立实现·测试·安全审查 55 / 失败与回滚 49 / 完成条件 55；按块归并分析定位缺口块为 M1.1–M3.4/M5.1–M5.2（边界字段 14 块）与 M1.1–M1.6（回滚字段 6 块）。

## 5. 审查结论

# **通过**

（附 5 项 P2 建议，供 G0 后按 M0.6 变更管理流程吸收；无 P0 阻断项、无 P1 批准前修复项。）

理由摘要：00_MASTER_PLAN v1.4 作为唯一主计划，在 PRD 冻结区一致性（A1）、已确认决策与决策门处置（A2）、DAG/55 步/覆盖矩阵完整性（A3）、全局契约内部一致性与跨文档同源性（B2）、v1.4 十项修复的真实落地（B1）、路径与仓库归属（B4）六个核心维度全部经逐项核验成立；唯一的形式缺口（A4 之 P2-05：20 个块位缺两个纯默认字段行、§23.3 自检表述失准）因 §4.2/§4.8 全局默认的明示适用而不产生任何执行歧义，不构成批准条件。5 项 P2 均为元数据/追溯性/形式完备类改进，建议随 G0 后的首次微修订或对应步骤派发契约吸收，不影响 G0 判定。

## 6. G0 决策提示（致项目负责人）

1. **本报告满足 G0 条件 ①**（`docs/reviews/M00.review.md`，独立 GLM 新会话、双口径、R3 缺省级）。
2. **G0 条件 ②（R3 级 GPT 压缩 Risk/Architecture Review）不在本任务内且尚未执行**：按 00 §4.6 规则 6，GPT 不可用时不存在全局自动替代——您可对 M00 这一个具体 R3 Task 签发一次性 `R3_GPT_WAIVER`（记录 TASK / REASON / HUMAN_APPROVAL / DATE，不可复用、不可泛化）后继续，否则等待 GPT 可用并补落 `M00.gpt-review.md`。本审查未对 GPT 审查部分作任何替代性判断。
3. **G0 条件 ③④** 仍需您完成：书面批准（批准记录即 G0 记录，由 M0.1 固化）与 PRD 基线最终确认（清零 PRD 文档头「待用户最终确认」状态）。
4. 本审查报告（及如有的 gpt-review.md）当前为 Git 化前本地文件，按 §6 G0 行将随 **M0.2 文档基线入库**形成闭环；M0.2 独立审查需核对入库集合含本文件。
5. 5 项 P2 建议均不阻塞 G0；若您倾向于批准前清零，成本最低的路径是仅处理 P2-05②（改 §23.3 一句表述）与 P2-01（刷新一行行数），其余可延后。
6. G0 批准将解禁 OR-3 ①–④（初始化/依赖安装/Git 初始化/AI coding）；⑤ PVE/生产在 G4 前仍逐项授权（§4.9）。

—— 审查完 ——
