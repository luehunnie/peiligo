# M1.4 独立审查报告（ADR-0003 项目骨架：peiligo_restart 全新仓库干净 Wagtail 骨架）

## 1. 审查元信息

| 项 | 内容 |
| --- | --- |
| 审查对象 | M1.4 步骤全部产物：`docs/adr/0003-clean-wagtail-skeleton.md`（新建，91 行）+ `docs/adr/README.md` 0003 状态行（分支 `arch/m1.4-adr-0003` @ `ceff8e1`，worktree `.worktrees/m1.4` 检出，`pwd` 已确认） |
| 所属步骤 | M1.4 · ADR-0003 项目骨架（00_MASTER_PLAN §8 M1.4 步骤块；引源 02 §3 S1.4，语义按 00 §3 修订 8） |
| 风险级 | R3（ADR 属架构冻结线；派发令 RATIONALE 确认——GPT 压缩 Risk/Architecture Review 按 §4.6-6/FIX-02 留待 G1 门执行，不阻塞本审查） |
| 审查日期 | 2026-08-18 |
| 审查者 | 独立 GLM 新会话（Orca Dispatch 派发，全新会话、无实现上下文继承；实现者为另一会话，commit `ceff8e1`） |
| 独立性声明 | 本审查者未参与 M1.4 实现；未读取实现 transcript、不信实现者 worker_done 自述（全部结论以亲自复跑/直读为准）；未读取 01/03/PRD 任何全文；除本报告外一切文件与 git 状态只读（仅运行只读 git/rg/sed/grep/test/ls 取证），未执行任何 git 写操作。读取范围：00 §8 M1.4 步骤块与 SE-A 断言定义表（§8 引言）、§2.1 C5 行、§2.2 D2 行、§1 OR-1/OR-2、§3 修订 2/3/8 行、§4.5、§4.6；02 §3 S1.4 区块（L244–262，转述源核对）；三份审计报告被引节号（§6/§4/§1/§8，仅被引区间，非全文）；`docs/adr/0003-clean-wagtail-skeleton.md`、`docs/adr/README.md`、`docs/adr/_template.md` 全文 |
| 基线说明 | 本分支基于 `rebuild/v1@fb01af5`，`rebuild/v1` 此后已前进（派发令已预告，属预期）；`git diff rebuild/v1...HEAD`（三点，自 merge-base 起算）实测仅含 M1.4 提交两文件，未混入 rebuild/v1 新前进内容 |
| 权威对照源 | 00_MASTER_PLAN §8 M1.4 步骤块（验收命令原文）、§8 SE-A 断言定义表（v1.2，SE-A04 行）、§2.1（C5 冻结决策）、§2.2（D2 处置行）、§1（OR-1/OR-2）、§3（修订 2/3/8/15）、§4.5（秘密扫描正则）、§4.6（独立审查规则） |

## 2. 结论

**通过**。无 P0/P1/P2 项；3 项 P3 观察记录于 §5（均不构成修改条件）。派发令六项检查全部 PASS（§3）；实现者"审计证据经 02 §3 S1.4 转述"的处理经审查者直读审计原文闭合验证，判定可接受（§3.6）。M1.4 完成条件「验收绿 + 独立审查通过」满足；G1 门（D2 正式裁决、转 Accepted、R3 补 GPT 压缩审查）按计划保留。

## 3. 逐项核对证据（派发令清单 1–6）

### 3.1 清单 1 · 验收命令复跑（B 口径）— PASS

审查者于 worktree 根亲自复跑 00 §8 M1.4 步骤块验收命令原文：

```
$ test -f docs/adr/0003-clean-wagtail-skeleton.md && grep -q 'peiligo_restart' docs/adr/0003-clean-wagtail-skeleton.md \
    && grep -q 'Proposed' docs/adr/0003-clean-wagtail-skeleton.md && grep -q 'wagtail start' docs/adr/0003-clean-wagtail-skeleton.md
$ echo $?
0
```

四条件分解核对：文件存在（新建 91 行）；`peiligo_restart` 命中（标题、L44 决策第 1 条等）；`Proposed` 命中（L7 状态字段头）；`wagtail start` 命中（L44"骨架以 `wagtail start` 起步"、L76 替代方案 3）。验收绿。

步骤块另两要求：「不得出现任何旧仓写操作安排」→ §3.3 核对通过；「§4.5 零命中」→ §3.5 核对通过。

### 3.2 清单 2 · SE-A04 断言（A 口径）— PASS

SE-A04 唯一定义源（00 §8 引言断言定义表 v1.2，L444）：**「ADR-0003 以 Proposed 固化"peiligo_restart 全新仓库 + `wagtail start` 干净骨架、不继承旧仓任何 Git 历史/分支/工作区"，全文无任何旧仓写操作安排」**。逐要件核对：

| SE-A04 要件 | ADR 实测 | 判定 |
| --- | --- | --- |
| 以 Proposed 固化 | L7 `\| 状态 \| Proposed \|`；L10 决策人注记明确"以 Proposed 落盘，待 G1 转 Accepted，代理不得代决"（FIX-04 状态模型，与 `docs/adr/README.md` §4 流转权限一致） | ✓ |
| peiligo_restart 全新仓库 | L44 第 1 条："唯一执行仓库为**全新仓库** `/Users/chenjunxian/vscode_projects/peiligo_restart`（OR-1）" | ✓ |
| `wagtail start` 干净骨架 | L44 第 1 条："骨架以 **`wagtail start` 起步**生成干净骨架" | ✓ |
| 不继承旧仓任何 Git 历史/分支/工作区 | L45 第 2 条：不继承"任何 Git 历史、分支或工作区（含 `feat/m4-admin-auth` 工作区现状等一切旧仓状态）；新旧仓库之间不建立任何 Git 级联关系"；fork-and-prune 替代方案（L75）亦以此明文否决 | ✓ |
| 全文无任何旧仓写操作安排 | §3.3 全文扫描核对 | ✓ |

另核对 C5 逐字引用固化（模板"决策"节硬要求）：00 §2.1 L42 C5 行 =「项目骨架：`peiligo_restart` 全新仓库干净 Wagtail 骨架（由 OR-1 派发指示确认；不继承旧仓任何 Git 历史/分支/工作区）」；ADR L40 引用块与该行逐字一致（仅按引用格式追加句号），无语义改写。✓

### 3.3 清单 3 · OR-1/OR-2 一致性（A 口径）— PASS

00 §1 原文：OR-1（L25）唯一项目位置 = `peiligo_restart`，一切 Git 仓库/worktree/分支/产物均且仅位于其内；OR-2（L26）旧仓 `peiligo` 及全部 worktree 永久只读，任何步骤不得安排 tag/branch/commit、文档迁入旧仓、在旧仓开发或任何写操作。

`grep -n '旧仓'` 全文枚举 ADR 0003 全部 13 处旧仓表述，逐条分类：

- **只读/复用语境**（L46"永久只读，仅可用于审计与选择性复用……从旧仓只读读取后向新仓移植"、L56"按综合审计 §3 清单只读取用"）——2 处；
- **否定/排除语境**（L23"不建只读标签或归档分支"、L24 PRE-2 已废止、L40 逐字引用内"不继承"、L45 不继承/不建级联、L54 不受旧仓状态影响/不阻塞、L61–62 复用走选择性通道/未列入资产不进新仓、L74–75 两替代方案以"与 OR-2 冲突"或"违反 C5 明文"否决、L83 合规表"无任何针对旧仓的写操作、分支/标签/提交、文档迁入或开发安排"声明）——11 处；
- **写操作安排语境**——0 处。

正则复核：`grep -nE '(旧仓|peiligo[^_]).{0,40}(tag|branch|commit|迁入|开发|写入|push|merge)|(tag|branch|commit|迁入|开发|写入|push|merge).{0,40}旧仓'` 全文唯一命中 = L83，即合规表中对写操作的**否定声明本身**，非操作安排。OR-1 侧：全文唯一仓库主体 = `peiligo_restart`（OR-1 路径原文），无任何其他仓库/worktree 安排（L44"唯一执行仓库"）。与 OR-1/OR-2 一致，无旧仓残留语义。

### 3.4 清单 4 · 模板五节结构 + status Proposed + D2 定位 — PASS

`rg -n '^## '` 实测 ADR 0003 节结构对照 `docs/adr/_template.md`（全项目唯一模板，修订 5）：

| 模板要求（SE-A01 序） | ADR 0003 实测行号 | 判定 |
| --- | --- | --- |
| 状态字段头（编号/日期/状态/关联决策，四字段不得增删改名） | L3–L10（编号 0003、日期 2026-08-18、状态 Proposed、关联决策 C5/D2）+ 决策人注记 | ✓ |
| 1. 背景 | L12（含触发事件、问题、证据 5 条、约束、可追溯性表——证据引用在本节给出，合模板要求） | ✓ |
| 2. 决策 | L36（C5 逐字引用 + 5 条可检验肯定句与边界） | ✓ |
| 3. 后果 | L50（正面收益/代价权衡/受影响步骤，**复核时点单列一行** L70=G1 门——合模板单列要求） | ✓ |
| 4. 替代方案 | L72（3 项：路径 A 原结构重构 / fork-and-prune / 手工搭建，各含方案→否决理由→遗留风险：无） | ✓ |
| 5. 合规性 | L78（OR-1/OR-2、C1–C5、PRD §18/§19/§20、不改 Wagtail 核心、秘密纪律逐项核对，结论"不违反任何一条，无豁免申请"） | ✓ |

五节齐全、序一致、无增删改名（"可追溯性表"为背景节内表格，非新增节）。状态 = Proposed ✓。

D2 定位三方一致性：00 §2.2 L52 D2 行「已确认 = C5（ADR-0003 按修订 8 语义，G1 批准转 Accepted）」↔ ADR L8「关联决策 C5（= 决策门 D2 正式裁决记录）」+ L57「决策门 D2 以本 ADR 为正式裁决记录」↔ README §2 L30 0003 行「转 Accepted 门 = G1（=D2 正式裁决记录）」。定位正确。日期一致性：ADR 首落盘日期 2026-08-18 = README 0003 行日期列（本次更新值）= 审查当日 ✓。

### 3.5 清单 5 · diff 边界 + README 单行 + §4.5 零命中（B 口径）— PASS

```
$ git diff --name-only rebuild/v1...HEAD
docs/adr/0003-clean-wagtail-skeleton.md
docs/adr/README.md
$ git diff --stat rebuild/v1...HEAD
 docs/adr/0003-clean-wagtail-skeleton.md | 91 ++++++++ (新建)
 docs/adr/README.md                      |  2 +- (1 行变更)
```

diff 恰两文件，与 00 §8 M1.4「允许修改的文件范围：该 ADR 文件 + `docs/adr/README.md` 状态行」一致，无越界。README 唯一变更行 = §2 索引 0003 行日期列 `—（待落盘）` → `2026-08-18`（`git diff` 实测仅此一行，状态列 Proposed 未动、其余五行未动）；§3 看板无需变更（0003 落盘前即为 Proposed 占位，计数不变）；ADR 状态字段头与索引行两处状态/日期一致（README §5 双处一致要求满足）。工作树干净（`git status --porcelain` 空）。

§4.5 扫描（正则原文取自 00 §4.5 L139）：

```
$ grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
（无输出）
$ echo $?
1        # 全 docs/ 零命中
$ 同正则扫描 docs/adr/0003-clean-wagtail-skeleton.md docs/adr/README.md
（无输出，退出码 1）  # 两变更文件零命中
```

§4.5 零命中 ✓（扫描对象按扫描契约取 `docs/` 全部入库文件）。ADR 仅含仓库绝对路径（00 路径基准 v1.1 要求的必写项，非秘密），与 ADR 自身合规表 L89 声明一致。

### 3.6 清单 6 · 实现者"审计证据经 02 §3 S1.4 转述"的可接受性 — 可接受（PASS）

**事实**：ADR L18/L34 两处显式注明证据"经 02 §3 S1.4 汇总转述两审计骨架比较章节，逐条可独立追溯"，未声称直读审计原文。实现者自述与文内标注一致（本审查不信自述，但以文内标注为核对对象）。

**审查者直读闭合验证**（审查者直读了被引审计原文区间，非转述）：

| ADR 转述证据 | 被引原文实测 | 判定 |
| --- | --- | --- |
| ① 无共享栈（旧站审计 §6） | §6「路径比较」L138：路径 A = "旧结构（Vue3+FastAPI）内重构"，"需自建（Wagtail 上游现成提供）：部门岗位账号+对象级权限……全部从零"；§6 内并见栈差异与"重开 PRD 技术基线"表述 | 实质一致 ✓ |
| ② 覆盖率趋近零（上游审计 §4） | §4 L136 落地建议："旧前端资产仅静态/模板层可评估复用"；旧站审计 §6 L141："现有可保留资产主要只剩前端展示层与后端测试范式；编辑侧/治理侧无论如何都是绿地" | 实质一致 ✓（措辞观察见 §5 Obs-1） |
| ③ 无真实数据迁移负担（综合审计 §1/§8） | §1 总结结论 1："无真实业务数据，强行在旧结构中演进不会显著减少 V1 工作量"；§8 D2 决策门行存在且议题相符 | 实质一致 ✓ |
| ④ app 拆分建议 = `notices`/`resources`/`guides`/`departments`/`frontend`（上游审计 §4） | §4 L135 逐字含该五 app 清单（"按域拆 app，例如：`notices`……`departments`……`frontend`"） | 逐字一致 ✓ |
| ⑤ 路径 A 否决理由"重开技术基线"（旧站审计 §6） | §6 L140："选 A 等于项目负责人重开 PRD 技术基线" | 逐字一致 ✓ |
| 复用范围 = 综合审计 §3 清单 | §3「可复用清单」节存在（L46） | 可追溯 ✓ |

**判定：可接受，无需返工**。理由：其一，ADR 文内如实标注转述性质与双出链（02 §3 S1.4 + 审计原文路径与节号），满足模板"依据与证据引用……逐条列出、可独立追溯"要求；其二，转述源 02 §3 S1.4 区块（L244–262）实测含 ADR 所载全部五项证据要素，转述无遗漏；其三，本次独立审查直读被引原文，全部转述与原文实质一致（两条逐字、三条实质），转述链已闭合，且引用节号全部真实存在、无虚引。审计原文属 `docs/` 基线文件（M0.2 字节级保真入库），可追溯性由 Git 基线保障。

## 4. 风险建议

1. **G1 门待办（R3 尾项）**：按 §4.6-6（FIX-02），R3 的 GPT 压缩 Risk/Architecture Review 正式裁决留待 G1 架构冻结门；本 ADR 的最终冻结（Proposed → Accepted，决策人 = 项目负责人）与 GPT 审查均在该门执行。G1 门清单须确认两项均已闭环，缺一不得转 Accepted。
2. **修订 8 语义正确落地**：审计原文与 02 §3 汇总行（02 L68）均含"同仓库干净骨架"旧语义（修订 8 前措辞）；ADR 全文（标题至决策第 1/2 条、fork-and-prune 否决）正确采用修订 8 覆盖后的"peiligo_restart 全新仓库、不继承任何 Git 历史"新语义，未继承转述链上游的旧措辞。后续从 02/审计取材的步骤（M1.5/M1.6）应延续此做法——凡遇"同仓库"类字样一律以 00 §3 修订清单为准（OR-6）。
3. **无返工条件**：六项检查全 PASS，无 P0–P2；本报告不设修改条件，M1.4 可并入主线。

## 5. P3 观察项（不构成修改条件）

| # | 级 | 观察 | 处置建议 |
| --- | --- | --- | --- |
| Obs-1 | P3 | L21"V1 功能对旧实现的功能覆盖率趋近零"系继承 02 §3 S1.4 的压缩措辞，字面主客方向（"V1 覆盖旧实现" vs "旧实现覆盖 V1"）有歧义空间；审计原文实质表述为"可保留资产仅前端展示层/静态模板层，编辑侧治理侧绿地"。ADR 已以破折号补语"旧实现几乎无可直接沿用的功能模块"消歧，语义无实质偏差 | 无需改动；G1 批准人复核时可顺带确认该句读法 |
| Obs-2 | P3 | 实现者以 02 §3 S1.4 为证据转述源、未直读审计原文的做法，本次经审查者直读闭合后判定可接受；该模式（文内注明转述 + 独立审查直读闭合）是双保险而非缺陷 | 后续 ADR（0004/0005）沿用证据转述时保持同模式：文内必须注明转述性质并给出原文路径+节号 |
| Obs-3 | P3 | 02 §3 S1.4 汇总行（02 L68）自身仍含"同仓库干净骨架"旧措辞（已被 00 修订 8 覆盖但 02 不可改，OR-6）；本次实现正确未采信 | 记录在案，提示后续步骤从 02 取材时始终以 00 修订清单为最终语义基准 |

**审查结论：通过。** M1.4 完成条件（验收绿 + 独立审查通过）满足；G1 门保留 D2 正式裁决、Accepted 转换与 R3 GPT 压缩审查。
