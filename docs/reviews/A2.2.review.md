# review(A2.2)：M2.2 前台 IA 实现独立审查——PASS

| 项 | 值 |
| --- | --- |
| 审查对象 | commit `18c8e31`（分支 `arch/a2.2-frontend-ia`，基线 `rebuild/v1@b7ed699`，merge-base 亲核 = b7ed699） |
| 审查方式 | 全新 Session 独立复跑：自建 `.venv-review`（pyenv CPython 3.13.11 + requirements-dev.txt 钉版安装 + `pip install -e .`，`peiligo.__file__` 亲证指向本 worktree src/，未复用任何兄弟 worktree 环境）；全部验收命令、运行时表面与断言亲自执行取证，不采信实现者自述 |
| 环境 | `.env` 复制自 ../a2.1-impl（不入库，gitignore 亲证）；PG socket /tmp:5432；DB 仅 peiligo_restart_dev/peiligo_restart_test（名称经 settings 解析亲证，凭据未读取） |
| 日期 | 2026-08-18 |
| 结论 | **PASS**（0×P2 / 0×P3 / 6×P4） |

## 1. 必跑六项（全部亲跑，退出码取证）

| 命令 | 结果 |
| --- | --- |
| `manage.py check` | exit 0；5 条 treebeard.E001 WARNING（钉版基线既有，A2.1 起口径不变，非本批引入） |
| `manage.py makemigrations --check --dry-run` | `No changes detected`，exit 0（模型-迁移零漂移） |
| `manage.py migrate` | `No migrations to apply`（实现者已在本 dev 库应用 0004）；二次复跑同输出（幂等），exit 0 |
| `pytest -q` | **65 passed, 50 subtests passed in 12.11s**；collect-only 逐文件对账：ia_frontend 30（全新增）+ ia_structure 21（原 20，移除空真守护 1、新增发射器/模型级排除 2，净 +1）+ settings 8 + home 4 + smoke 2 = 65；基线 34 零回归 |
| `ruff check .` | `All checks passed!` exit 0 |
| `ruff format --check .` | `69 files already formatted` exit 0（65→69 净 +4：context_processors.py、test_ia_frontend.py 等；migrations 按配置排除） |

## 2. 必验逐项（runserver 127.0.0.1:8765 亲测 + 进程内 DEBUG=False 404 复核 + dev 库直查）

**首页 200 含五板块入口**：`/` → 200；h1=`首页`（迁移 0004 已中文化种子标题）；`<ul class="section-grid">` 恰五项且顺序=chronicle/events/materials/software/guide，中文标题逐字=校园纪事/校园活动/学习资料/软件与工具/校园指南；`<title>` = 首页；无面包屑（IA §6.3）；无 robots meta（IA-10 可收录）。

**五板块 200 且 slug 正确**：五路径全 200；dev 库直查 depth-3 子页恰五个、全为 SectionPage、无多余节点；sitemap.xml 恰 6 loc（首页+五板块）。板块页亲证：面包屑=首页（可点链接）>板块名（纯文本 span aria-current）、h1=板块名、全空态"本板块暂无内容"+其余四板块+返回首页引导、无参数态无 noindex/canonical。

**通用 404**：`/no-such-page/`、`/chronicle/jwc/`、`/guide/lib/` 全 404。runserver DEBUG=True 显示技术页（Django 标准行为，README 已注明）；本项目 404.html 渲染由**进程内 override_settings(DEBUG=False) 亲证**：三路径均输出 h1"页面不存在"+站内搜索+五板块入口，零容器标题、零部门段引导（body 中"部门"仅出现于页脚静态文案"内容由各部门经站点后台发布"，非部门化引导）；pytest `assertTemplateUsed("404.html")` 同证。

**Container 直达 404 且四路排除**：URL 级双样本 404（上）；**导航**——base.html 一级导航由 `site_chrome` context processor 消费 `home.models.SECTIONS` 冻结常量、零页面树查询，容器无进入通道（模板审查+HTML 亲证：七项外零链接）；**列表**——板块页列表区当前=全空态，容器非列表项（pytest：容器 live 在场时板块页 NotContains 容器标题）；**sitemap**——`DepartmentContainerPage.get_sitemap_urls→[]` 整类排除（pytest：容器在场 sitemap 恰 6 loc 零容器 URL；模型级单测亲复跑）；**搜索**——A2.1 的 `search_fields=[]` 索引级排除延续未动（pytest test_ia04 双测在 65 内绿），dev 库零容器、零假部门数据。

**/search/ 200+noindex**：无参/带参均 200；`<meta name="robots" content="noindex">` + `<link rel="canonical" href="…/search/">` 亲证在 HTML（IA §10 #7 含无参数态）；面包屑=首页>站内搜索；表单显式 label（`label for="id_query"`）+ `role="search"`；无结果时=筛选无匹配态（"没有符合当前条件的内容"+"清除搜索条件"），与板块全空态文案互斥（IA §11/IA-14）。

**面包屑三规则**：首页无（HTML 亲证零 `aria-label="面包屑"`）；板块=首页>板块（上）；容器永不入——全模板唯一面包屑 partial 不含容器来源，容器 serve 404 不可达（pytest 断言+代码审查双证）；搜索页=首页>站内搜索（§6.3 全站搜索行）。

**首页无假数据仅扩展位**：`<main>` 内 grep 紧急/通知/活动/推荐/FeaturedItem 关键词零命中；层 1–3 以注释冻结扩展位与插入顺序（①紧急提示②推荐位③最新通知④近期活动→网格），零查询零渲染；dev 库零容器零业务内容页。

**纯 SSR 无 SPA**：frontend/ 仅空包标记；无 package.json/构建链；static/js/peiligo.js=0 字节；三表面 HTML 零 fetch/XMLHttpRequest/Vue/react；全部状态经 GET URL 服务端渲染（ADR-0002）。

**基础无障碍**：`<html lang="zh-hans">`；skip-link 为 body 首个链接（匿名态亲证）且锚点 `#main-content` 真实存在；header/nav/main/footer landmark；nav aria-label="全局导航"；每表面恰一 h1；当前导航项 aria-current（首页/板块/搜索四态 pytest 逐一验证）；`:focus-visible` 轮廓样式；搜索表单 label+`type="search"`。

**无旧名称**：院内逸事/软件资源/学院指南于 *.py/*.html/*.js/*.css 零命中（唯一命中=tests/test_ia_structure.py:37 禁复入断言字面量本身，合法）；docs/ 命中均为治理文档的禁复入引文。

## 3. IA-06..14 逐条核对（当前可验证部分）

| 断言 | 当前可验证部分 | 证据 | 结论 |
| --- | --- | --- | --- |
| IA-06 导航仅七类+列表无容器 | 四公开表面 nav 恰=首页+五板块（§2 顺序）+站内搜索；容器 live 在场全表面（含 404）零 `/<板块>/<部门>/` 链接；板块页零容器标题 | pytest 3 测+HTML 亲证+processor 代码审查（零 DB 查询） | ✅ |
| IA-07 面包屑一致性 | 首页无；板块=首页（可点→200）>板块（纯文本+aria-current）；容器段永不出现 | pytest 4 测+runserver 亲证 | ✅ |
| IA-08 筛选态标题 | **延期合规**：无筛选实现、无任何"主页/门户/空间/专区"措辞输出 | grep 零命中；延后 MB10 | ✅（未提前硬实现） |
| IA-09 单篇 noindex | **延期合规**：无内容模型字段 | grep noindex 仅模板 meta+测试；延后 M3.2/MB13 | ✅（未提前硬实现） |
| IA-10 默认可收录+入 sitemap | 首页+五板块无 robots meta；sitemap 恰 6 loc 全为 200 基础 URL | pytest 2 测+curl 亲证 | ✅ |
| IA-11 参数可选/非法回退 | 板块页与 /search/ 携任意/未知参数全 200（含 `?q=&dept=zzz&type=notice&tag=y`、`?section=bogus`），不 404/5xx；状态全在 URL、无 JS 依赖 | curl 三样本+代码审查；五参数规格本体延后 MB10 | ✅（当前部分） |
| IA-12 noindex+canonical | /search/ 无参/带参 noindex+canonical；板块 query 态 noindex+canonical 指向去参基础 URL；sitemap 零 query URL；跳转页未建（MB12，无提前实现） | pytest 4 测+curl 亲证（`/chronicle/?dept=jwc`→canonical=`/chronicle/`） | ✅ |
| IA-13 首页四层顺序 | 层 4 网格：恰五项、冻结顺序、链接全 200；层 1–3 扩展位注释按 ①→④ 顺序冻结、零渲染零假数据 | pytest 3 测+HTML 亲证；数据区延后 M3/MB8/MB9 | ✅（当前部分） |
| IA-14 四态 | 全空态（板块无参数态：文案+其余四板块+首页引导）与筛选无匹配态（搜索带参零结果：文案+清除动作）文案动作区分且互斥；载入中/出错+重试在纯 SSR 无客户端承载（实现者注释文档化，服务端异常=Django 500 页；终判 MB9/MB10，见 P4-2） | pytest 3 测+curl 亲证 | ✅（当前部分） |

## 4. 延期项未提前硬实现（逐项 grep+代码审查）

最终内容类型/EventFieldsMixin：六名（NoticePage/ArticlePage/MaterialPage/SoftwareToolPage/GuidePage/EventFieldsMixin）全仓**零定义零导入**（唯一代码命中=departments/models.py:22 前瞻注释，A2.1 已知悉延续，见 P4-4）。Department Snippet 最终模型：零定义（无任何 Snippet 模型）。dept·type·tag 正式筛选：零筛选逻辑（板块页仅按 §10 #6 输出 noindex+canonical，语义过滤未实现）。筛选结果标题：零实现。内容 noindex 字段：零模型字段。内容 sitemap 规则（单篇退出）：未实现（sitemap 仅首页+板块，无内容可规则）。首页真实查询（层 1–3）：零查询零渲染。FeaturedItem：仅模板注释扩展位引用，零建模。完整搜索（五参数）：未实现，/search/ 保持脚手架 query 参数基础页。发布归档/权限矩阵：零相关代码（go_live_at/expire_at/Group/Permission 业务目录零命中口径延续 A1 审查）。

## 5. 秘密扫描与数据红线

- **00 §4.5 v1.2 秘密扫描**（docs/ 双分支正则原文复跑）：**零命中**（raw grep rc=1 且无输出）。
- **DB 红线**：连接仅 peiligo_restart_dev（migrate/bootstrap/runserver/shell）与 peiligo_restart_test（pytest 建删，结束即不存在）；名称经 settings 解析亲证，凭据未读取。旧库 peiligo_dev（owner=peiligo_dev_user，8054 kB，3 表）与 peiligo_test（owner=peiligo_test_user，8134 kB，3 表）**t0/t1/t2 三时点**属主/尺寸/表数完全一致，零触碰。全仓（含 migrations）零 DROP authored（`grep -rw DROP` 零命中）。
- **bootstrap_sections 幂等复跑**：新建 0，跳过 5，五板块就位 5/5（dev 库不引入假数据）。
- **本审查零改动**：除本报告文件外 git status 零变更（.venv-review 为未跟踪会话工件，不入库）。

## 6. 发现清单（分级）

| # | 级 | 发现 | 处置建议 |
| --- | --- | --- | --- |
| 1 | P4 | base.html `request.GET` 触发的 noindex+canonical 泛化到**全部**页面（含 `/` 带任意参数）：超出 §10 #6 字面（仅板块筛选态），但与 §10.1"任一参数态不构成独立收录单元"理据一致，且仅影响带参访问（默认态不受限），无实害 | MB13 落地 §10 清单时随口径复核（是否豁免 utm 类跟踪参数等属 B 阶段决策） |
| 2 | P4 | IA-14"载入中/出错+重试"两态在纯 SSR 下无客户端请求过程可承载：实现者以模板注释+测试文档化该映射（服务端异常=Django 500 页）；规格源为旧站 SPA 审计（§11 引言），SSR 化解释合理但未经 MB9/MB10 终判 | MB9/MB10 验收时对四态的 SSR 等效口径显式留痕 |
| 3 | P4 | /search/ 沿用脚手架参数名 `query`；§9 目标规格为 `q/section/dept/type/tag` 五参数（实现位=MB10）——届时存在参数名迁移/兼容事项（当前文档已注明边界） | MB10 实施时处理（含旧链接 301 或双参兼容） |
| 4 | P4 | departments/models.py:22 前瞻注释含 M3 六名中 5 名（零定义零导入；A2.1 审查已登记的同款知悉项延续） | M3 落地时注释随白名单实际化 |
| 5 | P4 | `{% wagtailuserbar %}` 在 DOM 中先于 skip-link：登录员工访问时首个可聚焦元素为 userbar 链接；**匿名公开访客**（PRD §13 面向学生）首链接=skip-link 亲证不受影响 | 知悉项；如需可在 B 线模板调整标签顺序 |
| 6 | P4 | sitemap `<loc>` 域名取 Wagtail Site 记录（dev= http://localhost/…）：上线前须核对 Site hostname/端口与正式域名一致，否则 sitemap 收录 URL 全量错域 | 部署清单登记（B 线/交付线） |

（另有实现者自报并经亲证属实的环境知悉项：`LANGUAGE_CODE` 仍 en-us 前台已 lang=zh-hans 覆盖、FEEDBACK_EMAIL 哑值默认、品牌名取 WAGTAIL_SITE_NAME="peiligo"——均属 B 线/配置事项，不计发现。）

## 7. 结论

A2.2（M2.2 前台 IA 实现）**PASS**：必跑六项全绿（65=基线 34 零回归+新增 31，对账闭合）；IA §6–§11+§13 中不依赖 M3 的位点全部按冻结语义落地且经独立运行时取证；延期项 12 类逐一核实未提前硬实现；§4.5 v1.2 零命中；旧库三时点零触碰；除本报告外零改动。0×P2/0×P3/6×P4（全部为口径泛化/延后交接/部署知悉类观察项，无一影响本步验收判据）。
