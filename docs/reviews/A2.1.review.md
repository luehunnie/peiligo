# review(A2.1)：M2.1 结构实现独立审查——PASS

| 项 | 值 |
| --- | --- |
| 审查对象 | commit `c33d99e`（分支 `arch/a2.1-structure`，基线 `rebuild/v1@035c5ab`，merge-base 亲核 = 035c5ab） |
| 审查方式 | 全新 Session 独立复跑：自建 .venv-review（pyenv CPython 3.13.11 + requirements*.txt 钉版安装 + `pip install -e .`，非复用实现者环境），全部验收命令与断言亲自执行取证 |
| 环境 | `.env` 复制自 ../a2.1-impl（不入库，gitignore 亲证）；PG socket /tmp:5432 |
| 日期 | 2026-08-18 |
| 结论 | **PASS**（0×P2 / 0×P3 / 4×P4） |

## 1. 必跑六项（全部亲跑，退出码取证）

| 命令 | 结果 |
| --- | --- |
| `manage.py check` | exit 0；5 条 treebeard.E001（M1 基线 3 + 新增 SectionPage/DepartmentContainerPage 各 1，README 已同步说明 3→5） |
| `manage.py makemigrations --check --dry-run` | `No changes detected`，exit 0（模型-迁移零漂移） |
| `manage.py migrate` | `No migrations to apply`；二次复跑同输出（幂等），exit 0 |
| `pytest -q` | **34 passed, 8 subtests passed**（复跑第二遍同结果）；collect-only 逐节点核对：home 4 + ia_structure 20 + smoke 2 + settings 8 = 34，基线 14 零回归 + 新增 20 |
| `ruff check .` | `All checks passed!` exit 0 |
| `ruff format --check .` | `65 files already formatted` exit 0 |

ruff 计数口径亲查（65 = 28 py（migrations 按配置排除）+ 36 docs md + README.md；ruff 0.16.3 将 md 代码块纳入 format）。实现者报告"64"系其在写入 A2.1.impl.md 前运行（36 md 当时为 35），非差异。`.venv-review`（4295 个 py）对 ruff 发现集零污染——隐藏目录默认跳过，改名实验复核。

## 2. 结构断言逐项验证（runserver 127.0.0.1:8791 亲测 + pytest 复跑 + dev 库直查）

**首页**：`/` → 200，`Welcome to your new Wagtail site!`（M1 欢迎页原样，零假业务内容）。

**五板块（IA-01/IA-05）**：`/chronicle/ /events/ /materials/ /software/ /guide/` 各 200；h1 依次=校园纪事/校园活动/学习资料/软件与工具/校园指南；body_class=template-section（结构壳模板生效）。dev 库直查：默认站点根=HomePage（pk 3），子页恰 5 个且全为 SectionPage、全 live、depth 3；`SECTIONS`（home/models.py）与 PRD §6:97-101 五名逐字比对**一字不差**（独立 grep 取证，非转引）。全库 Page 计 7（根+首页+五板块），零多余节点。

**Container 直达 404（IA-02）**：live 路由级五板块 `<slug>/jwc/` 全 404；serve 级由 pytest 真容器经完整 HTTP 栈实证（`test_ia02_container_url_returns_404` 建真实容器于**测试库**后 GET `/chronicle/jwc/`=404，不向 dev 库写假部门数据——处置正确）。

**Container 四类排除（IA §3.2 当前可验证位点，边界不误判）**：
- sitemap（IA-03）：INSTALLED_APPS 无 `wagtail.contrib.sitemaps`（base.py 亲核）、urls.py 无 sitemap 路由、live `/sitemap.xml`=404、pytest 守护测试在位。**空真+守护**为实现位点 M2.2 §10 的正确过渡态（IA §10 归属 M2.2），非缺陷；
- 默认搜索（IA-04）：`search_fields=[]` 索引级排除（模型+`test_container_excluded_at_index_level` 双证）+ 行为实测（真容器按标题/slug 三 query 检索零命中，对照板块页可检索证链路活着）；live 复测 query=校园纪事 命中 /chronicle/、query=jwc/教务处 零命中；
- 导航/默认列表：base.html 为 M1 官方脚手架，无任何消费页面树的导航/列表模板（空真）；bootstrap 不产生容器。实现位点 M2.2 §6（IA-06），边界记录，非缺陷。

**旧站命名（IA-01 后半）**：全仓 tracked git grep 院内逸事/软件资源/学院指南——代码零命中（唯一命中为 tests/test_ia_structure.py:37 断言字面量本身，属断言必需）；docs 命中均为治理文本（IA §2 弃用声明、PRD、审计、计划）；live 页面零出现。

**M3 内容模型零越界**：`class|import` + 六名（NoticePage/ArticlePage/MaterialPage/SoftwareToolPage/GuidePage/EventFieldsMixin）全仓**零定义零导入**；唯一字面命中为 departments/models.py:22 前瞻注释（说明 M3 将扩充白名单）。容器 `subpage_types=[]` 为文档化的 M2.1→M3 过渡态。

**bootstrap 幂等复验**：dev 库连续两跑均输出"新建 0，跳过 5，五板块就位 5/5"，SectionPage pk 集合前后一致（5）；pytest 幂等/零假内容三测试绿（二次执行 pk 集合不变、容器计数 0、首页下恰 5 子页）。

**层级/树形（IA §5）**：pytest 矩阵亲复跑绿——HomePage 仅根下可建；SectionPage 仅 HomePage 下（根/板块/容器下均否）；容器仅 SectionPage 下（根/首页/容器下均否）；`creatable_subpage_models()==[]`（不得跳容器/套容器）；Home→Section→Container 相邻三层 depth 递增、url_path=树径（方案 A 同构）；容器 `preview_modes=[]` 且 `is_previewable()` False。

## 3. §4.5 v1.2 秘密扫描 + 数据红线

- **扫描**：v1.2 双分支正则原样复跑，`docs/` 与全仓 tracked 两域均退出码 1 零输出（PASS）；`.env` 不在 Git（gitignore 亲证，tracked 者仅 `.env.example` 且逐项哑值）。
- **DB 红线**：t0/t1/t2 三时点快照（属主/字节尺寸）`peiligo_dev / peiligo_test / postgres / template0 / template1` **完全一致**（peiligo_dev 8246975 / peiligo_test 8328895 / postgres 7960255 / template0 7799311 / template1 8042175，属主 peiligo_dev_user/peiligo_test_user/chenjunxian 未变；public 表数 3/3 持平）；运行期轮询目击 pytest 确用 `peiligo_restart_test`（无 test_ 前缀库，TEST_DATABASE_URL 亲核指向该名）；全仓零 DROP authored（commit diff 内 "DROP" 3 处均为报告/提交信息文字）。本审查仅连 peiligo_restart_dev/test。

## 4. 发现（0×P2 / 0×P3 / 4×P4）

| # | 级 | 发现 | 处置建议 |
| --- | --- | --- | --- |
| 1 | P4 | 实现报告 §5-① 表述"M3 六名 grep 零命中"过强：departments/models.py:22 注释含其中 5 名（无 EventFieldsMixin）。实质（零定义零导入）成立，仅报告措辞与事实有出入 | 无需改码；后续报告将"零命中"限定为"零定义/零导入"口径 |
| 2 | P4 | LegacyNamingScanTests 扫描范围（home/departments/search/src/templates 的 py+html）未含 manage.py 与四个空壳 app 的 `__init__.py`（均为空文件）及仓库根 py | 影响可忽略（全仓 git grep 已零命中兜底）；可将扫描范围改为仓库根 rglob 全量后排除 tests |
| 3 | P4 | bootstrap 汇总行"五板块就位 N/5"按 slug 占位计数不区分页型：病态场景（非板块页占位 slug）逐页 WARNING 已打但汇总仍显 5/5，输出语义略歧义 | 可选：汇总改为按 SectionPage 类型计数 |
| 4 | P4 | `HomePage.parent_page_types=["wagtailcore.Page"]` 为 Wagtail 根限定惯用式，但类型级语义=可挂任意未限制子页的裸 Page 父级；本仓所有具体类型均已限子页，实际可达父级仅根，无现实缺口 | M4.2 权限树原型阶段保持知悉即可（树深/层级测试在位兜底） |

## 5. 边界记录（按 IA §5/§13 记边界，不误判为缺陷）

1. sitemap 排除与 noindex 位点 → M2.2 §10（IA-03 当前为空真+守护）；导航/默认列表排除 → M2.2 §6（IA-06）；
2. 容器 subpage 白名单空 → M3 落地内容页类型时扩充（IA §5 表）并同步 `test_container_allows_no_subpages_yet`；
3. `Page.url` 多 Site 缓存态退化 → 测试已改用 `get_url_parts()`（产品无缺陷，A1 既有现象）；
4. treebeard.E001 ×5 → 上游弃用提示，README 已记，升级 Treebeard 6 前无动作；
5. A2.1 diff 仅涉 home/departments/templates/tests/README/迁移/报告（13 文件 +515/-5），零 settings/requirements 改动（ADR-0001 钉版未动），docs/ 仅新增 A2.1.impl.md。

## 6. 审查纪律

未读实现者 transcript；未改任何实现文件（本报告为唯一新增）；未 push；未 force/reset --hard；旧库零触碰零 DROP。审查产物：.venv-review/（未入库）。**A2.1 判定：PASS。**
