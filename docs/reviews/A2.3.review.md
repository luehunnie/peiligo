# A2.3 独立工程审查报告（M2 视觉恢复实现）

- **步骤**：A2.3 · M2 Visual Recovery 实现（`arch/a2.3-m2-visual-recovery` @ `aa8a025`，单提交，基于 `rebuild/v1` @ `24d54b5`）
- **审查范围**：diff `24d54b5..aa8a025`（11 文件，+743/−77：合同 md×1、视觉证据 png×5、模板×3、新增 include×1、`peiligo.css`×1）
- **审查会话**：Orca `arch/a2.3-review`（全新会话，不继承实现上下文；仅工程/回归/边界审查，不重裁视觉——M2 Visual / Human Acceptance 已正式 PASS）
- **审查日期**：2026-08-21
- **VERDICT**：**PASS**（0×P2 / 0×P3 / 6×P4）

## 0. 审查环境（自建，未复用任何兄弟 worktree venv）

```bash
~/.pyenv/versions/3.13.11/bin/python -m venv .venv     # Python 3.13.11（README 钉版）
.venv/bin/pip install -r requirements.txt              # 全 == 钉版
.venv/bin/pip install -r requirements-dev.txt
.venv/bin/pip install -e .
.venv/bin/python -c "import peiligo; print(peiligo.__file__)"
# → <USER_HOME>/orca/workspaces/peiligo_restart/a2.3-review/src/peiligo/__init__.py
```

关键钉版实测：`wagtail==7.4.2`、`Django==5.2.17`、`modelsearch==1.3.2`、`psycopg==3.3.4`（ADR-0001 组合）。PG 经本机 socket（`/tmp/.s.PGSQL.5432`，postgresql@18）；测试库仅用 `TEST_DATABASE_URL=peiligo_restart_test`（pytest-django 自建自清，跑毕实测无残留）；旧库 `peiligo_dev`/`peiligo_test` 全程只读快照核对（见 §8）。分支首步已规范化 `git branch -m a2.3-review arch/a2.3-review`；worktree 守卫核验：`git rev-parse --git-common-dir` 归属主仓 `<LOCAL_PROJECT_PATH>_restart/.git` 且本 worktree 在 `git worktree list` 中。未进入旧仓 `<LOCAL_PROJECT_PATH>`。

## 1. 合同一致性（必查 1）——PASS

唯一 SSOT `docs/frontend/M2_VISUAL_RECOVERY_CONTRACT.md` 逐节对照实现：

| 合同条款 | 实现证据 | 判定 |
| --- | --- | --- |
| §2 tokens DIRECT_REUSE | `peiligo.css:10-62` `:root` 八冻结色（#f5f7fa/#fff/#1f2a37/#64748b/#2f7a6e/#266359/#e8f1ee/#e2e8f0）+1120px+8px 阶（8/12/16/24/40/56）+6/8px 圆角+subtle 阴影+中文系统字体栈，与 §3 逐值吻合 | ✓ |
| §2 字体/排版 | 16/1.7 正文、14 小字、H1 32/1.3/700、H2 24/600、H3 20/600（`--font-size-*`/`--line-height-*`） | ✓ |
| §2 Header/Footer ADAPT | Header 白底 64px 单行+青绿字标（`.brand`）+固定七项导航；Footer 白底细上边框两栏+版权细线行（`.footer-meta`） | ✓ |
| §2 空态卡 DIRECT_REUSE | `.empty-state` 居中白底**虚线**卡（`peiligo.css:508-515`） | ✓ |
| §2 Vue REJECT | 见 §5（零 JS） | ✓ |
| §4 首页 | Hero（eyebrow“校园信息与资源入口”+H1+lede）→搜索工作台（`form GET {% url 'search' %}`+可见 label+具体 placeholder+唯一主按钮）→五板块 3+2 卡组（`grid 6 列 span2×3+span3×2`）+`site-note` 非业务反馈引导；无空业务区标题/骨架屏/假模块 | ✓ |
| §4.3 冻结文案 | `includes/section_positioning.html` 五句定位与 `docs/INFORMATION_ARCHITECTURE.md` §2 表**逐字一致**（chronicle/events/materials/software/guide 五句人工逐字比对零差异，含 else 兜底） | ✓ |
| §5 板块页 | 共享 Header→面包屑→`page-head`（H1+定位）→`empty-state`（“本板块暂无内容”+行动说明+其余四板块+返回首页）；不渲染列表/容器；旧 `<h2>本板块内容</h2>` 空业务区标题已移除 | ✓ |
| §6 响应式/无障碍 | 768px 两列（nth-child 同特异度源序覆盖，注释自证）/600px 单列+16px gutter+搜索堆叠全宽；focus-visible 2px 品牌描边 offset 2px；aria-current 色+字重双提示；44px 触控（10px padding+16px/1.7）；`prefers-reduced-motion` 关停、过渡全为 150ms 色彩 | ✓ |
| §7 实现范围 | 改动=base.html/home_page.html/section_page.html/section_positioning.html（includes 展示片段）/peiligo.css，全部在允许清单内；零 JS 调整、未越清单半步 | ✓ |
| §8 Do Not | 无 Vue/SPA、无 M3 模拟、无页面树/URL/权限改动（diff 零 models/urls/migrations）、无假数据；grep `gradient|backdrop-filter|blur(|neon|glass` 于 peiligo.css **零命中**；旧站弃用三名（院内逸事/软件资源/学院指南）于模板/静态**零复入** | ✓ |

**渲染级冒烟**（进程内 Django test client，dev 库 migrate+`bootstrap_sections` 幂等后）：`/` 200 含 `css/peiligo.css`/`hero`/`eyebrow`/`search-bench`/`role="search"`/`section-grid`/`card-desc`/`site-note`/`footer-meta`/svg 搜索图标/skip-link/aria-current；`/chronicle/` 200 含 `page-head`/`section-lede`/`empty-state`/冻结定位文案；`/search/` 200+noindex+canonical；未知路径 404；七项导航全可见。`search.html` 与 `404.html` **零改动**（`git diff` 0 行），CSS 以结构选择器（`.template-searchresults main ul`/`form[role="search"]`/`.template-404 .section-links`）统一视觉，与模板结构逐一匹配。视觉证据五图在 `docs/frontend/evidence/`（58–151KB 非空；审美裁决以 Human Acceptance PASS 为准，本审查不重裁）。

## 2. diff 工程质量（必查 2）——PASS

- `peiligo.css` +670 行为旧 M2.2 工程占位样式（60rem 容器/#2f6fed 蓝焦点/system-ui）**全量重写**为 token 体系，删除侧无遗留类引用（skip-link/breadcrumb/nav 等在新文件全部重定义，冒烟实证在页）；品牌色集中 `:root` 定义，组件内零复制色值（文件内自注释约定且实测遵守）。
- 模板改动全部服务端 Django 模板语法；`site_brand`/`feedback_email` 上下文消费可溯源至基线 `src/peiligo/context_processors.py::site_chrome`（A2.2 已有，零新增处理器）；mailto `&` 正确转义为 `&amp;`；`{% now "Y" %}` 服务端年份。
- `home/templates/home/home_page.html:8-14` 扩展位注释同步更新（“网格之前”→“Hero 之前”），与 IA §7.1 顺序约束一致。
- 无违合同新发明：无网络字体、无外部资源引用（唯一外部依赖为本地 static CSS）、无内联样式、无 `!important` 滥用（仅 reduced-motion 关停 1 处，属无障碍惯例）。

## 3. 自动测试亲自复跑（必查 3）——PASS

```bash
.venv/bin/python -m pytest -q   # → 65 passed, 50 subtests passed in 9.63s
```

基线 65 绿零回归（`tests/` 在 diff 中零改动，数量/文件完全一致）。注：首次跑出现 1 failed（production settings 导入）系本审查 shell 少导 `ALLOWED_HOSTS` 所致（`base.py:109-117` `env_required` 快速失败设计，A1.1 行为），补齐 `.env.example` 全套哑值后全绿——非实现回归，记录备考。

## 4. M3 未越界（必查 4）——PASS

```bash
grep -rnE 'class (NoticePage|ArticlePage|EventPage|MaterialEntry|SoftwareEntry|GuideEntry|Notice|Article|FeaturedItem)|EventFieldsMixin|Orderable|ParentalKey|ClusterableModel|register_snippet' \
  --include="*.py" src/ home/ search/ departments/ notices/ resources/ guides/ frontend/ tests/
# → 零命中（exit=1）
```

全仓 Page 类型仅三个既有结构类型：`HomePage`/`SectionPage`（`home/models.py:15,38`）、`DepartmentContainerPage`（`departments/models.py:5`）；`home/models.py`+`departments/models.py` 业务字段计数 **0**。diff 零 models/urls/migrations/views 改动。无假通知/假活动/假数量（模板空态即真实状态）。

## 5. Vue/SPA 零回归（必查 5）——PASS

```bash
git diff 24d54b5..aa8a025 | grep -inE '\.js\b|<script|vue|axios|fetch\(|webpack|vite|npm|import '
# → 仅 2 命中，均为合同 md 的 REJECT 规则文本自身，非代码
grep -rn "vue\|Vue" static/ templates/ home/templates/    # → 零命中
git diff 24d54b5..aa8a025 -- requirements*.txt package.json # → 0 行
```

diff 无 .js 文件、无 `<script>` 新增（`base.html:96` 的 `peiligo.js` 引用系骨架基线且未入 diff）；`static/js/peiligo.js` 实测 **0 字节**；零构建链、零新依赖。纯 SSR（ADR-0002）维持。

## 6. 秘密扫描（必查 6）——PASS

`docs/plans/00_MASTER_PLAN.md` §4.5 v1.2 原式复跑（双分支分别验证）：

```bash
grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
# → 无输出，exit=1（零命中）
# 分支①键值对形式：exit=1；分支②裸 dcap_ 令牌：exit=1
```

含本报告写入后复扫（见 §9 提交前复核），亦零命中。

## 7. 全量自测（必查 7）——PASS

| 命令 | 结果 |
| --- | --- |
| `.venv/bin/python manage.py check` | exit=0；5 条 `treebeard.E001` WARNING（README 记载口径：M1 基线 3 条+A2.1 两个新 Page 类型=5 条，上游 django-treebeard 5.3 对 Wagtail 7.4 的弃用提示） |
| `.venv/bin/python manage.py makemigrations --check --dry-run` | exit=0，`No changes detected`（零漂移） |
| `.venv/bin/python -m pytest -q` | `65 passed, 50 subtests passed` |
| `.venv/bin/ruff check .` | `All checks passed!` |
| `.venv/bin/ruff format --check .` | `71 files already formatted` |

## 8. 旧库零触碰（三时点核对，全程只读）

| 时点 | peiligo_dev | peiligo_test |
| --- | --- | --- |
| t0（审查开始） | peiligo_dev_user / 8246975B / 3 表 | peiligo_test_user / 8328895B / 3 表 |
| t1（全部命令跑毕） | 同上，零变化 | 同上，零变化 |
| t2（提交后，见 §9） | 同上，零变化 | 同上，零变化 |

`peiligo_restart_test` 由 pytest-django 自建自清（t1 实测库不存在残留）；全仓 diff 零 DROP 语句。

## 9. 发现分级

**P2：无。P3：无。**

P4（观察项，均不阻塞合并）：

1. **token 外零散字面值**：`peiligo.css` 存在少量非冻结 token 值——`gap: 6px`（导航图标间距 :263）、`max-width: 640px`（搜索工作台/搜索页表单 :409,:547）、`padding: 10px …`（44px 触控目标推导）、`line-height: 1.6`（card-desc）、`flex: 1 1 20rem`（footer）。合同 §3 禁“随意值泛滥”——实测量少且各有功能理由，不构成泛滥；建议 M3 扩组件前升格 token（如 `--control-height`、`--space-2xs`、`--bench-width`）。
2. **任务口径笔误**：派发令写“3 条 treebeard.E001”，README 与实测均为 **5 条**（A2.1 起口径）。非实现问题，登记口径修正。
3. **`.template-searchresults main ul` 结构选择器脆弱**：search.html 零改动约束下以“main 内唯一 ul”定位结果列表；若 M3 在搜索页 main 内新增其他 ul 将被误样式化。M3.4/MB10 改模板时建议补显式 class。
4. **首页 mailto subject=「首页」信息量低**：`home_page.html:53` `feedback_subject=page.title` → 邮件主题为“首页”；正文 body 带 `build_absolute_uri` 已可定位，误导性为零，M3 站点级配置终判时可一并优化。
5. **搜索页表单结构异源**：search.html（零改动基线）无 `.search-controls`/`.search-hint` 骨架，纯 CSS 网格语法统一后与首页工作台视觉接近但不完全一致——属 M2.2“保持可用”既定边界（模板内注释自证），M3.4/MB10 终判。
6. **`request.build_absolute_uri` 隐式依赖**：mailto body 依赖 `django.template.context_processors.request`（Wagtail 默认启用，测试+冒烟实证正常）；后续若精简 context processors 需保留该项，登记工程提示。

## 10. 审查边界声明

仅新增本报告文件，未触任何实现文件；未 push/PR/force/reset --hard/clean -fd；未调用 Codex/GPT；未进入旧仓；未读 audit 报告；未重裁 Human 已 PASS 的视觉裁决。全部结论基于本 worktree 亲自复跑取证。

提交前复核（本次提交后执行并回填）：§4.5 复扫含本报告零命中；t2 快照与 t0/t1 一致；`git show --stat HEAD` 仅 +1 文件（本报告）。
