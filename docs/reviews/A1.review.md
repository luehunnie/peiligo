# review(A1) · M1 工程基座独立审查报告

PASS

| 项目 | 内容 |
| --- | --- |
| 审查对象 | A1 · M1 工程基座（官方 wagtail start 干净骨架落地），提交 `8e229f7`，分支 `m1-engineering-foundation`（worktree `basslet`） |
| 审查人 | 独立 Reviewer 会话（不继承实现对话上下文；只写本报告与提交，未改任何实现文件） |
| 审查日期 | 2026-08-18 |
| 结论 | **PASS**（0×P2 / 1×P3 / 4×P4；八项必查全部亲自复跑通过；P3 为 S1 规格项的未记录延期，建议合并前由协调者裁决补记，不构成本步合并条件） |
| 输入 | A1 提交与 `git diff --stat`；`docs/adr/0001/0002/0003`；`docs/plans/03_DELIVERY_OPERATIONS_PLAN.md` §2/S1；`docs/decisions/ROLLING_IMPLEMENTATION_POLICY.md`；实现者完成报告（以 A1 提交信息为其入库镜像——跨终端 transcript 属 00 §4.5 代理禁区，不予读取） |

## 1. 前置状态核验

- worktree 干净（`git status --porcelain` 空），HEAD = `8e229f7`；`rebuild/v1`（`a397794`）为本分支祖先——批次基线非旧基线。
- 提交 40 文件、+1101 行，全部位于仓库根骨架/配置层（`src/`、五个空壳 app、根级 `home`/`search`/`templates`/`static`/`tests`、`requirements*`、`pyproject.toml`、`.env.example`、`.gitignore`、`README.md`、`manage.py`）；**零 `docs/` 改动、零 `.github/`/`Dockerfile`/`compose*`/`scripts/`**（S1「允许修改文件范围」合规）。
- 本地无 remote（`git remote -v` 空）——「未 push」成立。

## 2. 八项必查亲自复跑结果

### 2.1 Python 版本

```
$ .venv/bin/python --version
Python 3.13.11
```

✓ 3.13.x（ADR-0001 约束 3；pyproject `requires-python = ">=3.13,<3.14"` 一致）。

### 2.2 依赖版本核对（ADR-0001 四项约束）

```
$ .venv/bin/pip list | grep -iE "^(wagtail|Django|modelsearch)"
Django        5.2.17
modelsearch   1.3.2
wagtail       7.4.2
```

✓ wagtail 7.4.2（区间 `>=7.4.2,<7.5`）、django 5.2.17（`>=5.2,<5.3`）、modelsearch 1.3.2（`>=1.3.2,<1.4`）——与 ADR-0001「实际锁定」逐项一致。dev 链 pytest 9.1.1 / pytest-django 4.14.0 / ruff 0.16.3。

钉版完整性：`pip freeze` 与 `requirements.txt` 逐行比对，运行时 33 包 **全部 `==` 钉版、零浮动**；diff 差集仅为 `-e` 自身安装与 dev 工具链（含 iniconfig/packaging/pluggy/Pygments），无额外运行时漂移。另核对上游 v7.4.2 官方模板 requirements 为浮动区间（`Django>=6,<6.1`）——本实现改钉 Django 5.2.17 属 ADR-0001 要求的正确收敛（官方模板缺省会解析到 Django 6.x 违反区间，提交信息已如实披露该风险）。

### 2.3 manage.py check / migrate

```
$ .venv/bin/python manage.py check
System check identified some issues: WARNINGS: treebeard.E001 ×3
check exit: 0
$ .venv/bin/python manage.py makemigrations --check --dry-run
No changes detected   (exit 0)
$ .venv/bin/python manage.py migrate
No migrations to apply   (exit 0)
```

✓ 全绿。3 条 `treebeard.E001` 为上游 django-treebeard 5.3 对 Wagtail 7.4 管理器的弃用提示（退出码 0，Wagtail 7.4.2 官方组合一致），README:62-64 已记录——非新增问题。迁移零漂移。

### 2.4 pytest

```
$ .venv/bin/python -m pytest -q
10 passed in 3.09s   (exit 0)
```

✓ 全绿。覆盖 S1 测试要求：首页 200、admin 登录页 200（test_smoke.py）、三套 settings 可导入（test_settings.py 参数化）、无硬编码 `127.0.0.1` 防 R5 复发（test_settings.py 扫描 settings 包/conftest/pyproject）。

### 2.5 Ruff

```
$ .venv/bin/ruff check .
All checks passed!   (exit 0)
$ .venv/bin/ruff format --check .
56 files already formatted   (exit 0)
```

✓ 零告警、零格式偏差。

### 2.6 最小启动 smoke（runserver 后访问、记录响应即停）

```
$ .venv/bin/python manage.py runserver 127.0.0.1:8137 --noreload   # 后台启动
GET /                      → HTTP 200（6800B，正文含 "Welcome to your new Wagtail site!"）
GET /admin/                → 302 → /admin/login/?next=/admin/ → HTTP 200（12239B，"Sign in to Wagtail"）
GET /search/?query=home    → HTTP 200（806B）
$ kill <pid> → ps 复核 server stopped
```

✓ 首页与 Wagtail admin 均可达（`/admin/` 挂载于 wagtailadmin urls；`/cms/` 不存在于本骨架，任务口径「/admin/ 或 /cms/」取前者）。服务已停止，无残留进程。

### 2.7 秘密扫描（00 §4.5 v1.2 口径原样复跑）

```
$ grep -rInE '(SECRET|TOKEN|PASSWORD|API_KEY|dcap_|BEGIN (RSA|OPENSSH))[=: ]+[A-Za-z0-9_./+-]{8,}|dcap_[A-Za-z0-9][A-Za-z0-9_-]{7,}' docs/
（无输出）grep exit: 1
```

✓ 零命中（判据=退出码 1 且无输出）。加做扩展扫描（全仓、排除 `.venv/.git/.pytest_cache/.ruff_cache/db.sqlite3`）同样零命中；`.env.example` 仅 `DATABASE_URL=sqlite:///db.sqlite3` 哑值，无真实秘密。

### 2.8 越界检查（无 M2+ 提前实现）

- **页面树/业务模型**：全仓唯一 Page 子类为 `home/models.py` 的 `HomePage(Page): pass`（官方最小占位，零业务字段）；五个空壳 app（departments/notices/resources/guides/frontend）各仅 `__init__.py`。
- **EventFieldsMixin / 权限 / 发布**：`grep -rniE "EventFieldsMixin|Group|Permission|workflow|go_live_at|expire_at|ScheduledPublish"` 于全部业务目录与 src/tests——零命中。
- **搜索业务**：`search/views.py`、`templates/base.html` 与上游 v7.4.2 官方模板逐字一致（默认 database 后端，无中文搜索/M6 预置）。
- **NFR/Docker/CI**：无 Dockerfile/compose/docker/ 目录、无 `.github/`、无 `scripts/`；模板无 SEO/noindex/a11y 超出官方脚手架的增量。

✓ 无越界。

## 3. 附加复验（S1 审查规格补充项）

- **`check --deploy`（production settings，环境变量法）**：exit 0，9 项告警 = 3×treebeard.E001（已记录）＋ W004/W008/W012/W016（HSTS/SSL 重定向/安全 Cookie——部署环境才有的项）＋ **W009/W020（SECRET_KEY 缺失、ALLOWED_HOSTS 为空，见 P3-1(d)）**。DEBUG=False、`ManifestStaticFilesStorage` 已按 production 生效。
- **官方模板保真**（GitHub `v7.4.2` tag 逐一核对）：SECRET_KEY 置于 dev.py 为官方原位（`django-insecure-` 官方前缀占位，非泄密）；dj-database-url 为本提交新增适配之一（官方模板不含）；「四处结构适配」（BASE_DIR/src 布局、DATABASE_URL 外置、模板与静态目录上提、STATIC_ROOT 独立）与代码中 `M1:` 注释逐一对得上。
- **branch 基线**：`rebuild/v1` 已含于本分支，无旧基线问题。

## 4. 发现与分级

### P3-1 · 03 计划 S1 规格项未实现且延期未在库内记录

S1（MB1）目标/输出中的以下项未落地，且除提交信息边界句「无……生产配置」部分覆盖外，无库内延期记录：

| 子项 | S1 规格 | 现状 |
| --- | --- | --- |
| (a) | 目标「本地与测试同用 PostgreSQL」 | 默认 SQLite（`DATABASE_URL` 未设时回退 sqlite，本地 `db.sqlite3` 与 pytest 实跑均 sqlite）；**psycopg 不在 requirements**（S1 输出明列） |
| (b) | 输出钉版清单含 gunicorn | 未钉版——属生产配置，边界句可覆盖 |
| (c) | 输出 dev 清单含 pip-audit | 不在 requirements-dev.txt |
| (d) | 输出「`.env.example`（DB URL、密钥、允许主机全部外置，哑值）」 | 仅 DATABASE_URL 一项；production.py 未从环境接线二者 → production settings 现不可直接运行（W009/W020 佐证） |

**影响**：不阻塞本步任何验收命令（2.1–2.8 全绿），S16/S19 前无实际后果；DATABASE_URL 逃生通道已在位（base.py:103-111），后续切换成本低但存在 sqlite/postgres 迁移行为差异的累积风险。
**依据**：ROLLING_IMPLEMENTATION_POLICY §2-4 明定 03 线规格为提前实现的「详细实现参考与验收口径来源」——偏离应实现或记录。
**建议处置**：S2 启动前由协调者二选一并留档——补齐 (a)(c)(d) 哑值/依赖，或显式记录延期裁决（落后续步骤 S16/S19 或 deferral 台账）。不构成本步合并条件。

### P4 观察（均不构成条件）

1. dev.py `SECRET_KEY` 为官方脚手架占位（官方原位原前缀），dev 下 `--deploy` W009 为预期输出，非泄密（§4.5 扫描零命中已证）。
2. `WAGTAILADMIN_BASE_URL` 保持官方缺省 `http://example.com`，留待后续步骤设置。
3. base.py:26-28 保留官方「Quick-start development settings」注释而 SECRET_KEY 在 dev.py——注释略悬空，纯外观。
4. 提交信息「仅 M1 注释四处结构适配」与 base.py 实际 6 处 `M1:` 注释实例的计数口径（空壳 app 单列于正文）——描述性差异，内容无失实。

## 5. 结论

**PASS**。八项必查全部亲自复跑通过（版本/依赖/检查/迁移/测试/lint/smoke/秘密扫描/越界），骨架与 ADR-0001/0002/0003 及 03 §2 结构逐项相符，测试真实覆盖 S1 冒烟要求，无 M2+ 越界。唯一 P3 为 S1 规格项的延期未记录问题，建议协调者在合并前或 S2 启动前显式裁决留档。本审查仅新增本报告文件，未触任何实现文件，未 push。
